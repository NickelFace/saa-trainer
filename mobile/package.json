{
  "name": "saa-trainer-mobile",
  "version": "1.0.0",
  "description": "SAA-C03 Trainer — Android wrapper around the offline web app",
  "private": true,
  "scripts": {
    "www": "bash ../scripts/build-app.sh www",
    "sync": "npm run www && npx cap sync android",
    "apk": "npm run sync && cd android && ./gradlew assembleDebug",
    "apk:release": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@capacitor/android": "^7.6.8",
    "@capacitor/app": "^7.1.2",
    "@capacitor/cli": "^7.6.8",
    "@capacitor/core": "^7.6.8"
  }
}
