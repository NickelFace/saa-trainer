window.SAA_THEORY_EN={"chapters":[{"id":"s3","title":"S3 — object storage","order":1,"svc":["S3"],"questions":[1,2,3,4,6,9,11,12,13,16,17,18,21,22,23,24,26,32,33,36,38,40,41,42,43,44,46,48,49,51,52,53,54,59,61,63,64,66,69,71,72,76,77,78,80,83,85,86,88,89,91,92,94,97,98,100,103,106,107,109,110,114,115,116,118,121,123,126,127,131,132,134,139,147,153,154,155,156,158,160,161,162,165,166,173,178,183,185,188,189,190,192,199,202,204,205,206,208,212,213,214,215,216,221,225,226,227,233,236,243,244,248,249,250,252,256,257,258,259,266,267,270,272,277,278,279,280,283,285,286,288,289,291,292,293,294,295,299,300,302,305,307,309,310,313,317,320,321,323,324,325,326,328,331,332,334,336,341,343,344,345,346,349,353,356,357,358,359,361,372,373,379,381,384,393,397,398,402,403,404,412,414,415,416,418,421,422,426,430,438,440,441,443,445,446,453,460,463,469,471,477,478,480,482,485,486,490,495,496,497,498,500,501,502,506,511,513,517,528,529,533,534,539,541,542,545,547,551,553,557,564,566,567,568,583,587,588,590,592,598,601,603,604,605,606,607,609,610,612,616,617,621,622,626,628,632,634,635,636,638,640,641,643,646,651,654,656,663,667,672,673,678,679,680,687,690,691,696,699,702,703,707,710,713,717,718,719,725,728,733,736,737,738,740,742,747,753,756,759,760,762,763,767,768,769,771,778,784,785,788,799,801,804,806,808,812,816,817,818,819,822,825,829,830,832,835,837,839,841,842,852,856,860,862,866,868,871,875,877,880,881,889,890,891,892,894,895,898,901,903,904,906,907,910,911,912,913,914,915,916,918,925,927,930,931,934,938,942,944,952,953,954,956,960,964,965,968,971,974,975,980,981,982,984,985,991,993,994,995,997,998,1000,1003,1005,1007,1009,1011,1014,1015,1016,1017],"html":"<h1>S3</h1>\n<p>Object storage: a flat key space inside a bucket, accessed over an HTTP API. It is not a file system and not a block device — no partial object overwrites, no POSIX semantics, no directories (the prefix <code>logs/2026/</code> is part of the key, not a folder).</p>\n<p>Baseline facts the exam treats as given:</p>\n<ul><li>a bucket name is globally unique, and a bucket lives in a single Region;</li><li>an object can be up to 5 TB, a single-operation upload up to 5 GB, beyond that it's multipart;</li><li>writes to any object are strongly consistent for reads immediately after the write is acknowledged (read-after-write) — there is no separate \"eventual consistency\" anymore;</li><li>durability of eleven nines comes from replication within the Region; availability depends on the storage class.</li></ul>\n<h2>Storage classes</h2>\n<div class='table-wrap'><table><thead><tr><th>Class</th><th>AZs</th><th>Min. duration</th><th>Min. size</th><th>Access</th><th>When it's chosen</th></tr></thead><tbody><tr><td>Standard</td><td>3+</td><td>none</td><td>none</td><td>instant</td><td>active data, unpredictable access</td></tr><tr><td>Intelligent-Tiering</td><td>3+</td><td>none</td><td>none</td><td>instant</td><td>access pattern unknown or changing</td></tr><tr><td>Standard-IA</td><td>3+</td><td>30 days</td><td>128 KB</td><td>instant</td><td>infrequent access, but fault tolerance needed</td></tr><tr><td>One Zone-IA</td><td>1</td><td>30 days</td><td>128 KB</td><td>instant</td><td>reproducible copies, losing the AZ is acceptable</td></tr><tr><td>Glacier Instant Retrieval</td><td>3+</td><td>90 days</td><td>128 KB</td><td>milliseconds</td><td>archive with rare but instant reads</td></tr><tr><td>Glacier Flexible Retrieval</td><td>3+</td><td>90 days</td><td>40 KB</td><td>minutes to 12 hours</td><td>archive, accessed a few times a year</td></tr><tr><td>Glacier Deep Archive</td><td>3+</td><td>180 days</td><td>40 KB</td><td>12–48 hours</td><td>long-term retention for regulatory reasons</td></tr><tr><td>Express One Zone</td><td>1</td><td>none</td><td>none</td><td>single-digit ms</td><td>hot data with a high request rate</td></tr></tbody></table></div>\n<p>Key pricing mechanics: IA and Glacier classes charge less for storage but more for read requests, plus there's a penalty for early deletion (you're billed the full minimum duration) and for objects below the minimum billable size. That's why millions of small files in Standard-IA end up costing more than in Standard — a classic trap in cost-optimization questions.</p>\n<p>Retrieval tiers from Glacier Flexible Retrieval: Expedited (1–5 minutes), Standard (3–5 hours), Bulk (5–12 hours, the cheapest). For Deep Archive: Standard (12 hours) and Bulk (up to 48 hours). If the question asks for retrieval \"within a few minutes,\" Deep Archive is disqualified by definition.</p>\n<h2>Lifecycle</h2>\n<p>Lifecycle rules move objects between classes and delete them based on age. Scope is set by prefix, tags, or object size.</p>\n<pre><code>Standard --(30 days)--&gt; Standard-IA --(90 days)--&gt; Glacier Flexible --(365)--&gt; Deep Archive --&gt; expire</code></pre>\n<p>Key things to remember:</p>\n<ul><li>transitions to cheaper classes are allowed; moving back up is only done by copying (<code>RestoreObject</code> returns a temporary copy, it doesn't change the storage class);</li><li>transition to IA is possible no earlier than 30 days after object creation;</li><li>in a versioned bucket, rules apply separately to current and noncurrent versions, and also to incomplete multipart uploads (<code>AbortIncompleteMultipartUpload</code>);</li><li>Intelligent-Tiering moves objects between tiers automatically and charges a per-object monitoring fee, so it's not cost-effective for very small objects.</li></ul>\n<p>If a requirement reads like \"data is needed for the first 30 days, then almost never, delete after 7 years, minimal manual work\" — that's exactly a lifecycle rule, not a Lambda script.</p>\n<h2>Versioning, Object Lock, MFA Delete</h2>\n<p>Versioning is enabled on a bucket and can never be turned off again — only suspended (Suspended). Deleting an object in a versioned bucket creates a delete marker; the object itself remains and keeps being billed. Hence the standard requirement \"protect against accidental deletion and overwrite\" = versioning.</p>\n<p>Object Lock is WORM storage, enabled only on a bucket with versioning turned on (on an existing bucket it requires a support request; on a new bucket it's set at creation time). Two modes:</p>\n<ul><li>Governance: a user with the <code>s3:BypassGovernanceRetention</code> permission can remove or shorten the retention period;</li><li>Compliance: nobody can, including the root user, until the retention period expires.</li></ul>\n<p>Legal Hold is an indefinite deletion block on a version, removed by a separate permission, and unrelated to the retention period.</p>\n<p>MFA Delete protects against deleting versions and disabling versioning; it can only be enabled by the root user, and only via CLI/API.</p>\n<h2>Replication</h2>\n<p>CRR (to another Region) and SRR (within the same Region) both require versioning on the source and destination, plus an IAM role for S3. Only new operations after the rule is enabled get replicated; existing objects are migrated with S3 Batch Replication.</p>\n<ul><li>a delete marker is replicated only if that's explicitly enabled; deleting a specific version is never replicated;</li><li>by default the replication chain doesn't continue (a replica isn't replicated further) — Replica Modification Sync exists for that;</li><li>S3 Replication Time Control (RTC) provides a 15-minute SLA and metrics — the answer whenever a question needs predictable replication time;</li><li>object ownership in the destination account is changed with the Owner Override option.</li></ul>\n<p>Why it's tested: CRR addresses requirements to keep a copy in another Region, to reduce latency for readers in a different Region, and to isolate environments.</p>\n<h2>Encryption</h2>\n<div class='table-wrap'><table><thead><tr><th>Option</th><th>Who holds the key</th><th>Who encrypts</th><th>Notes</th></tr></thead><tbody><tr><td>SSE-S3 (AES-256)</td><td>AWS</td><td>S3</td><td>on by default for all new objects</td></tr><tr><td>SSE-KMS</td><td>KMS</td><td>S3</td><td>audited in CloudTrail, controlled by key policy, subject to KMS limits</td></tr><tr><td>DSSE-KMS</td><td>KMS</td><td>S3</td><td>double encryption for specific regulatory requirements</td></tr><tr><td>SSE-C</td><td>client</td><td>S3</td><td>key is passed with every request, HTTPS only</td></tr><tr><td>client-side</td><td>client</td><td>client</td><td>S3 only ever sees already-encrypted bytes</td></tr></tbody></table></div>\n<p>S3 Bucket Keys cut KMS request volume by up to 99 percent — the right answer when the complaint is KMS cost or throttling during bulk uploads.</p>\n<p>The requirement \"encrypt everything put into the bucket\" is met with a bucket policy that <code>Deny</code>s requests missing the <code>x-amz-server-side-encryption</code> header, while \"traffic must be TLS only\" is met with a policy condition on <code>aws:SecureTransport: false</code>.</p>\n<h2>Access</h2>\n<ul><li>Block Public Access — four account- and bucket-level flags that override any permissive policies. The first answer for \"the bucket accidentally became public.\"</li><li>Bucket policy — resource-based, works for cross-account access and for conditions (<code>aws:SourceIp</code>, <code>aws:SourceVpce</code>, <code>aws:PrincipalOrgID</code>).</li><li>ACL — a legacy mechanism, disabled by default (Object Ownership = Bucket owner enforced). In modern answers, ACL is almost always the wrong option.</li><li>Presigned URL — temporary access to a specific object on behalf of whoever signed it, without issuing credentials.</li><li>Access Points — separate entry points with their own policies and names, useful when dozens of applications share one bucket and the bucket policy is getting unwieldy. Multi-Region Access Point adds a single global endpoint on top of buckets in different Regions.</li><li>Object Lambda Access Point — transforms an object on the fly (masking PII, redaction) without creating a second copy of the data.</li><li>VPC gateway endpoint for S3 — private access from a VPC without internet access and without NAT data-transfer charges.</li></ul>\n<h2>Performance and uploads</h2>\n<ul><li>5,500 GETs and 3,500 PUTs per second per prefix; scaling is achieved with different prefixes — random hash prefixes on keys are no longer needed;</li><li>multipart upload: mandatory from 5 GB, recommended from 100 MB, parts upload in parallel and are retried individually;</li><li>S3 Transfer Acceleration routes traffic through the nearest CloudFront edge location to the bucket — the answer for \"fast uploads of large files from multiple countries into one bucket\";</li><li>byte-range fetch speeds up reading large objects in parts;</li><li>S3 Select reads a subset of CSV/JSON/Parquet data directly within S3;</li><li>Storage Lens and Storage Class Analysis show what to move and where.</li></ul>\n<h2>What the exam asks</h2>\n<ol><li>\"Minimum storage cost with quarterly access, retrieval within minutes\" — Glacier Flexible Retrieval, not Deep Archive (that's hours) and not IA (storage costs more there).</li><li>\"Unknown or changing access pattern, no manual work\" — Intelligent-Tiering, not a lifecycle rule: a rule requires knowing the timing up front.</li><li>\"Protection against deletion mandated by regulation, no one can override it\" — Object Lock in Compliance mode. Governance fits when a bypass capability is needed.</li><li>\"Files need to act as a network drive for multiple servers\" — that's not S3, it's EFS or FSx.</li><li>\"Speed up bulk uploads from remote offices\" — Transfer Acceleration or multipart; if the network is weak and the volume is hundreds of TB — Snowball.</li><li>\"Data sits in S3, need SQL analytics without moving it\" — Athena; for constant heavy analytics — Redshift.</li><li>\"A web application in an external account needs access to one specific object for an hour\" — presigned URL, not a public bucket and not an IAM user.</li><li>Any phrasing of \"bucket is public\" points to Block Public Access, not to editing the ACL.</li></ol>\n<h2>Numbers to remember</h2>\n<ul><li>minimum storage duration: Standard-IA and One Zone-IA — 30 days, Glacier Instant Retrieval — 90, Glacier Flexible Retrieval — 90, Deep Archive — 180;</li><li>minimum billable object size in those classes — 128 KB;</li><li>retrieval from Glacier Flexible Retrieval: Expedited 1–5 minutes, Standard 3–5 hours, Bulk 5–12 hours; from Deep Archive — 12 hours, up to 48 in Bulk mode;</li><li>Glacier Instant Retrieval returns objects in milliseconds, same as Standard-IA;</li><li>an object ranges from 0 bytes to 5 TB, a single PUT up to 5 GB, beyond that only multipart (up to 10,000 parts);</li><li>performance per prefix: 3,500 write requests and 5,500 read requests per second, any number of prefixes;</li><li>durability 99.999999999%, Standard availability 99.99%, One Zone-IA 99.5%;</li><li>transition to Standard-IA via a lifecycle rule is possible no earlier than 30 days after object creation.</li></ul>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> An archive is needed quarterly, retrieval within a few hours is acceptable, storage cost must be minimal. → Glacier Flexible Retrieval. Deep Archive is cheaper but takes 12 hours, while IA costs more to store.</p>\n<p><strong>2.</strong> The file access pattern is unknown in advance and keeps changing; no manual rules wanted. → Intelligent-Tiering: the class moves objects between tiers automatically, with no retrieval penalty.</p>\n<p><strong>3.</strong> A regulator requires that files can't be modified or deleted for five years, including by an administrator with full permissions. → Object Lock in Compliance mode. Governance can be bypassed by a user with a special permission.</p>\n<p><strong>4.</strong> An application in an external account needs one-hour access to a single object. → presigned URL. Neither a public bucket nor a dedicated IAM user is needed.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"903\" href=\"#\">questions 903</a> — prefix-based access for hundreds of teams: the correct answer is S3 Access Points; the dump key of S3 Batch Operations + ACL was corrected during the audit.</li><li><a class=\"qref\" data-q=\"974\" href=\"#\">questions 974</a> — a certificate for CloudFront in front of a bucket must be issued in us-east-1 regardless of the bucket's Region.</li><li><a class=\"qref\" data-q=\"971\" href=\"#\">questions 971</a> — disputed question: serving the same files to servers in different Regions is CloudFront caching, not Transfer Acceleration.</li><li><a class=\"qref\" data-q=\"835\" href=\"#\">questions 835</a> — disputed question: accessing S3 over Direct Connect is usually cheaper through a public VIF than through an interface endpoint.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>125</td><td>31%</td><td>27</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>64</td><td>16%</td><td>21</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>103</td><td>26%</td><td>69</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>109</td><td>27%</td><td>10</td></tr></tbody></table></div><p>Total questions in this chapter: <b>401</b>. With a written walkthrough: <b>401</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#1</b> — PERF: speed of aggregating data into a single bucket</li><li><b>#2</b> — PERF: choosing an analytics service for data in S3</li><li><b>#3</b> — SEC: restricting bucket access to the organization</li><li><b>#6</b> — PERF: transfer speed with limited bandwidth</li><li><b>#16</b> — PERF: choosing a BI platform for a data mart</li><li><b>#17</b> — SEC: EC2 access to S3 via a role</li><li><b>#44</b> — RES: protecting data from accidental deletion</li><li><b>#51</b> — RES: event-driven report generation and distribution</li></ul>","text":" S3 Object storage: a flat key space inside a bucket, accessed over an HTTP API. It is not a file system and not a block device — no partial object overwrites, no POSIX semantics, no directories (the prefix logs/2026/ is part of the key, not a folder). Baseline facts the exam treats as given: a bucket name is globally unique, and a bucket lives in a single Region; an object can be up to 5 TB, a single operation upload up to 5 GB, beyond that it's multipart; writes to any object are strongly consistent for reads immediately after the write is acknowledged (read after write) — there is no separate \"eventual consistency\" anymore; durability of eleven nines comes from replication within the Region; availability depends on the storage class. Storage classes Class AZs Min. duration Min. size Access When it's chosen : : : : : : Standard 3+ none none instant active data, unpredictable access Intelligent Tiering 3+ none none instant access pattern unknown or changing Standard IA 3+ 30 days 128 KB instant infrequent access, but fault tolerance needed One Zone IA 1 30 days 128 KB instant reproducible copies, losing the AZ is acceptable Glacier Instant Retrieval 3+ 90 days 128 KB milliseconds archive with rare but instant reads Glacier Flexible Retrieval 3+ 90 days 40 KB minutes to 12 hours archive, accessed a few times a year Glacier Deep Archive 3+ 180 days 40 KB 12–48 hours long term retention for regulatory reasons Express One Zone 1 none none single digit ms hot data with a high request rate Key pricing mechanics: IA and Glacier classes charge less for storage but more for read requests, plus there's a penalty for early deletion (you're billed the full minimum duration) and for objects below the minimum billable size. That's why millions of small files in Standard IA end up costing more than in Standard — a classic trap in cost optimization questions. Retrieval tiers from Glacier Flexible Retrieval: Expedited (1–5 minutes), Standard (3–5 hours), Bulk (5–12 hours, the cheapest). For Deep Archive: Standard (12 hours) and Bulk (up to 48 hours). If the question asks for retrieval \"within a few minutes,\" Deep Archive is disqualified by definition. Lifecycle Lifecycle rules move objects between classes and delete them based on age. Scope is set by prefix, tags, or object size. Standard (30 days) Standard IA (90 days) Glacier Flexible (365) Deep Archive expire Key things to remember: transitions to cheaper classes are allowed; moving back up is only done by copying ( RestoreObject returns a temporary copy, it doesn't change the storage class); transition to IA is possible no earlier than 30 days after object creation; in a versioned bucket, rules apply separately to current and noncurrent versions, and also to incomplete multipart uploads ( AbortIncompleteMultipartUpload ); Intelligent Tiering moves objects between tiers automatically and charges a per object monitoring fee, so it's not cost effective for very small objects. If a requirement reads like \"data is needed for the first 30 days, then almost never, delete after 7 years, minimal manual work\" — that's exactly a lifecycle rule, not a Lambda script. Versioning, Object Lock, MFA Delete Versioning is enabled on a bucket and can never be turned off again — only suspended (Suspended). Deleting an object in a versioned bucket creates a delete marker; the object itself remains and keeps being billed. Hence the standard requirement \"protect against accidental deletion and overwrite\" = versioning. Object Lock is WORM storage, enabled only on a bucket with versioning turned on (on an existing bucket it requires a support request; on a new bucket it's set at creation time). Two modes: Governance: a user with the s3:BypassGovernanceRetention permission can remove or shorten the retention period; Compliance: nobody can, including the root user, until the retention period expires. Legal Hold is an indefinite deletion block on a version, removed by a separate permission, and unrelated to the retention period. MFA Delete protects against deleting versions and disabling versioning; it can only be enabled by the root user, and only via CLI/API. Replication CRR (to another Region) and SRR (within the same Region) both require versioning on the source and destination, plus an IAM role for S3. Only new operations after the rule is enabled get replicated; existing objects are migrated with S3 Batch Replication. a delete marker is replicated only if that's explicitly enabled; deleting a specific version is never replicated; by default the replication chain doesn't continue (a replica isn't replicated further) — Replica Modification Sync exists for that; S3 Replication Time Control (RTC) provides a 15 minute SLA and metrics — the answer whenever a question needs predictable replication time; object ownership in the destination account is changed with the Owner Override option. Why it's tested: CRR addresses requirements to keep a copy in another Region, to reduce latency for readers in a different Region, and to isolate environments. Encryption Option Who holds the key Who encrypts Notes SSE S3 (AES 256) AWS S3 on by default for all new objects SSE KMS KMS S3 audited in CloudTrail, controlled by key policy, subject to KMS limits DSSE KMS KMS S3 double encryption for specific regulatory requirements SSE C client S3 key is passed with every request, HTTPS only client side client client S3 only ever sees already encrypted bytes S3 Bucket Keys cut KMS request volume by up to 99 percent — the right answer when the complaint is KMS cost or throttling during bulk uploads. The requirement \"encrypt everything put into the bucket\" is met with a bucket policy that Deny s requests missing the x amz server side encryption header, while \"traffic must be TLS only\" is met with a policy condition on aws:SecureTransport: false . Access Block Public Access — four account and bucket level flags that override any permissive policies. The first answer for \"the bucket accidentally became public.\" Bucket policy — resource based, works for cross account access and for conditions ( aws:SourceIp , aws:SourceVpce , aws:PrincipalOrgID ). ACL — a legacy mechanism, disabled by default (Object Ownership = Bucket owner enforced). In modern answers, ACL is almost always the wrong option. Presigned URL — temporary access to a specific object on behalf of whoever signed it, without issuing credentials. Access Points — separate entry points with their own policies and names, useful when dozens of applications share one bucket and the bucket policy is getting unwieldy. Multi Region Access Point adds a single global endpoint on top of buckets in different Regions. Object Lambda Access Point — transforms an object on the fly (masking PII, redaction) without creating a second copy of the data. VPC gateway endpoint for S3 — private access from a VPC without internet access and without NAT data transfer charges. Performance and uploads 5,500 GETs and 3,500 PUTs per second per prefix; scaling is achieved with different prefixes — random hash prefixes on keys are no longer needed; multipart upload: mandatory from 5 GB, recommended from 100 MB, parts upload in parallel and are retried individually; S3 Transfer Acceleration routes traffic through the nearest CloudFront edge location to the bucket — the answer for \"fast uploads of large files from multiple countries into one bucket\"; byte range fetch speeds up reading large objects in parts; S3 Select reads a subset of CSV/JSON/Parquet data directly within S3; Storage Lens and Storage Class Analysis show what to move and where. What the exam asks 1. \"Minimum storage cost with quarterly access, retrieval within minutes\" — Glacier Flexible Retrieval, not Deep Archive (that's hours) and not IA (storage costs more there). 2. \"Unknown or changing access pattern, no manual work\" — Intelligent Tiering, not a lifecycle rule: a rule requires knowing the timing up front. 3. \"Protection against deletion mandated by regulation, no one can override it\" — Object Lock in Compliance mode. Governance fits when a bypass capability is needed. 4. \"Files need to act as a network drive for multiple servers\" — that's not S3, it's EFS or FSx. 5. \"Speed up bulk uploads from remote offices\" — Transfer Acceleration or multipart; if the network is weak and the volume is hundreds of TB — Snowball. 6. \"Data sits in S3, need SQL analytics without moving it\" — Athena; for constant heavy analytics — Redshift. 7. \"A web application in an external account needs access to one specific object for an hour\" — presigned URL, not a public bucket and not an IAM user. 8. Any phrasing of \"bucket is public\" points to Block Public Access, not to editing the ACL. Numbers to remember minimum storage duration: Standard IA and One Zone IA — 30 days, Glacier Instant Retrieval — 90, Glacier Flexible Retrieval — 90, Deep Archive — 180; minimum billable object size in those classes — 128 KB; retrieval from Glacier Flexible Retrieval: Expedited 1–5 minutes, Standard 3–5 hours, Bulk 5–12 hours; from Deep Archive — 12 hours, up to 48 in Bulk mode; Glacier Instant Retrieval returns objects in milliseconds, same as Standard IA; an object ranges from 0 bytes to 5 TB, a single PUT up to 5 GB, beyond that only multipart (up to 10,000 parts); performance per prefix: 3,500 write requests and 5,500 read requests per second, any number of prefixes; durability 99.999999999%, Standard availability 99.99%, One Zone IA 99.5%; transition to Standard IA via a lifecycle rule is possible no earlier than 30 days after object creation. Mini practicum 1. An archive is needed quarterly, retrieval within a few hours is acceptable, storage cost must be minimal. → Glacier Flexible Retrieval. Deep Archive is cheaper but takes 12 hours, while IA costs more to store. 2. The file access pattern is unknown in advance and keeps changing; no manual rules wanted. → Intelligent Tiering: the class moves objects between tiers automatically, with no retrieval penalty. 3. A regulator requires that files can't be modified or deleted for five years, including by an administrator with full permissions. → Object Lock in Compliance mode. Governance can be bypassed by a user with a special permission. 4. An application in an external account needs one hour access to a single object. → presigned URL. Neither a public bucket nor a dedicated IAM user is needed. Bank audit {{q:903}} — prefix based access for hundreds of teams: the correct answer is S3 Access Points; the dump key of S3 Batch Operations + ACL was corrected during the audit. {{q:974}} — a certificate for CloudFront in front of a bucket must be issued in us east 1 regardless of the bucket's Region. {{q:971}} — disputed question: serving the same files to servers in different Regions is CloudFront caching, not Transfer Acceleration. {{q:835}} — disputed question: accessing S3 over Direct Connect is usually cheaper through a public VIF than through an interface endpoint. "},{"id":"ec2","title":"EC2 — compute and purchasing models","order":2,"svc":["EC2"],"questions":[1,4,5,7,8,11,12,14,17,18,20,21,24,25,29,31,32,35,37,40,41,42,47,48,50,52,54,55,58,59,61,63,67,69,70,73,74,75,77,83,84,91,92,94,96,99,100,101,102,104,110,111,112,113,114,115,116,118,120,123,124,125,127,128,129,130,132,133,138,140,141,142,143,145,146,150,152,161,162,163,164,167,170,171,174,176,178,179,180,181,182,183,185,186,187,188,190,192,193,194,195,196,197,198,203,205,206,208,209,210,211,213,217,218,219,220,221,224,225,226,229,231,232,234,236,237,238,241,242,244,245,246,248,249,251,252,253,254,257,261,262,263,264,265,266,268,271,272,274,275,276,277,282,283,285,287,290,294,296,297,298,299,300,302,303,304,305,306,310,312,315,316,317,318,319,320,323,324,327,328,329,332,333,334,335,336,337,340,342,343,347,351,353,355,357,358,369,370,375,377,380,382,383,384,386,388,390,391,392,394,396,397,401,402,405,409,410,411,413,417,419,421,423,424,425,427,429,430,434,437,439,441,443,444,449,450,452,453,456,458,459,461,462,464,465,466,470,473,480,486,492,494,497,502,503,505,508,509,513,514,520,527,528,529,537,543,545,549,552,554,555,556,559,562,566,567,570,574,580,581,584,585,589,592,594,595,596,602,608,610,611,612,614,618,622,627,630,632,635,637,639,642,646,650,654,655,656,658,660,661,666,671,674,675,676,678,680,681,682,683,686,690,691,693,694,697,699,701,704,707,708,709,710,714,715,719,720,721,723,724,729,730,732,734,735,738,744,746,749,752,753,754,756,757,758,760,762,764,766,768,770,772,773,775,779,782,783,789,790,791,794,795,802,813,814,815,816,818,822,824,826,831,832,834,836,837,838,839,840,841,842,843,845,846,849,852,853,854,857,860,865,866,867,872,873,874,875,876,877,879,882,884,885,887,892,894,895,897,900,901,902,904,905,906,908,910,911,912,914,915,917,919,920,921,923,927,930,933,934,935,937,938,940,944,948,949,955,956,963,964,966,968,969,972,973,976,977,980,984,986,987,988,991,993,994,995,997,998,999,1001,1004,1005,1006,1008,1012,1013,1014,1015,1017],"html":"<h1>EC2</h1>\n<p>A virtual machine in someone else's data center: you're responsible for the OS, patches, the application, and configuration; AWS is responsible for the hypervisor, hardware, and network. On the exam, EC2 almost always competes with managed services, and the question \"do we even need EC2 here\" is worth asking first.</p>\n<h2>Instance families</h2>\n<div class='table-wrap'><table><thead><tr><th>Family</th><th>Letters</th><th>Profile</th><th>Typical workloads</th></tr></thead><tbody><tr><td>General purpose</td><td>T, M</td><td>balanced</td><td>web servers, small databases, applications</td></tr><tr><td>Compute optimized</td><td>C</td><td>high CPU-to-memory ratio</td><td>batch processing, HPC nodes, video encoding</td></tr><tr><td>Memory optimized</td><td>R, X, z1d</td><td>high memory</td><td>in-memory caches, SAP HANA, large DBMS</td></tr><tr><td>Storage optimized</td><td>I, D, H</td><td>local NVMe/HDD</td><td>high-IOPS NoSQL, data warehouse nodes</td></tr><tr><td>Accelerated</td><td>P, G, Inf, Trn</td><td>GPU and accelerators</td><td>model training and inference, graphics</td></tr></tbody></table></div>\n<p>The T family runs on CPU credits: a baseline level plus accumulated credits. Once credits run out, in standard mode the instance is throttled; in unlimited mode it runs at full speed for an extra charge. Sustained high utilization on a T instance is a typical design mistake — the fix is to move to M or C.</p>\n<p>If the question says \"high memory utilization\" and is asking you to pick an instance type, the answer is memory optimized for every component with high memory usage, whether it's the application or the database.</p>\n<h2>Purchasing models</h2>\n<div class='table-wrap'><table><thead><tr><th>Model</th><th>Discount</th><th>Commitment</th><th>When</th></tr></thead><tbody><tr><td>On-Demand</td><td>none</td><td>none</td><td>unpredictable load, development, spikes</td></tr><tr><td>Savings Plans</td><td>up to 72%</td><td>hourly spend commitment for 1 or 3 years</td><td>baseline load, flexibility across instance types</td></tr><tr><td>Reserved Instances</td><td>up to 72%</td><td>specific configuration for 1 or 3 years</td><td>steady load, needs capacity reservation</td></tr><tr><td>Spot</td><td>up to 90%</td><td>none, interruption with 2-minute notice</td><td>fault-tolerant batch workloads</td></tr><tr><td>Dedicated Host</td><td>—</td><td>physical server</td><td>socket-bound licensing, regulatory requirements</td></tr><tr><td>Dedicated Instance</td><td>—</td><td>hardware-level isolation</td><td>isolation requirements without license binding</td></tr></tbody></table></div>\n<p>Distinctions the exam likes to test:</p>\n<ul><li>Compute Savings Plans apply to EC2, Fargate, and Lambda and aren't tied to a Region, family, or OS; EC2 Instance Savings Plans are cheaper but lock in a family and Region;</li><li>Standard RIs give the maximum discount but can't change family; Convertible RIs can be exchanged for a different configuration;</li><li>Capacity Reservation reserves capacity in an AZ and doesn't itself provide a discount; the discount comes from adding RIs or Savings Plans;</li><li>Spot can't be used where interruption is unacceptable; Spot Fleet and an ASG with a mixed instances policy blend On-Demand and Spot in a single group.</li></ul>\n<p>A typical design for a steady service with peaks: baseline capacity on Savings Plans or RIs, peaks on On-Demand, background batch jobs on Spot.</p>\n<h2>Instance storage</h2>\n<ul><li>EBS — a network volume that survives an instance stop, snapshots go to S3, encrypted with KMS.</li><li>Instance store — the hypervisor's physical disk, maximum IOPS, but data disappears on stop or host failure. The answer wherever you need a temporary cache or scratch space.</li><li>The root volume is deleted on terminate by default; additional volumes are not.</li></ul>\n<h2>Placement and networking</h2>\n<p>Placement groups:</p>\n<ul><li>Cluster — instances in a single AZ and a single rack, minimal latency and up to 100 Gbps between nodes; the answer for HPC and tightly coupled computation;</li><li>Spread — instances on distinct hardware, maximum 7 per AZ; the answer for a handful of critical nodes that must not fail together;</li><li>Partition — independent groups of racks, up to 7 partitions per AZ; the answer for HDFS, Cassandra, Kafka, where the cluster itself is aware of the topology.</li></ul>\n<p>Networking options: ENA and Enhanced Networking by default on modern types, EFA for HPC with MPI, multiple ENIs to separate management and data traffic.</p>\n<h2>Metadata, roles, and bootstrapping</h2>\n<ul><li>An IAM role is granted to an instance through an instance profile; keys baked into an AMI or stored in files are always the wrong answer.</li><li>IMDSv2 (session-oriented) protects against SSRF theft of temporary credentials — require it whenever a question is about metadata compromise.</li><li>User data runs once, on first launch, via cloud-init.</li><li>A golden AMI reduces launch time and makes configuration reproducible; EC2 Image Builder automates image building and patching.</li></ul>\n<h2>Lifecycle and maintenance</h2>\n<ul><li>Stop/Start moves the instance to a different host, and the public IPv4 address changes (unless it's an Elastic IP);</li><li>Hibernate saves RAM to the root volume and restores state;</li><li>Auto Recovery and Scheduled Maintenance events handle hardware degradation;</li><li>Systems Manager Session Manager gives console access without SSH, open ports, or a bastion host — the correct answer in nearly every question about \"how to safely reach an instance in a private subnet.\"</li></ul>\n<h2>What the exam asks</h2>\n<ol><li>\"Reduce the cost of steady, round-the-clock load without changing the architecture\" — Savings Plans or RIs, not Spot and not ASG.</li><li>\"Batch processing, the job can be restarted\" — Spot, often paired with SQS.</li><li>\"License tied to physical sockets, BYOL\" — Dedicated Host, not Dedicated Instance.</li><li>\"Latency between cluster nodes must be minimal\" — cluster placement group.</li><li>\"Instances must not fail together because of a single node\" — spread placement group.</li><li>\"Administrator access without open ports or keys\" — SSM Session Manager.</li><li>\"The application can't be rewritten, but OS administration needs to go away\" — first consider Elastic Beanstalk or containers, only then EC2 with automation.</li><li>\"Need peak disk performance only during processing\" — instance store or io2 Block Express, not a larger gp2 volume.</li></ol>\n<h2>Monitoring and limits</h2>\n<p>CloudWatch collects hypervisor-level metrics by default: CPU, disk, and network at the device level, plus status checks. The hypervisor can't see memory or file-system utilization — those come from the CloudWatch agent inside the OS. A question about \"need to alert on EC2 memory\" is always solved by installing the agent, not by built-in metrics.</p>\n<p>Detailed monitoring moves metrics from a 5-minute interval to 1 minute and affects how fast Auto Scaling reacts. Regional vCPU quotas are counted per family (On-Demand Standard and Spot separately) and are raised through Service Quotas — a frequent answer to \"can't launch the required number of instances.\"</p>\n<h2>EC2 versus managed services</h2>\n<div class='table-wrap'><table><thead><tr><th>Requirement</th><th>EC2 fits</th><th>Better choice</th></tr></thead><tbody><tr><td>Full control over OS and kernel needed</td><td>yes</td><td>—</td></tr><tr><td>Non-standard software with socket-based licensing</td><td>yes, Dedicated Host</td><td>—</td></tr><tr><td>Web application on a standard stack</td><td>possible</td><td>Elastic Beanstalk, ECS Fargate</td></tr><tr><td>Event-driven processing in seconds</td><td>no</td><td>Lambda</td></tr><tr><td>Batch jobs with a queue</td><td>possible</td><td>AWS Batch, ECS on Spot</td></tr><tr><td>Relational database</td><td>almost never</td><td>RDS, Aurora</td></tr></tbody></table></div>\n<p>Phrasing like \"minimize operational overhead,\" \"least management effort,\" \"no servers to manage\" almost always steers the answer away from EC2 toward a managed service. The reverse cue: \"legacy application that cannot be modified,\" \"requires access to the operating system,\" \"custom kernel modules\" — stay on EC2.</p>\n<h2>Licensing and compliance</h2>\n<p>License Manager tracks license usage tied to cores, sockets, or hosts and blocks launches beyond the limit. For BYOL Windows and SQL Server tied to physical hardware, you need a Dedicated Host, which exposes sockets and physical cores. Dedicated Instance isolates hardware but doesn't provide visibility or socket binding, so it's the wrong answer in licensing questions.</p>\n<h2>Numbers to remember</h2>\n<ul><li>billing is per-second with a 60-second minimum (Linux); Windows and some Marketplace images are billed hourly;</li><li>commitments run for 1 or 3 years: Compute Savings Plan gives up to 66% savings, EC2 Instance Savings Plan and standard Reserved Instances up to 72%, convertible ones around up to 54%;</li><li>Spot instances are up to 90% cheaper than On-Demand, and the interruption notice arrives 2 minutes ahead;</li><li>spread placement group: no more than 7 instances in one Availability Zone; partition — up to 7 partitions per zone; cluster — a single Availability Zone;</li><li>instance metadata is available at 169.254.169.254, IMDSv2 requires a session token, and that's exactly what questions about SSRF protection are asking for;</li><li>user data runs only on first launch by default;</li><li>an instance has two status checks: system status (AWS infrastructure) and instance status (its own operating system);</li><li>instance store data is lost on stop and terminate, EBS volumes survive stop;</li><li>Dedicated Host exposes sockets and cores — only it fits licenses tied to physical hardware.</li></ul>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> An application runs around the clock, but in six months it's planned to move to a different instance family. → Compute Savings Plan: the discount carries over across family, size, and even service changes.</p>\n<p><strong>2.</strong> Batch jobs run six hours overnight; losing an instance just means reprocessing. → Spot instances: exactly the workload profile they exist for.</p>\n<p><strong>3.</strong> Compute cluster nodes exchange data with each other, and network latency matters. → Cluster placement group in a single Availability Zone.</p>\n<p><strong>4.</strong> A commercial software license was purchased per socket and core, and the load is steady. → Dedicated Host with reservation: the license carries over, and the price is lower than On-Demand.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"554\" href=\"#\">questions 554</a> — an SAP application and SQL Server with high memory utilization: memory optimized for both components.</li><li><a class=\"qref\" data-q=\"245\" href=\"#\">questions 245</a> — disputed: reducing dev environment cost; swapping the target group by itself doesn't reduce the number of instances in the ASG.</li><li><a class=\"qref\" data-q=\"622\" href=\"#\">questions 622</a> — disputed: a sharp jump from thousands to millions of users, on-demand capacity scales instantly, provisioned Auto Scaling lags behind.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>115</td><td>24%</td><td>32</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>132</td><td>28%</td><td>39</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>127</td><td>27%</td><td>86</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>105</td><td>22%</td><td>13</td></tr></tbody></table></div><p>Total questions in this chapter: <b>479</b>. With a written walkthrough: <b>479</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#1</b> — PERF: speed of aggregating data into a single bucket</li><li><b>#5</b> — RES: shared access to documents from both instances</li><li><b>#17</b> — SEC: EC2 access to S3 via a role</li><li><b>#20</b> — PERF: fast clone with high I/O for testing</li><li><b>#25</b> — RES: buffer between API and database instead of scaling quotas</li><li><b>#31</b> — SEC: enforcing resource tag compliance</li><li><b>#47</b> — RES: guaranteed capacity across three zones</li><li><b>#50</b> — SEC: urgent vulnerability patching</li></ul>","text":" EC2 A virtual machine in someone else's data center: you're responsible for the OS, patches, the application, and configuration; AWS is responsible for the hypervisor, hardware, and network. On the exam, EC2 almost always competes with managed services, and the question \"do we even need EC2 here\" is worth asking first. Instance families Family Letters Profile Typical workloads General purpose T, M balanced web servers, small databases, applications Compute optimized C high CPU to memory ratio batch processing, HPC nodes, video encoding Memory optimized R, X, z1d high memory in memory caches, SAP HANA, large DBMS Storage optimized I, D, H local NVMe/HDD high IOPS NoSQL, data warehouse nodes Accelerated P, G, Inf, Trn GPU and accelerators model training and inference, graphics The T family runs on CPU credits: a baseline level plus accumulated credits. Once credits run out, in standard mode the instance is throttled; in unlimited mode it runs at full speed for an extra charge. Sustained high utilization on a T instance is a typical design mistake — the fix is to move to M or C. If the question says \"high memory utilization\" and is asking you to pick an instance type, the answer is memory optimized for every component with high memory usage, whether it's the application or the database. Purchasing models Model Discount Commitment When On Demand none none unpredictable load, development, spikes Savings Plans up to 72% hourly spend commitment for 1 or 3 years baseline load, flexibility across instance types Reserved Instances up to 72% specific configuration for 1 or 3 years steady load, needs capacity reservation Spot up to 90% none, interruption with 2 minute notice fault tolerant batch workloads Dedicated Host — physical server socket bound licensing, regulatory requirements Dedicated Instance — hardware level isolation isolation requirements without license binding Distinctions the exam likes to test: Compute Savings Plans apply to EC2, Fargate, and Lambda and aren't tied to a Region, family, or OS; EC2 Instance Savings Plans are cheaper but lock in a family and Region; Standard RIs give the maximum discount but can't change family; Convertible RIs can be exchanged for a different configuration; Capacity Reservation reserves capacity in an AZ and doesn't itself provide a discount; the discount comes from adding RIs or Savings Plans; Spot can't be used where interruption is unacceptable; Spot Fleet and an ASG with a mixed instances policy blend On Demand and Spot in a single group. A typical design for a steady service with peaks: baseline capacity on Savings Plans or RIs, peaks on On Demand, background batch jobs on Spot. Instance storage EBS — a network volume that survives an instance stop, snapshots go to S3, encrypted with KMS. Instance store — the hypervisor's physical disk, maximum IOPS, but data disappears on stop or host failure. The answer wherever you need a temporary cache or scratch space. The root volume is deleted on terminate by default; additional volumes are not. Placement and networking Placement groups: Cluster — instances in a single AZ and a single rack, minimal latency and up to 100 Gbps between nodes; the answer for HPC and tightly coupled computation; Spread — instances on distinct hardware, maximum 7 per AZ; the answer for a handful of critical nodes that must not fail together; Partition — independent groups of racks, up to 7 partitions per AZ; the answer for HDFS, Cassandra, Kafka, where the cluster itself is aware of the topology. Networking options: ENA and Enhanced Networking by default on modern types, EFA for HPC with MPI, multiple ENIs to separate management and data traffic. Metadata, roles, and bootstrapping An IAM role is granted to an instance through an instance profile; keys baked into an AMI or stored in files are always the wrong answer. IMDSv2 (session oriented) protects against SSRF theft of temporary credentials — require it whenever a question is about metadata compromise. User data runs once, on first launch, via cloud init. A golden AMI reduces launch time and makes configuration reproducible; EC2 Image Builder automates image building and patching. Lifecycle and maintenance Stop/Start moves the instance to a different host, and the public IPv4 address changes (unless it's an Elastic IP); Hibernate saves RAM to the root volume and restores state; Auto Recovery and Scheduled Maintenance events handle hardware degradation; Systems Manager Session Manager gives console access without SSH, open ports, or a bastion host — the correct answer in nearly every question about \"how to safely reach an instance in a private subnet.\" What the exam asks 1. \"Reduce the cost of steady, round the clock load without changing the architecture\" — Savings Plans or RIs, not Spot and not ASG. 2. \"Batch processing, the job can be restarted\" — Spot, often paired with SQS. 3. \"License tied to physical sockets, BYOL\" — Dedicated Host, not Dedicated Instance. 4. \"Latency between cluster nodes must be minimal\" — cluster placement group. 5. \"Instances must not fail together because of a single node\" — spread placement group. 6. \"Administrator access without open ports or keys\" — SSM Session Manager. 7. \"The application can't be rewritten, but OS administration needs to go away\" — first consider Elastic Beanstalk or containers, only then EC2 with automation. 8. \"Need peak disk performance only during processing\" — instance store or io2 Block Express, not a larger gp2 volume. Monitoring and limits CloudWatch collects hypervisor level metrics by default: CPU, disk, and network at the device level, plus status checks. The hypervisor can't see memory or file system utilization — those come from the CloudWatch agent inside the OS. A question about \"need to alert on EC2 memory\" is always solved by installing the agent, not by built in metrics. Detailed monitoring moves metrics from a 5 minute interval to 1 minute and affects how fast Auto Scaling reacts. Regional vCPU quotas are counted per family (On Demand Standard and Spot separately) and are raised through Service Quotas — a frequent answer to \"can't launch the required number of instances.\" EC2 versus managed services Requirement EC2 fits Better choice Full control over OS and kernel needed yes — Non standard software with socket based licensing yes, Dedicated Host — Web application on a standard stack possible Elastic Beanstalk, ECS Fargate Event driven processing in seconds no Lambda Batch jobs with a queue possible AWS Batch, ECS on Spot Relational database almost never RDS, Aurora Phrasing like \"minimize operational overhead,\" \"least management effort,\" \"no servers to manage\" almost always steers the answer away from EC2 toward a managed service. The reverse cue: \"legacy application that cannot be modified,\" \"requires access to the operating system,\" \"custom kernel modules\" — stay on EC2. Licensing and compliance License Manager tracks license usage tied to cores, sockets, or hosts and blocks launches beyond the limit. For BYOL Windows and SQL Server tied to physical hardware, you need a Dedicated Host, which exposes sockets and physical cores. Dedicated Instance isolates hardware but doesn't provide visibility or socket binding, so it's the wrong answer in licensing questions. Numbers to remember billing is per second with a 60 second minimum (Linux); Windows and some Marketplace images are billed hourly; commitments run for 1 or 3 years: Compute Savings Plan gives up to 66% savings, EC2 Instance Savings Plan and standard Reserved Instances up to 72%, convertible ones around up to 54%; Spot instances are up to 90% cheaper than On Demand, and the interruption notice arrives 2 minutes ahead; spread placement group: no more than 7 instances in one Availability Zone; partition — up to 7 partitions per zone; cluster — a single Availability Zone; instance metadata is available at 169.254.169.254, IMDSv2 requires a session token, and that's exactly what questions about SSRF protection are asking for; user data runs only on first launch by default; an instance has two status checks: system status (AWS infrastructure) and instance status (its own operating system); instance store data is lost on stop and terminate, EBS volumes survive stop; Dedicated Host exposes sockets and cores — only it fits licenses tied to physical hardware. Mini practicum 1. An application runs around the clock, but in six months it's planned to move to a different instance family. → Compute Savings Plan: the discount carries over across family, size, and even service changes. 2. Batch jobs run six hours overnight; losing an instance just means reprocessing. → Spot instances: exactly the workload profile they exist for. 3. Compute cluster nodes exchange data with each other, and network latency matters. → Cluster placement group in a single Availability Zone. 4. A commercial software license was purchased per socket and core, and the load is steady. → Dedicated Host with reservation: the license carries over, and the price is lower than On Demand. Bank audit {{q:554}} — an SAP application and SQL Server with high memory utilization: memory optimized for both components. {{q:245}} — disputed: reducing dev environment cost; swapping the target group by itself doesn't reduce the number of instances in the ASG. {{q:622}} — disputed: a sharp jump from thousands to millions of users, on demand capacity scales instantly, provisioned Auto Scaling lags behind. "},{"id":"scaling-elb","title":"Load Balancers and Auto Scaling","order":3,"svc":["ELB","Auto Scaling"],"questions":[5,7,8,12,14,19,21,29,30,32,35,40,41,52,59,60,62,63,69,70,75,79,81,82,104,111,112,116,120,123,125,128,130,132,138,141,142,143,145,146,163,169,170,171,174,180,183,188,190,192,195,203,205,209,210,213,217,219,229,230,234,236,244,245,246,248,257,261,264,265,266,268,271,272,275,276,281,282,286,290,297,298,302,303,306,310,327,328,333,335,337,340,342,345,355,357,358,367,369,377,378,382,385,388,389,390,391,397,401,405,408,409,411,413,422,427,434,437,441,443,444,447,450,461,462,464,466,473,479,502,505,509,516,520,527,530,531,537,541,545,559,565,567,570,572,575,580,581,587,589,591,592,593,594,595,596,600,608,611,614,616,622,625,627,637,639,642,644,647,654,655,660,666,674,676,677,683,691,692,693,694,697,700,701,704,707,714,716,720,721,724,729,730,735,744,749,753,754,758,773,775,782,790,810,813,815,822,823,824,832,836,843,846,852,865,866,871,874,876,879,884,892,894,897,900,902,905,914,917,921,923,927,928,930,933,935,937,944,945,948,954,955,961,963,964,973,976,984,986,987,993,995,999,1001,1002,1006,1008,1012,1015,1016],"html":"<h1>Load Balancers and Auto Scaling</h1>\n<p>The pairing \"ELB in front of an Auto Scaling group across multiple AZs\" is the baseline fault tolerance pattern on the exam. The load balancer provides a single entry point and health checks; the ASG is responsible for instance count and replacing failed instances.</p>\n<h2>Types of load balancers</h2>\n<div class='table-wrap'><table><thead><tr><th></th><th>ALB</th><th>NLB</th><th>GWLB</th><th>CLB (legacy)</th></tr></thead><tbody><tr><td>Layer</td><td>7 (HTTP/HTTPS/gRPC)</td><td>4 (TCP/UDP/TLS)</td><td>3 (GENEVE)</td><td>4 and 7</td></tr><tr><td>Routing</td><td>by host, path, header, query, method</td><td>by port</td><td>all traffic to appliances</td><td>basic</td></tr><tr><td>Performance</td><td>high</td><td>millions of requests, minimal latency</td><td>depends on appliance</td><td>low</td></tr><tr><td>Static IP</td><td>no, DNS only</td><td>yes, Elastic IP per AZ</td><td>no</td><td>no</td></tr><tr><td>Target types</td><td>instance, IP, Lambda</td><td>instance, IP, ALB</td><td>appliance</td><td>instance</td></tr><tr><td>TLS termination</td><td>yes</td><td>yes (TLS listener)</td><td>no</td><td>yes</td></tr><tr><td>Sticky sessions</td><td>load balancer or application cookie</td><td>by source IP</td><td>—</td><td>cookie</td></tr></tbody></table></div>\n<p>How the exam expects you to choose:</p>\n<ul><li>need URL/host parsing, authentication via Cognito or OIDC, WAF — ALB;</li><li>need UDP, a static IP, extremely low latency, TLS passthrough — NLB;</li><li>need to route traffic through third-party firewall appliances — GWLB;</li><li>\"WAF must protect it\" — WAF attaches to ALB, CloudFront, API Gateway, and AppSync, but not to NLB.</li></ul>\n<p>Useful details: ALB supports redirecting HTTP to HTTPS and returning fixed responses with no backend; a target group of type Lambda lets ALB invoke a function; cross-zone load balancing is always on and free for ALB, but disabled by default for NLB and must be turned on explicitly; serving an application across multiple domains uses SNI with multiple certificates.</p>\n<h2>Health checks</h2>\n<p>Checks happen at two levels: the load balancer checks a target over a protocol and path, and the ASG uses either EC2 status checks or the ELB health check result. If the ASG is configured to use only EC2 status checks, a hung application on a live instance won't be replaced — a classic trap. The correct answer: set the ASG health check type to ELB.</p>\n<h2>Auto Scaling group</h2>\n<p>Core parameters: launch template (versioned, recommended over launch configuration), min/desired/max, a list of subnets across different AZs, a warm-up policy, health check type and grace period, termination policy.</p>\n<p>Scaling policies:</p>\n<div class='table-wrap'><table><thead><tr><th>Policy</th><th>What it does</th><th>When</th></tr></thead><tbody><tr><td>Target tracking</td><td>keeps a metric at a target value (e.g., CPU 50%)</td><td>almost always the simplest answer</td></tr><tr><td>Step scaling</td><td>different steps for different alarm thresholds</td><td>needs different reactions at different levels</td></tr><tr><td>Simple scaling</td><td>one step with a cooldown period</td><td>legacy option</td></tr><tr><td>Scheduled</td><td>scaling on a schedule</td><td>known peaks: business hours, reporting</td></tr><tr><td>Predictive</td><td>ML forecast from history, provisions ahead of time</td><td>regular recurring patterns</td></tr></tbody></table></div>\n<p>Practical details:</p>\n<ul><li>the metric doesn't have to be CPU: ALBRequestCountPerTarget, SQS queue depth (via a custom \"messages per instance\" metric), network traffic;</li><li>predictive scaling learns from recurring patterns and handles rare, unpredictable spikes poorly;</li><li>lifecycle hooks give time to warm the cache on launch and to drain connections and flush logs on termination;</li><li>a warm pool keeps pre-initialized but stopped instances ready for a fast start;</li><li>instance refresh rolls out a new launch template version gradually;</li><li>an ASG can span multiple AZs, which is a cheap way to meet the \"survive an AZ failure\" requirement.</li></ul>\n<h2>Fault tolerance and cost</h2>\n<p>The requirement \"survive an AZ failure\" = at least two AZs, with min set so the remaining zones can carry the load. \"As cheap as possible\" + \"workload can tolerate interruption\" = a mixed instances policy with Spot. \"Serve static content instantly\" = offload static content to S3 and CloudFront rather than adding more instances.</p>\n<p>Connection draining (deregistration delay) lets active connections finish while an instance is being taken out of service — the right answer for \"users get errors during scale-in or during a deployment.\"</p>\n<h2>What the exam asks</h2>\n<ol><li>\"Users lose their session during scaling\" — move state out to ElastiCache or DynamoDB; sticky sessions are a workaround, not data storage.</li><li>\"A hung application isn't being replaced\" — health check type = ELB.</li><li>\"Load predictably grows every weekday at 9 AM\" — scheduled scaling.</li><li>\"Need a fixed IP for a partner's allowlist\" — NLB with an Elastic IP or Global Accelerator; ALB won't work.</li><li>\"Load balancing between EC2 and an on-premises data center\" — a target group of type IP.</li><li>\"Workers should scale with queue length\" — the SQS ApproximateNumberOfMessages metric per instance, target tracking.</li><li>\"Minimum operational effort for a web application with autoscaling\" — Elastic Beanstalk or ECS Fargate, not hand-assembling an ASG.</li></ol>\n<h2>Standard three-tier application pattern</h2>\n<pre><code>Route 53 -&gt; CloudFront -&gt; ALB (public subnets, 2+ AZ)\n             |                 -&gt; ASG with web instances (private subnets)\n             |                        -&gt; internal ALB -&gt; application ASG\n             |                                                -&gt; RDS Multi-AZ / Aurora\nstatic -&gt; S3 (CloudFront origin with OAC)</code></pre>\n<p>What's being tested here: the load balancer sits in public subnets, the instances sit in private subnets, the instance security group allows traffic only from the load balancer's security group (not a CIDR range), and the database accepts connections only from the application's security group. Referencing a security group from another security group instead of a CIDR range is the marker of a correct answer in questions about securing this pattern.</p>\n<h2>Diagnosing common symptoms</h2>\n<div class='table-wrap'><table><thead><tr><th>Symptom</th><th>Cause</th><th>Fix</th></tr></thead><tbody><tr><td>502 and 504 from ALB</td><td>target not responding or application timeout</td><td>check health check, increase idle timeout</td></tr><tr><td>Instances get replaced in a loop</td><td>health check is stricter than application startup time</td><td>increase health check grace period</td></tr><tr><td>Errors during scale-in</td><td>connections are dropped</td><td>deregistration delay, lifecycle hook</td></tr><tr><td>Load is uneven across AZs</td><td>NLB cross-zone is off</td><td>enable cross-zone balancing</td></tr><tr><td>Slow response after idle</td><td>cold start of instances</td><td>warm pool, predictive scaling</td></tr></tbody></table></div>\n<h2>Cost of load balancing</h2>\n<p>Charges consist of an hourly rate plus LCUs (or NLCUs) — units that measure new connections, active connections, throughput, and rules. That leads to practical conclusions: extra ALB routing rules cost money, long-lived keep-alive connections are cheaper than frequent new ones, and traffic served by CloudFront never reaches the load balancer at all and isn't billed as LCUs. NLB cross-zone traffic between AZs is billed separately — another argument against enabling it without need.</p>\n<h2>Listeners, rules, and certificates</h2>\n<p>A listener accepts connections on a port and protocol; rules parse the request by conditions: host-header, path, http-header, query-string, source-ip, http-request-method. Actions: forward to a target group (with weights for canary rollouts), redirect, fixed-response, authenticate-cognito, and authenticate-oidc. Authentication on ALB satisfies the requirement \"only let employees in\" without touching the application.</p>\n<p>Certificates: one default plus additional ones via SNI, which lets a single ALB serve dozens of domains. The listener's security policy sets the minimum TLS version — the standard answer for \"block TLS below 1.2.\"</p>\n<h2>Numbers to remember</h2>\n<ul><li>ALB operates at layer 7 (HTTP and HTTPS), NLB at layer 4 (TCP, UDP, TLS) and provides a static address in each Availability Zone; GWLB is only needed for network appliances;</li><li>ALB's default idle timeout is 60 seconds; the deregistration delay is 300 seconds;</li><li>the default health check interval is 30 seconds: 5 failures move a target to unhealthy, 2 successes bring it back;</li><li>an Auto Scaling group's default cooldown period is 300 seconds, matching the default health check grace period after an instance launches;</li><li>predictive scaling requires at least a day of history and forecasts 48 hours ahead; the pre-launch lead time is set in minutes;</li><li>cross-zone load balancing is always on and free for ALB, disabled by default for NLB, and cross-zone traffic is billed;</li><li>NLB preserves the client's original address, ALB passes it in the X-Forwarded-For header;</li><li>a lifecycle hook pauses an instance for a duration that can be extended with a heartbeat signal.</li></ul>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> A gaming service needs a load balancer for millions of UDP requests per second. → Network Load Balancer: it's the only one that handles UDP and sustains that rate.</p>\n<p><strong>2.</strong> A spike repeats every Friday evening; capacity is needed in advance. → A scheduled action on the Auto Scaling group, not a metric-based policy.</p>\n<p><strong>3.</strong> Logs must be flushed before an instance terminates. → A lifecycle hook on the terminating state: the instance waits until the action completes.</p>\n<p><strong>4.</strong> The application has several services and needs to route by URL path. → ALB with path-based rules: NLB can't route based on request content.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"744\" href=\"#\">questions 744</a> — a static IP in front of a load balancer: NLB supports an Elastic IP, ALB does not; the dump key was corrected.</li><li><a class=\"qref\" data-q=\"591\" href=\"#\">questions 591</a> — disputed question: routing to microservices in EKS is normally done with ALB via the AWS Load Balancer Controller; API Gateway is usually more expensive.</li><li><a class=\"qref\" data-q=\"664\" href=\"#\">questions 664</a> — low confidence: a CPU spike twice a month; predictive scaling catches regular patterns but can miss rare spikes.</li><li><a class=\"qref\" data-q=\"390\" href=\"#\">questions 390</a> — disputed question: sticky sessions don't store session data; durability requires ElastiCache Redis with persistence.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>49</td><td>19%</td><td>13</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>82</td><td>32%</td><td>18</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>78</td><td>30%</td><td>51</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>51</td><td>20%</td><td>8</td></tr></tbody></table></div><p>Total questions in this chapter: <b>260</b>. With a written walkthrough: <b>260</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#5</b> — RES: shared access to documents from both instances</li><li><b>#59</b> — PERF: ingesting and processing clickstream data</li><li><b>#60</b> — SEC: enforcing HTTPS for all requests</li><li><b>#70</b> — RES: replacing instances on HTTP health check failure instead of manual restart</li><li><b>#75</b> — RES: transactions lost under tier overload</li><li><b>#79</b> — COST: unpredictable spikes without overpaying</li><li><b>#112</b> — PERF: elastic platform for growing request volume</li><li><b>#116</b> — PERF: static site instead of a CMS requiring patching</li></ul>","text":" Load Balancers and Auto Scaling The pairing \"ELB in front of an Auto Scaling group across multiple AZs\" is the baseline fault tolerance pattern on the exam. The load balancer provides a single entry point and health checks; the ASG is responsible for instance count and replacing failed instances. Types of load balancers ALB NLB GWLB CLB (legacy) Layer 7 (HTTP/HTTPS/gRPC) 4 (TCP/UDP/TLS) 3 (GENEVE) 4 and 7 Routing by host, path, header, query, method by port all traffic to appliances basic Performance high millions of requests, minimal latency depends on appliance low Static IP no, DNS only yes, Elastic IP per AZ no no Target types instance, IP, Lambda instance, IP, ALB appliance instance TLS termination yes yes (TLS listener) no yes Sticky sessions load balancer or application cookie by source IP — cookie How the exam expects you to choose: need URL/host parsing, authentication via Cognito or OIDC, WAF — ALB; need UDP, a static IP, extremely low latency, TLS passthrough — NLB; need to route traffic through third party firewall appliances — GWLB; \"WAF must protect it\" — WAF attaches to ALB, CloudFront, API Gateway, and AppSync, but not to NLB. Useful details: ALB supports redirecting HTTP to HTTPS and returning fixed responses with no backend; a target group of type Lambda lets ALB invoke a function; cross zone load balancing is always on and free for ALB, but disabled by default for NLB and must be turned on explicitly; serving an application across multiple domains uses SNI with multiple certificates. Health checks Checks happen at two levels: the load balancer checks a target over a protocol and path, and the ASG uses either EC2 status checks or the ELB health check result. If the ASG is configured to use only EC2 status checks, a hung application on a live instance won't be replaced — a classic trap. The correct answer: set the ASG health check type to ELB. Auto Scaling group Core parameters: launch template (versioned, recommended over launch configuration), min/desired/max, a list of subnets across different AZs, a warm up policy, health check type and grace period, termination policy. Scaling policies: Policy What it does When Target tracking keeps a metric at a target value (e.g., CPU 50%) almost always the simplest answer Step scaling different steps for different alarm thresholds needs different reactions at different levels Simple scaling one step with a cooldown period legacy option Scheduled scaling on a schedule known peaks: business hours, reporting Predictive ML forecast from history, provisions ahead of time regular recurring patterns Practical details: the metric doesn't have to be CPU: ALBRequestCountPerTarget, SQS queue depth (via a custom \"messages per instance\" metric), network traffic; predictive scaling learns from recurring patterns and handles rare, unpredictable spikes poorly; lifecycle hooks give time to warm the cache on launch and to drain connections and flush logs on termination; a warm pool keeps pre initialized but stopped instances ready for a fast start; instance refresh rolls out a new launch template version gradually; an ASG can span multiple AZs, which is a cheap way to meet the \"survive an AZ failure\" requirement. Fault tolerance and cost The requirement \"survive an AZ failure\" = at least two AZs, with min set so the remaining zones can carry the load. \"As cheap as possible\" + \"workload can tolerate interruption\" = a mixed instances policy with Spot. \"Serve static content instantly\" = offload static content to S3 and CloudFront rather than adding more instances. Connection draining (deregistration delay) lets active connections finish while an instance is being taken out of service — the right answer for \"users get errors during scale in or during a deployment.\" What the exam asks 1. \"Users lose their session during scaling\" — move state out to ElastiCache or DynamoDB; sticky sessions are a workaround, not data storage. 2. \"A hung application isn't being replaced\" — health check type = ELB. 3. \"Load predictably grows every weekday at 9 AM\" — scheduled scaling. 4. \"Need a fixed IP for a partner's allowlist\" — NLB with an Elastic IP or Global Accelerator; ALB won't work. 5. \"Load balancing between EC2 and an on premises data center\" — a target group of type IP. 6. \"Workers should scale with queue length\" — the SQS ApproximateNumberOfMessages metric per instance, target tracking. 7. \"Minimum operational effort for a web application with autoscaling\" — Elastic Beanstalk or ECS Fargate, not hand assembling an ASG. Standard three tier application pattern Route 53 CloudFront ALB (public subnets, 2+ AZ) ASG with web instances (private subnets) internal ALB application ASG RDS Multi AZ / Aurora static S3 (CloudFront origin with OAC) What's being tested here: the load balancer sits in public subnets, the instances sit in private subnets, the instance security group allows traffic only from the load balancer's security group (not a CIDR range), and the database accepts connections only from the application's security group. Referencing a security group from another security group instead of a CIDR range is the marker of a correct answer in questions about securing this pattern. Diagnosing common symptoms Symptom Cause Fix 502 and 504 from ALB target not responding or application timeout check health check, increase idle timeout Instances get replaced in a loop health check is stricter than application startup time increase health check grace period Errors during scale in connections are dropped deregistration delay, lifecycle hook Load is uneven across AZs NLB cross zone is off enable cross zone balancing Slow response after idle cold start of instances warm pool, predictive scaling Cost of load balancing Charges consist of an hourly rate plus LCUs (or NLCUs) — units that measure new connections, active connections, throughput, and rules. That leads to practical conclusions: extra ALB routing rules cost money, long lived keep alive connections are cheaper than frequent new ones, and traffic served by CloudFront never reaches the load balancer at all and isn't billed as LCUs. NLB cross zone traffic between AZs is billed separately — another argument against enabling it without need. Listeners, rules, and certificates A listener accepts connections on a port and protocol; rules parse the request by conditions: host header, path, http header, query string, source ip, http request method. Actions: forward to a target group (with weights for canary rollouts), redirect, fixed response, authenticate cognito, and authenticate oidc. Authentication on ALB satisfies the requirement \"only let employees in\" without touching the application. Certificates: one default plus additional ones via SNI, which lets a single ALB serve dozens of domains. The listener's security policy sets the minimum TLS version — the standard answer for \"block TLS below 1.2.\" Numbers to remember ALB operates at layer 7 (HTTP and HTTPS), NLB at layer 4 (TCP, UDP, TLS) and provides a static address in each Availability Zone; GWLB is only needed for network appliances; ALB's default idle timeout is 60 seconds; the deregistration delay is 300 seconds; the default health check interval is 30 seconds: 5 failures move a target to unhealthy, 2 successes bring it back; an Auto Scaling group's default cooldown period is 300 seconds, matching the default health check grace period after an instance launches; predictive scaling requires at least a day of history and forecasts 48 hours ahead; the pre launch lead time is set in minutes; cross zone load balancing is always on and free for ALB, disabled by default for NLB, and cross zone traffic is billed; NLB preserves the client's original address, ALB passes it in the X Forwarded For header; a lifecycle hook pauses an instance for a duration that can be extended with a heartbeat signal. Mini practicum 1. A gaming service needs a load balancer for millions of UDP requests per second. → Network Load Balancer: it's the only one that handles UDP and sustains that rate. 2. A spike repeats every Friday evening; capacity is needed in advance. → A scheduled action on the Auto Scaling group, not a metric based policy. 3. Logs must be flushed before an instance terminates. → A lifecycle hook on the terminating state: the instance waits until the action completes. 4. The application has several services and needs to route by URL path. → ALB with path based rules: NLB can't route based on request content. Bank audit {{q:744}} — a static IP in front of a load balancer: NLB supports an Elastic IP, ALB does not; the dump key was corrected. {{q:591}} — disputed question: routing to microservices in EKS is normally done with ALB via the AWS Load Balancer Controller; API Gateway is usually more expensive. {{q:664}} — low confidence: a CPU spike twice a month; predictive scaling catches regular patterns but can miss rare spikes. {{q:390}} — disputed question: sticky sessions don't store session data; durability requires ElastiCache Redis with persistence. "},{"id":"vpc","title":"VPC — networking, routing, endpoints","order":4,"svc":["VPC"],"questions":[4,15,19,35,42,43,55,72,73,91,92,101,104,115,125,135,151,162,170,176,184,188,208,215,223,231,232,237,248,250,251,254,262,282,294,296,323,327,332,352,360,370,374,385,388,405,417,421,431,439,448,450,468,471,474,480,487,497,504,510,514,534,549,555,558,562,566,576,600,610,612,614,639,657,659,663,676,680,684,685,686,697,703,705,710,712,716,722,729,734,741,758,763,766,775,782,792,800,810,811,821,835,839,842,853,858,860,866,882,893,899,907,913,928,932,938,945,949,950,957,961,966,980,983,992,998,1011,1016],"html":"<h1>VPC</h1>\n<p>A VPC is an isolated network within a Region with its own address range. A subnet is tied to a single AZ, so \"a resource in two AZs\" always means at least two subnets. A convenient way to work through a networking question is the chain: addressing → route → security group → NACL → where the traffic exits.</p>\n<h2>Addressing and subnets</h2>\n<ul><li>CIDR from /16 to /28; it can't be changed after creation, but a secondary CIDR can be added;</li><li>AWS reserves 5 addresses in every subnet: the network address, the VPC router, DNS, future use, and broadcast; a /24 subnet has 251 usable addresses;</li><li>a public subnet differs from a private one by exactly one thing: its route table has a <code>0.0.0.0/0</code> route to an Internet Gateway;</li><li>IPv6 addresses in AWS are always public; \"privacy\" is provided by an egress-only internet gateway, the IPv6 equivalent of NAT.</li></ul>\n<h2>Getting to the internet</h2>\n<div class='table-wrap'><table><thead><tr><th>Mechanism</th><th>Direction</th><th>Notes</th></tr></thead><tbody><tr><td>Internet Gateway</td><td>inbound and outbound</td><td>resource needs a public IP or Elastic IP</td></tr><tr><td>NAT Gateway</td><td>outbound only</td><td>managed, scales automatically, billed hourly and per GB</td></tr><tr><td>NAT Instance</td><td>outbound only</td><td>your own EC2 instance, must disable source/destination check</td></tr><tr><td>Egress-only IGW</td><td>outbound IPv6 only</td><td>free</td></tr></tbody></table></div>\n<p>A NAT Gateway lives in a specific AZ. The fault-tolerant pattern is one NAT Gateway per AZ, with each private subnet's route pointing to \"its own\" NAT: a zone failure then doesn't break connectivity for the others, and there's no cross-AZ data-transfer charge. A single NAT Gateway for the whole VPC is both a single point of failure and extra cost from cross-AZ traffic.</p>\n<p>NAT cost is a recurring theme in optimization questions: traffic to S3 and DynamoDB through NAT is billed, but through a gateway endpoint it's free.</p>\n<h2>Security groups and NACLs</h2>\n<div class='table-wrap'><table><thead><tr><th></th><th>Security group</th><th>Network ACL</th></tr></thead><tbody><tr><td>Level</td><td>ENI (instance)</td><td>subnet</td></tr><tr><td>State</td><td>stateful: return traffic is allowed automatically</td><td>stateless: both directions need rules</td></tr><tr><td>Rules</td><td>allow only</td><td>allow and deny, evaluated by rule number</td></tr><tr><td>Scope</td><td>multiple SGs per ENI</td><td>one NACL per subnet</td></tr><tr><td>References</td><td>can reference another SG as the source</td><td>CIDR only</td></tr></tbody></table></div>\n<p>Exam pattern: \"block a specific attacker IP address\" — that's a NACL with a deny rule, because a security group can't deny anything. \"Let the application reach the database\" — the database's security group referencing the application's security group.</p>\n<h2>Connecting networks</h2>\n<ul><li>VPC peering: one-to-one, non-transitive (A–B and B–C don't give you A–C), CIDRs must not overlap, works across Regions and accounts;</li><li>Transit Gateway: a hub for hundreds of VPCs, VPN, and Direct Connect connections; supports route tables for segmentation and inter-Region peering — the answer for \"many VPCs, centralized routing\";</li><li>PrivateLink (interface endpoint): access to a single service over a private IP without connecting the networks, CIDRs can overlap, traffic is one-way from consumer to service;</li><li>VPN and Direct Connect — see the hybrid connectivity chapter.</li></ul>\n<p>Choosing between peering and PrivateLink: need access to the whole network — peering or a Transit Gateway; need access to a single application and don't want to connect the networks — PrivateLink.</p>\n<h2>Endpoints</h2>\n<div class='table-wrap'><table><thead><tr><th>Type</th><th>Services</th><th>How it works</th><th>Cost</th></tr></thead><tbody><tr><td>Gateway endpoint</td><td>S3, DynamoDB</td><td>an entry in the route table</td><td>free</td></tr><tr><td>Interface endpoint (PrivateLink)</td><td>most AWS services and partner services</td><td>an ENI with a private IP + private DNS</td><td>hourly plus per GB</td></tr><tr><td>Gateway Load Balancer endpoint</td><td>third-party appliances</td><td>entry point into a GWLB</td><td>hourly plus per GB</td></tr></tbody></table></div>\n<p>The key trap: a gateway endpoint exists only for S3 and DynamoDB. SQS, KMS, Secrets Manager, ECR, Systems Manager, and everything else need an interface endpoint. The requirement \"traffic must not leave to the internet\" + \"the service isn't S3 or DynamoDB\" = interface endpoint.</p>\n<p>Access through an endpoint is restricted by an endpoint policy, and on the bucket side by the <code>aws:SourceVpce</code> condition in the bucket policy.</p>\n<h2>DNS and name resolution</h2>\n<ul><li>the VPC attributes <code>enableDnsSupport</code> and <code>enableDnsHostnames</code> turn on AmazonProvidedDNS at the VPC+2 address;</li><li>Route 53 Resolver endpoints: inbound — so an on-premises network can resolve a private hosted zone in AWS; outbound with forwarding rules — so AWS can resolve corporate zones;</li><li>a private hosted zone is associated with one or more VPCs;</li><li>with an interface endpoint, enabling private DNS swaps the service's public name for the private address, so the application doesn't need to change.</li></ul>\n<h2>Observability</h2>\n<p>VPC Flow Logs record connection metadata (who, where, port, ACCEPT or REJECT) to CloudWatch Logs, S3, or Kinesis Data Firehose. They don't show packet contents — for that you need Traffic Mirroring. The answer to \"figure out why traffic isn't arriving\" = Flow Logs; \"analyze the payload and run an IDS\" = Traffic Mirroring. Reachability Analyzer and Network Access Analyzer check reachability and access policy statically, without sending any traffic.</p>\n<h2>What the exam asks</h2>\n<ol><li>\"Instances in a private subnet need to download updates\" — a NAT Gateway in a public subnet and a route from the private subnet.</li><li>\"Reduce the cost of accessing S3 from private subnets\" — gateway endpoint.</li><li>\"Traffic to SQS/KMS must not go over the internet\" — interface endpoint.</li><li>\"Connect 50 VPCs and an on-premises network without a peering mesh\" — Transit Gateway.</li><li>\"Expose your own SaaS application to customers in their VPCs\" — PrivateLink with an endpoint service.</li><li>\"Block a specific address\" — NACL deny.</li><li>\"Find out who's connecting to the instance\" — Flow Logs.</li><li>\"Overlapping CIDRs between two companies after a merger\" — PrivateLink or NAT; peering is impossible in that case.</li></ol>\n<h2>Designing the address space</h2>\n<p>Practical rule: pick a /16 for the VPC and a /24 per subnet, leaving room for future AZs and for subnets dedicated to endpoint ENIs, databases, and Transit Gateway attachments. Ranges must not overlap with either the on-premises network or partner networks — otherwise peering and VPN become impossible, and the only remaining options are PrivateLink or NAT with translation.</p>\n<p>Standard layout across three AZs:</p>\n<div class='table-wrap'><table><thead><tr><th>Subnet</th><th>Purpose</th><th>Default route</th></tr></thead><tbody><tr><td>public-a/b/c</td><td>ALB, NAT Gateway, bastion</td><td>Internet Gateway</td></tr><tr><td>app-a/b/c</td><td>EC2, ECS, Lambda with VPC access</td><td>NAT Gateway in its own AZ</td></tr><tr><td>data-a/b/c</td><td>RDS, ElastiCache, Redshift</td><td>no outbound route</td></tr><tr><td>tgw-a/b/c</td><td>Transit Gateway attachments</td><td>Transit Gateway</td></tr></tbody></table></div>\n<p>A database subnet with no route to the internet is the simplest way to satisfy the requirement \"the database must not be reachable from the internet\": even a misconfigured security group won't make it public.</p>\n<h2>Segmentation and centralized egress</h2>\n<p>In large landscapes, outbound traffic is consolidated into a dedicated egress VPC: private VPCs send <code>0.0.0.0/0</code> to a Transit Gateway, and the NAT Gateway and Network Firewall live in one place. This is cheaper than a NAT Gateway in every VPC and gives a single point of filtering. The tradeoff is that cross-AZ and inter-VPC traffic through the TGW is billed per GB, so for two or three VPCs this pattern is usually overkill.</p>\n<p>Shared VPC via AWS RAM lets one account own the network while other accounts launch resources into its subnets. The answer to \"centralized network management across dozens of application accounts.\"</p>\n<h2>Numbers to remember</h2>\n<ul><li>address block size from /16 to /28; AWS reserves 5 addresses in every subnet;</li><li>additional address blocks can be added to a network, but they must not overlap with a peered neighboring network;</li><li>a security group is connection-state aware and contains only allow rules; a network ACL is stateless, its rules are numbered and can deny, which means response traffic needs the ephemeral port range 1024–65535 allowed;</li><li>a NAT Gateway scales to 100 Gbps and supports up to 55,000 simultaneous connections to a single destination; billed hourly and per gigabyte;</li><li>peering is not transitive: three networks need three connections, while a Transit Gateway needs one attachment from each;</li><li>a gateway endpoint exists only for S3 and DynamoDB and costs nothing; an interface endpoint is billed hourly and per traffic, but works for dozens of services;</li><li>Flow Logs don't show calls to 169.254.169.254, to Amazon DNS, or to DHCP.</li></ul>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> Instances in a private subnet need outbound internet access for updates, inbound connections are not allowed. → A NAT Gateway in a public subnet with a default route pointing to it.</p>\n<p><strong>2.</strong> Same requirement, but with IPv6 addresses. → Egress-only internet gateway: a NAT Gateway doesn't support IPv6.</p>\n<p><strong>3.</strong> Traffic to S3 must not leave the AWS network, and paying for NAT isn't desired. → A gateway endpoint for S3: it's free and removes the need to go through NAT.</p>\n<p><strong>4.</strong> A handful of addresses are flooding the site with requests and need to be blocked quickly. → A deny rule in a network ACL: security groups have no deny rules.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"125\" href=\"#\">questions 125</a> — PDF defect: the fifth option is labeled D instead of E; relabeled in the data, the answer is AE.</li><li><a class=\"qref\" data-q=\"835\" href=\"#\">questions 835</a> — disputed question: private access to S3 over Direct Connect, public VIF versus interface endpoint — the difference is specifically about cost.</li><li><a class=\"qref\" data-q=\"878\" href=\"#\">questions 878</a> — disputed question: granting account owners access to a shared mailbox conflicts with the requirement for maximum security.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>65</td><td>51%</td><td>19</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>12</td><td>9%</td><td>2</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>28</td><td>22%</td><td>20</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>23</td><td>18%</td><td>2</td></tr></tbody></table></div><p>Total questions in this chapter: <b>128</b>. With a written walkthrough: <b>128</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#15</b> — SEC: VPC traffic inspection and filtering</li><li><b>#55</b> — SEC: database access only from private subnets</li><li><b>#72</b> — COST: data transfer charges to S3 within the region</li><li><b>#91</b> — SEC: S3 access without internet egress</li><li><b>#101</b> — RES: internet egress from private subnets in three zones</li><li><b>#115</b> — SEC: private route for file transfer</li><li><b>#125</b> — SEC: web and database without public access during outbound traffic</li><li><b>#135</b> — SEC: private connectivity to a provider's service</li></ul>","text":" VPC A VPC is an isolated network within a Region with its own address range. A subnet is tied to a single AZ, so \"a resource in two AZs\" always means at least two subnets. A convenient way to work through a networking question is the chain: addressing → route → security group → NACL → where the traffic exits. Addressing and subnets CIDR from /16 to /28; it can't be changed after creation, but a secondary CIDR can be added; AWS reserves 5 addresses in every subnet: the network address, the VPC router, DNS, future use, and broadcast; a /24 subnet has 251 usable addresses; a public subnet differs from a private one by exactly one thing: its route table has a 0.0.0.0/0 route to an Internet Gateway; IPv6 addresses in AWS are always public; \"privacy\" is provided by an egress only internet gateway, the IPv6 equivalent of NAT. Getting to the internet Mechanism Direction Notes Internet Gateway inbound and outbound resource needs a public IP or Elastic IP NAT Gateway outbound only managed, scales automatically, billed hourly and per GB NAT Instance outbound only your own EC2 instance, must disable source/destination check Egress only IGW outbound IPv6 only free A NAT Gateway lives in a specific AZ. The fault tolerant pattern is one NAT Gateway per AZ, with each private subnet's route pointing to \"its own\" NAT: a zone failure then doesn't break connectivity for the others, and there's no cross AZ data transfer charge. A single NAT Gateway for the whole VPC is both a single point of failure and extra cost from cross AZ traffic. NAT cost is a recurring theme in optimization questions: traffic to S3 and DynamoDB through NAT is billed, but through a gateway endpoint it's free. Security groups and NACLs Security group Network ACL Level ENI (instance) subnet State stateful: return traffic is allowed automatically stateless: both directions need rules Rules allow only allow and deny, evaluated by rule number Scope multiple SGs per ENI one NACL per subnet References can reference another SG as the source CIDR only Exam pattern: \"block a specific attacker IP address\" — that's a NACL with a deny rule, because a security group can't deny anything. \"Let the application reach the database\" — the database's security group referencing the application's security group. Connecting networks VPC peering: one to one, non transitive (A–B and B–C don't give you A–C), CIDRs must not overlap, works across Regions and accounts; Transit Gateway: a hub for hundreds of VPCs, VPN, and Direct Connect connections; supports route tables for segmentation and inter Region peering — the answer for \"many VPCs, centralized routing\"; PrivateLink (interface endpoint): access to a single service over a private IP without connecting the networks, CIDRs can overlap, traffic is one way from consumer to service; VPN and Direct Connect — see the hybrid connectivity chapter. Choosing between peering and PrivateLink: need access to the whole network — peering or a Transit Gateway; need access to a single application and don't want to connect the networks — PrivateLink. Endpoints Type Services How it works Cost Gateway endpoint S3, DynamoDB an entry in the route table free Interface endpoint (PrivateLink) most AWS services and partner services an ENI with a private IP + private DNS hourly plus per GB Gateway Load Balancer endpoint third party appliances entry point into a GWLB hourly plus per GB The key trap: a gateway endpoint exists only for S3 and DynamoDB. SQS, KMS, Secrets Manager, ECR, Systems Manager, and everything else need an interface endpoint. The requirement \"traffic must not leave to the internet\" + \"the service isn't S3 or DynamoDB\" = interface endpoint. Access through an endpoint is restricted by an endpoint policy, and on the bucket side by the aws:SourceVpce condition in the bucket policy. DNS and name resolution the VPC attributes enableDnsSupport and enableDnsHostnames turn on AmazonProvidedDNS at the VPC+2 address; Route 53 Resolver endpoints: inbound — so an on premises network can resolve a private hosted zone in AWS; outbound with forwarding rules — so AWS can resolve corporate zones; a private hosted zone is associated with one or more VPCs; with an interface endpoint, enabling private DNS swaps the service's public name for the private address, so the application doesn't need to change. Observability VPC Flow Logs record connection metadata (who, where, port, ACCEPT or REJECT) to CloudWatch Logs, S3, or Kinesis Data Firehose. They don't show packet contents — for that you need Traffic Mirroring. The answer to \"figure out why traffic isn't arriving\" = Flow Logs; \"analyze the payload and run an IDS\" = Traffic Mirroring. Reachability Analyzer and Network Access Analyzer check reachability and access policy statically, without sending any traffic. What the exam asks 1. \"Instances in a private subnet need to download updates\" — a NAT Gateway in a public subnet and a route from the private subnet. 2. \"Reduce the cost of accessing S3 from private subnets\" — gateway endpoint. 3. \"Traffic to SQS/KMS must not go over the internet\" — interface endpoint. 4. \"Connect 50 VPCs and an on premises network without a peering mesh\" — Transit Gateway. 5. \"Expose your own SaaS application to customers in their VPCs\" — PrivateLink with an endpoint service. 6. \"Block a specific address\" — NACL deny. 7. \"Find out who's connecting to the instance\" — Flow Logs. 8. \"Overlapping CIDRs between two companies after a merger\" — PrivateLink or NAT; peering is impossible in that case. Designing the address space Practical rule: pick a /16 for the VPC and a /24 per subnet, leaving room for future AZs and for subnets dedicated to endpoint ENIs, databases, and Transit Gateway attachments. Ranges must not overlap with either the on premises network or partner networks — otherwise peering and VPN become impossible, and the only remaining options are PrivateLink or NAT with translation. Standard layout across three AZs: Subnet Purpose Default route public a/b/c ALB, NAT Gateway, bastion Internet Gateway app a/b/c EC2, ECS, Lambda with VPC access NAT Gateway in its own AZ data a/b/c RDS, ElastiCache, Redshift no outbound route tgw a/b/c Transit Gateway attachments Transit Gateway A database subnet with no route to the internet is the simplest way to satisfy the requirement \"the database must not be reachable from the internet\": even a misconfigured security group won't make it public. Segmentation and centralized egress In large landscapes, outbound traffic is consolidated into a dedicated egress VPC: private VPCs send 0.0.0.0/0 to a Transit Gateway, and the NAT Gateway and Network Firewall live in one place. This is cheaper than a NAT Gateway in every VPC and gives a single point of filtering. The tradeoff is that cross AZ and inter VPC traffic through the TGW is billed per GB, so for two or three VPCs this pattern is usually overkill. Shared VPC via AWS RAM lets one account own the network while other accounts launch resources into its subnets. The answer to \"centralized network management across dozens of application accounts.\" Numbers to remember address block size from /16 to /28; AWS reserves 5 addresses in every subnet; additional address blocks can be added to a network, but they must not overlap with a peered neighboring network; a security group is connection state aware and contains only allow rules; a network ACL is stateless, its rules are numbered and can deny, which means response traffic needs the ephemeral port range 1024–65535 allowed; a NAT Gateway scales to 100 Gbps and supports up to 55,000 simultaneous connections to a single destination; billed hourly and per gigabyte; peering is not transitive: three networks need three connections, while a Transit Gateway needs one attachment from each; a gateway endpoint exists only for S3 and DynamoDB and costs nothing; an interface endpoint is billed hourly and per traffic, but works for dozens of services; Flow Logs don't show calls to 169.254.169.254, to Amazon DNS, or to DHCP. Mini practicum 1. Instances in a private subnet need outbound internet access for updates, inbound connections are not allowed. → A NAT Gateway in a public subnet with a default route pointing to it. 2. Same requirement, but with IPv6 addresses. → Egress only internet gateway: a NAT Gateway doesn't support IPv6. 3. Traffic to S3 must not leave the AWS network, and paying for NAT isn't desired. → A gateway endpoint for S3: it's free and removes the need to go through NAT. 4. A handful of addresses are flooding the site with requests and need to be blocked quickly. → A deny rule in a network ACL: security groups have no deny rules. Bank audit {{q:125}} — PDF defect: the fifth option is labeled D instead of E; relabeled in the data, the answer is AE. {{q:835}} — disputed question: private access to S3 over Direct Connect, public VIF versus interface endpoint — the difference is specifically about cost. {{q:878}} — disputed question: granting account owners access to a shared mailbox conflicts with the requirement for maximum security. "},{"id":"iam","title":"IAM — identities, policies, cross-account access","order":5,"svc":["IAM","Organizations"],"questions":[3,6,16,17,27,28,37,53,54,86,89,92,96,100,109,122,131,137,151,152,154,159,168,179,185,208,222,223,227,232,239,253,256,260,289,305,325,330,332,334,341,364,366,368,371,387,395,403,410,412,418,419,423,428,429,433,438,442,455,459,467,476,477,478,484,488,492,494,503,512,514,521,524,543,548,555,556,559,560,564,586,592,609,619,624,628,634,638,640,641,653,657,665,668,675,682,688,689,696,709,710,723,733,743,745,748,750,751,761,765,766,774,777,779,780,787,789,792,797,803,823,826,828,854,856,859,868,870,872,878,880,883,889,893,906,908,913,922,924,929,931,941,945,950,966,980,981,982,988,1017,1018],"html":"<h1>IAM</h1>\n<p>Every request to AWS is checked against who (principal), what (action), on what (resource), and under what conditions (condition). Permission is granted only when there's an explicit allow and no explicit deny. An explicit deny always wins, at any level.</p>\n<h2>Entities</h2>\n<ul><li>Root — the account owner, can do anything, cannot be disabled. Best practice: MFA on root, delete root's access keys, do day-to-day work under other identities.</li><li>IAM user — long-term credentials. Works for people without SSO and for external systems that can't be given a role. Almost always the losing option in modern answers.</li><li>IAM group — a container of policies for users, not itself a principal.</li><li>IAM role — a set of permissions with temporary keys via STS. The primary mechanism for AWS services, applications on EC2/ECS/Lambda, cross-account access, and federation.</li><li>Identity Center (formerly SSO) — manages people's access across multiple accounts through a corporate directory, using permission sets instead of copying policies.</li></ul>\n<h2>Policy types</h2>\n<div class='table-wrap'><table><thead><tr><th>Type</th><th>Attached to</th><th>Purpose</th></tr></thead><tbody><tr><td>Identity-based</td><td>user, group, role</td><td>what that identity is allowed to do</td></tr><tr><td>Resource-based</td><td>bucket, queue, topic, key, function</td><td>who is allowed to access the resource</td></tr><tr><td>Permissions boundary</td><td>user or role</td><td>upper limit on the identity's permissions</td></tr><tr><td>SCP (Organizations)</td><td>OU or account</td><td>upper limit for the whole account</td></tr><tr><td>Session policy</td><td>STS session</td><td>narrows permissions for the session's duration</td></tr><tr><td>ACL</td><td>S3, legacy</td><td>don't use in new designs</td></tr></tbody></table></div>\n<p>How the result is computed: effective permissions = (identity-based ∪ resource-based) ∩ boundary ∩ SCP, minus any explicit denies. SCPs never grant anything on their own — they only restrict. That's why \"grant access through an SCP\" is always the wrong option.</p>\n<h2>Cross-account access</h2>\n<p>The canonical pattern: in the account that owns the resource, you create a role whose trust policy names the consumer — an account or a role — as principal; the consumer calls <code>sts:AssumeRole</code> and gets temporary keys. There's no need to duplicate users in every account.</p>\n<p>Subtleties that get tested:</p>\n<ul><li>the trust policy names who will assume the role, not who is already in the same account: a role in account A that trusts account A itself accomplishes nothing;</li><li><code>sts:ExternalId</code> protects against the confused deputy problem for third-party access;</li><li>for S3 and other resources with resource-based policies, access can be granted directly through the resource policy, without assuming a role;</li><li>the assumed role's permissions are limited by the intersection of its policy and any session policy.</li></ul>\n<h2>Federation</h2>\n<ul><li>A corporate directory (AD, Okta, Entra) via SAML 2.0 or Identity Center — employees sign in with their own credentials, and roles are issued by group.</li><li>Web identities and mobile apps — Cognito identity pools exchange a provider's token for temporary AWS keys.</li><li>Cognito user pools handle application user authentication; identity pools handle granting AWS permissions. Questions about \"signing in via Google/Facebook in a mobile app and accessing S3\" need both.</li><li>IAM Roles Anywhere issues roles to workloads outside AWS based on certificates.</li></ul>\n<h2>Conditions in policies</h2>\n<p>Commonly used keys: <code>aws:SourceIp</code>, <code>aws:SourceVpce</code>, <code>aws:PrincipalOrgID</code>, <code>aws:RequestedRegion</code>, <code>aws:MultiFactorAuthPresent</code>, <code>s3:x-amz-server-side-encryption</code>, <code>ec2:ResourceTag/...</code>. These are the tool for requirements like \"access only from the office network,\" \"only from our organization,\" \"only with MFA enabled,\" \"only for resources tagged with the project\" (ABAC).</p>\n<p>ABAC is tag-based access control: a single policy along the lines of \"allow the action if the resource's tag matches the principal's tag\" replaces dozens of per-project policies and needs no edits when a new team is added.</p>\n<h2>Control tools</h2>\n<ul><li>IAM Access Analyzer finds resources with external access, checks policies for mistakes, and generates a policy from actual API calls seen in CloudTrail;</li><li>Credential report and Access Advisor show unused keys and permissions — the basis for reducing privileges;</li><li>Access Analyzer plus permission boundaries is the standard answer to \"let teams create roles, but don't let them escalate their own privileges.\"</li></ul>\n<h2>What the exam asks</h2>\n<ol><li>\"An application on EC2 accesses S3\" — an instance role, not keys in a file and not an IAM user.</li><li>\"Developers need access to the production account\" — a role in the target account that trusts the development account.</li><li>\"Block all accounts from using Regions outside the EU\" — an SCP with a deny on <code>aws:RequestedRegion</code>.</li><li>\"Teams create their own roles but can't expand their own permissions\" — a permissions boundary.</li><li>\"Single sign-on for employees across all accounts\" — Identity Center with federation.</li><li>\"Give a contractor access to our bucket\" — a role with an ExternalId or a bucket policy naming their account, never shared keys.</li><li>\"Application secrets with automatic rotation\" — Secrets Manager, not IAM.</li><li>\"Cut permissions down to what's actually used\" — Access Advisor or a policy generated by Access Analyzer.</li></ol>\n<h2>Policy structure</h2>\n<pre><code class=\"lang-json\">{\n  &quot;Version&quot;: &quot;2012-10-17&quot;,\n  &quot;Statement&quot;: [{\n    &quot;Sid&quot;: &quot;OnlyFromOfficeWithMFA&quot;,\n    &quot;Effect&quot;: &quot;Allow&quot;,\n    &quot;Action&quot;: [&quot;s3:GetObject&quot;, &quot;s3:PutObject&quot;],\n    &quot;Resource&quot;: &quot;arn:aws:s3:::reports/*&quot;,\n    &quot;Condition&quot;: {\n      &quot;IpAddress&quot;: {&quot;aws:SourceIp&quot;: &quot;203.0.113.0/24&quot;},\n      &quot;Bool&quot;: {&quot;aws:MultiFactorAuthPresent&quot;: &quot;true&quot;}\n    }\n  }]\n}</code></pre>\n<p>What to look for in questions: <code>Resource</code> set to <code>*</code> signals excessive permissions; a bucket-level action (<code>s3:ListBucket</code>) requires the bucket's ARN, not the objects' ARN; <code>NotAction</code> and <code>NotResource</code> are almost always a design mistake; missing a <code>Condition</code> where the requirement says \"only from the corporate network\" makes the option wrong.</p>\n<h2>Organizing accounts</h2>\n<p>Organizations groups accounts into an OU tree with a single bill and SCPs. A typical layout: separate OUs for production, non-production, security, and sandbox; CloudTrail and Config logs are collected into a dedicated security account that application teams have no delete access to.</p>\n<p>Control Tower deploys this structure automatically: a landing zone, guardrails (preventive via SCPs, detective via Config), and Account Factory for issuing new accounts from a template. The requirement \"new accounts must comply with the standard immediately\" is always solved by Control Tower, not manual policies.</p>\n<h2>Temporary credentials</h2>\n<p>STS issues time-limited keys: <code>AssumeRole</code> for roles within and across accounts, <code>AssumeRoleWithSAML</code> for corporate federation, <code>AssumeRoleWithWebIdentity</code> for Cognito and OIDC providers, including IRSA in EKS. Maximum session duration is set by the role (up to 12 hours), and a role chain is capped at one hour. Revoking access before expiration is done with a policy condition on <code>aws:TokenIssueTime</code> or by disabling the role — this comes up in questions about compromise.</p>\n<h2>Numbers to remember</h2>\n<ul><li>up to 10 managed policies can be attached to a single entity; inline policy size is capped at 2,048 characters for a user and 10,240 for a role;</li><li>a user can have at most two access keys;</li><li>a role session lasts an hour by default, up to 12 hours maximum, and no more than an hour when roles are chained;</li><li>account quotas: 5,000 users, 1,000 roles, 300 groups;</li><li>up to 5 SCPs can be attached to an organization root, OU, or account;</li><li>an SCP grants no permissions, only restricts them, and doesn't apply to the organization's management account; a permissions boundary works the same way;</li><li>evaluation order: an explicit deny always wins first, then permissions must agree across every applicable policy (identity, resource, SCP, boundary), or there's no access;</li><li>roles have a trust policy: without it, no one can assume the role, no matter what permissions they have.</li></ul>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> An application on an instance needs access to a bucket. → An instance role, not access keys in configuration: keys would have to be stored and rotated.</p>\n<p><strong>2.</strong> A contractor from another account needs to read one bucket. → A role that trusts their account, or a bucket policy naming their principal; an external ID protects against impersonation.</p>\n<p><strong>3.</strong> Developers need to create their own roles without granting themselves admin rights. → A permissions boundary: it caps the maximum permissions the roles they create can ever get.</p>\n<p><strong>4.</strong> The organization must not deploy resources outside two Regions. → An SCP with a Region condition: the restriction applies to every account except the management account.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"870\" href=\"#\">questions 870</a> — cross-account access: the role is created in the account that owns the resource and trusts the consumer's account; the dump key with a contradictory trust policy was corrected.</li><li><a class=\"qref\" data-q=\"931\" href=\"#\">questions 931</a> — a cross-account Lambda subscription to SNS needs a resource policy on the function and a topic policy; the key was corrected to AC.</li><li><a class=\"qref\" data-q=\"936\" href=\"#\">questions 936</a> — automatic secret rotation is available only in Secrets Manager, not in Parameter Store; the key was corrected to BC.</li><li><a class=\"qref\" data-q=\"893\" href=\"#\">questions 893</a> — guardrails on new account creation come from Control Tower, not Organizations by itself.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>124</td><td>82%</td><td>30</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>4</td><td>3%</td><td>1</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>4</td><td>3%</td><td>4</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>19</td><td>13%</td><td>2</td></tr></tbody></table></div><p>Total questions in this chapter: <b>151</b>. With a written walkthrough: <b>151</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#3</b> — SEC: restricting bucket access to the organization</li><li><b>#6</b> — PERF: transfer speed with limited bandwidth</li><li><b>#16</b> — PERF: choosing a BI platform for a data mart</li><li><b>#17</b> — SEC: EC2 access to S3 via a role</li><li><b>#28</b> — SEC: single sign-on and federation with AD</li><li><b>#109</b> — SEC: preventing object modification and controlling deletion</li><li><b>#131</b> — SEC: blocking direct access to the bucket URL</li><li><b>#154</b> — SEC: write permissions with deletion prevention for files</li></ul>","text":" IAM Every request to AWS is checked against who (principal), what (action), on what (resource), and under what conditions (condition). Permission is granted only when there's an explicit allow and no explicit deny. An explicit deny always wins, at any level. Entities Root — the account owner, can do anything, cannot be disabled. Best practice: MFA on root, delete root's access keys, do day to day work under other identities. IAM user — long term credentials. Works for people without SSO and for external systems that can't be given a role. Almost always the losing option in modern answers. IAM group — a container of policies for users, not itself a principal. IAM role — a set of permissions with temporary keys via STS. The primary mechanism for AWS services, applications on EC2/ECS/Lambda, cross account access, and federation. Identity Center (formerly SSO) — manages people's access across multiple accounts through a corporate directory, using permission sets instead of copying policies. Policy types Type Attached to Purpose Identity based user, group, role what that identity is allowed to do Resource based bucket, queue, topic, key, function who is allowed to access the resource Permissions boundary user or role upper limit on the identity's permissions SCP (Organizations) OU or account upper limit for the whole account Session policy STS session narrows permissions for the session's duration ACL S3, legacy don't use in new designs How the result is computed: effective permissions = (identity based ∪ resource based) ∩ boundary ∩ SCP, minus any explicit denies. SCPs never grant anything on their own — they only restrict. That's why \"grant access through an SCP\" is always the wrong option. Cross account access The canonical pattern: in the account that owns the resource, you create a role whose trust policy names the consumer — an account or a role — as principal; the consumer calls sts:AssumeRole and gets temporary keys. There's no need to duplicate users in every account. Subtleties that get tested: the trust policy names who will assume the role, not who is already in the same account: a role in account A that trusts account A itself accomplishes nothing; sts:ExternalId protects against the confused deputy problem for third party access; for S3 and other resources with resource based policies, access can be granted directly through the resource policy, without assuming a role; the assumed role's permissions are limited by the intersection of its policy and any session policy. Federation A corporate directory (AD, Okta, Entra) via SAML 2.0 or Identity Center — employees sign in with their own credentials, and roles are issued by group. Web identities and mobile apps — Cognito identity pools exchange a provider's token for temporary AWS keys. Cognito user pools handle application user authentication; identity pools handle granting AWS permissions. Questions about \"signing in via Google/Facebook in a mobile app and accessing S3\" need both. IAM Roles Anywhere issues roles to workloads outside AWS based on certificates. Conditions in policies Commonly used keys: aws:SourceIp , aws:SourceVpce , aws:PrincipalOrgID , aws:RequestedRegion , aws:MultiFactorAuthPresent , s3:x amz server side encryption , ec2:ResourceTag/... . These are the tool for requirements like \"access only from the office network,\" \"only from our organization,\" \"only with MFA enabled,\" \"only for resources tagged with the project\" (ABAC). ABAC is tag based access control: a single policy along the lines of \"allow the action if the resource's tag matches the principal's tag\" replaces dozens of per project policies and needs no edits when a new team is added. Control tools IAM Access Analyzer finds resources with external access, checks policies for mistakes, and generates a policy from actual API calls seen in CloudTrail; Credential report and Access Advisor show unused keys and permissions — the basis for reducing privileges; Access Analyzer plus permission boundaries is the standard answer to \"let teams create roles, but don't let them escalate their own privileges.\" What the exam asks 1. \"An application on EC2 accesses S3\" — an instance role, not keys in a file and not an IAM user. 2. \"Developers need access to the production account\" — a role in the target account that trusts the development account. 3. \"Block all accounts from using Regions outside the EU\" — an SCP with a deny on aws:RequestedRegion . 4. \"Teams create their own roles but can't expand their own permissions\" — a permissions boundary. 5. \"Single sign on for employees across all accounts\" — Identity Center with federation. 6. \"Give a contractor access to our bucket\" — a role with an ExternalId or a bucket policy naming their account, never shared keys. 7. \"Application secrets with automatic rotation\" — Secrets Manager, not IAM. 8. \"Cut permissions down to what's actually used\" — Access Advisor or a policy generated by Access Analyzer. Policy structure json { \"Version\": \"2012 10 17\", \"Statement\": [{ \"Sid\": \"OnlyFromOfficeWithMFA\", \"Effect\": \"Allow\", \"Action\": [\"s3:GetObject\", \"s3:PutObject\"], \"Resource\": \"arn:aws:s3:::reports/ \", \"Condition\": { \"IpAddress\": {\"aws:SourceIp\": \"203.0.113.0/24\"}, \"Bool\": {\"aws:MultiFactorAuthPresent\": \"true\"} } }] } What to look for in questions: Resource set to signals excessive permissions; a bucket level action ( s3:ListBucket ) requires the bucket's ARN, not the objects' ARN; NotAction and NotResource are almost always a design mistake; missing a Condition where the requirement says \"only from the corporate network\" makes the option wrong. Organizing accounts Organizations groups accounts into an OU tree with a single bill and SCPs. A typical layout: separate OUs for production, non production, security, and sandbox; CloudTrail and Config logs are collected into a dedicated security account that application teams have no delete access to. Control Tower deploys this structure automatically: a landing zone, guardrails (preventive via SCPs, detective via Config), and Account Factory for issuing new accounts from a template. The requirement \"new accounts must comply with the standard immediately\" is always solved by Control Tower, not manual policies. Temporary credentials STS issues time limited keys: AssumeRole for roles within and across accounts, AssumeRoleWithSAML for corporate federation, AssumeRoleWithWebIdentity for Cognito and OIDC providers, including IRSA in EKS. Maximum session duration is set by the role (up to 12 hours), and a role chain is capped at one hour. Revoking access before expiration is done with a policy condition on aws:TokenIssueTime or by disabling the role — this comes up in questions about compromise. Numbers to remember up to 10 managed policies can be attached to a single entity; inline policy size is capped at 2,048 characters for a user and 10,240 for a role; a user can have at most two access keys; a role session lasts an hour by default, up to 12 hours maximum, and no more than an hour when roles are chained; account quotas: 5,000 users, 1,000 roles, 300 groups; up to 5 SCPs can be attached to an organization root, OU, or account; an SCP grants no permissions, only restricts them, and doesn't apply to the organization's management account; a permissions boundary works the same way; evaluation order: an explicit deny always wins first, then permissions must agree across every applicable policy (identity, resource, SCP, boundary), or there's no access; roles have a trust policy: without it, no one can assume the role, no matter what permissions they have. Mini practicum 1. An application on an instance needs access to a bucket. → An instance role, not access keys in configuration: keys would have to be stored and rotated. 2. A contractor from another account needs to read one bucket. → A role that trusts their account, or a bucket policy naming their principal; an external ID protects against impersonation. 3. Developers need to create their own roles without granting themselves admin rights. → A permissions boundary: it caps the maximum permissions the roles they create can ever get. 4. The organization must not deploy resources outside two Regions. → An SCP with a Region condition: the restriction applies to every account except the management account. Bank audit {{q:870}} — cross account access: the role is created in the account that owns the resource and trusts the consumer's account; the dump key with a contradictory trust policy was corrected. {{q:931}} — a cross account Lambda subscription to SNS needs a resource policy on the function and a topic policy; the key was corrected to AC. {{q:936}} — automatic secret rotation is available only in Secrets Manager, not in Parameter Store; the key was corrected to BC. {{q:893}} — guardrails on new account creation come from Control Tower, not Organizations by itself. "},{"id":"encryption-kms","title":"Encryption, KMS, and secrets","order":6,"svc":["KMS","Secrets Manager"],"questions":[11,13,36,61,80,86,89,100,106,121,122,134,179,189,202,216,234,256,270,291,313,330,336,339,349,359,364,371,382,410,438,460,480,491,521,529,535,550,564,613,638,640,645,663,667,669,678,681,682,696,740,742,743,777,791,793,801,825,838,847,862,887,901,916,929,936,942,947,951,952,965,970,975,977,982,994,1007,1009],"html":"<h1>Encryption, KMS, and secrets</h1>\n<p>Encryption in AWS splits into two jobs: data at rest and data in transit. The first is almost always covered by KMS together with the storage service in question; the second by a TLS certificate from ACM plus a policy that rejects unencrypted connections.</p>\n<h2>KMS: key types</h2>\n<div class='table-wrap'><table><thead><tr><th>Type</th><th>Owned by</th><th>Rotation</th><th>Key policy</th><th>Visible in CloudTrail</th></tr></thead><tbody><tr><td>AWS owned</td><td>AWS, shared across the service</td><td>AWS</td><td>no</td><td>no</td></tr><tr><td>AWS managed (<code>aws/s3</code>)</td><td>your account, created by the service</td><td>yearly, automatic</td><td>not editable</td><td>yes</td></tr><tr><td>Customer managed (CMK)</td><td>you</td><td>enable it, yearly or on a schedule</td><td>yours</td><td>yes</td></tr><tr><td>Imported key material</td><td>you</td><td>manual, re-import only</td><td>yours</td><td>yes</td></tr><tr><td>CloudHSM-backed / External store</td><td>you</td><td>manual</td><td>yours</td><td>yes</td></tr></tbody></table></div>\n<p>A customer managed key is the right choice when you need your own access policy, an audit trail of key usage, rotation on your own schedule, cross-account access, or the ability to revoke access to data quickly. Phrasing like \"control over the key policy,\" \"ability to disable/delete the key,\" or \"audit key usage\" points straight at it.</p>\n<p>Deleting a key only happens after a 7-to-30-day waiting period. Disabling a key makes the data unreadable instantly — that's the answer to \"immediately stop access to encrypted data.\"</p>\n<h2>How envelope encryption works</h2>\n<ol><li>The service asks KMS for a data key (<code>GenerateDataKey</code>).</li><li>KMS returns both a plaintext and an encrypted copy of the key.</li><li>Data is encrypted locally with the plaintext key, which is then wiped from memory.</li><li>The encrypted key is stored alongside the data; on read it's decrypted through <code>Decrypt</code>.</li></ol>\n<p>The consequences follow from this: KMS never sees the actual data, direct encryption through KMS is capped at 4 KB, and load on KMS is measured in requests. Bulk operations run into KMS quotas — S3 Bucket Keys, the data key cache in the AWS Encryption SDK, and <code>GenerateDataKeyWithoutPlaintext</code> for batch writes all bring that down.</p>\n<h2>Multi-Region keys</h2>\n<p>Multi-Region keys are a set of keys that share key material across different Regions. You need them wherever encrypted data crosses a Region boundary: CRR of an encrypted bucket, DynamoDB global tables, copying snapshots into a disaster recovery Region. A regular CMK is tied to its own Region, and a snapshot copy in another Region has to be re-encrypted with a local key — that's usually the correct answer in DR questions.</p>\n<h2>Encryption by service</h2>\n<ul><li>S3: SSE-S3 by default, SSE-KMS for control and auditing, SSE-C for your own keys, Bucket Keys to cut cost; a policy can reject unencrypted writes.</li><li>EBS: volume and snapshot encryption, the key is chosen at creation; you can't turn on encryption for an existing unencrypted volume directly — you snapshot it and restore the snapshot into an encrypted volume. The account attribute \"encryption by default\" forces encryption on all new volumes.</li><li>RDS and Aurora: encryption is enabled at creation time; to encrypt an existing database, you take a snapshot, copy it with encryption enabled, and restore from that copy. A read replica inherits the source's encryption state.</li><li>DynamoDB: encryption is always on, only the key type is a choice.</li><li>EFS and FSx: encryption at rest at creation time, in transit via TLS at mount time.</li><li>SQS, SNS, Kinesis, CloudWatch Logs: SSE-KMS is turned on at the resource level.</li><li>Redshift, EMR, OpenSearch, Glue: cluster and intermediate-data encryption is configured at creation.</li></ul>\n<h2>ACM and traffic</h2>\n<p>ACM issues free public certificates and auto-renews the ones validated through DNS. ACM certificates can't be exported and installed on your own server — that's what ACM Private CA is for. A certificate for CloudFront is always issued or imported in us-east-1; for ALB and API Gateway, in the resource's own Region.</p>\n<p>You can import a certificate from a third-party CA into ACM, but the service can't issue one chained from someone else's CA — more than one trap question is built on that fact.</p>\n<h2>Secrets Manager and Parameter Store</h2>\n<div class='table-wrap'><table><thead><tr><th></th><th>Secrets Manager</th><th>SSM Parameter Store</th></tr></thead><tbody><tr><td>Cost</td><td>per secret and per call</td><td>standard parameters are free</td></tr><tr><td>Rotation</td><td>built in, via Lambda</td><td>none, manual only</td></tr><tr><td>Cross-account</td><td>resource policy</td><td>via RAM/policy, limited</td></tr><tr><td>Cross-Region replication</td><td>yes</td><td>no</td></tr><tr><td>Integration with RDS/Redshift/DocumentDB</td><td>yes, ready-made rotators</td><td>no</td></tr><tr><td>Size</td><td>up to 64 KB</td><td>4 KB (advanced up to 8 KB)</td></tr></tbody></table></div>\n<p>If the requirement mentions \"automatic rotation,\" the answer is Secrets Manager. If the emphasis is \"store configuration and non-secret parameters cheaply,\" the answer is Parameter Store. Hardcoding credentials in code, environment variables, or an AMI is always the wrong option.</p>\n<h2>What the exam asks</h2>\n<ol><li>\"All new EBS volumes must be encrypted, no exceptions allowed\" — the account attribute \"encrypt by default,\" not detection through Config after the fact.</li><li>\"An existing RDS database needs to be encrypted\" — snapshot, encrypted copy, restore.</li><li>\"Keys must be kept in a device under our control at FIPS 140-2 level 3\" — CloudHSM or a KMS custom key store.</li><li>\"Audit every operation on the key\" — a customer managed key plus CloudTrail.</li><li>\"KMS costs spiked from millions of small S3 objects\" — S3 Bucket Keys.</li><li>\"Database passwords must rotate automatically every 30 days\" — Secrets Manager with rotation.</li><li>\"Data is replicated to another Region and must stay encrypted\" — a multi-Region key, or the destination Region's own key on copy.</li><li>\"The application reaches KMS from a private subnet with no internet access\" — an interface endpoint for KMS.</li></ol>\n<h2>Key policy and access</h2>\n<p>The key policy is a resource-based policy without which an identity policy alone does nothing on KMS: if the key policy doesn't delegate rights to the account, no amount of user permissions will help. That sets KMS apart from most services and shows up regularly in questions about cross-account access to encrypted data.</p>\n<p>The standard pattern for cross-account access: the key policy lists the consumer account with the actions <code>kms:Decrypt</code> and <code>kms:DescribeKey</code>, and on the consumer side the role gets the same actions in its identity policy. It's further narrowed with conditions like <code>kms:ViaService</code> (use the key only through a specific service) and <code>kms:EncryptionContext</code> (only for a specific resource).</p>\n<p>Grants are a temporary delegation of key rights to services acting on your behalf; a service issues and revokes them, and they're more convenient than editing the policy for short-lived operations.</p>\n<h2>CloudHSM and regulatory requirements</h2>\n<p>CloudHSM is a dedicated hardware module under your sole control, FIPS 140-2 level 3, accessed via PKCS#11 and JCE. AWS has no access to the keys and can't recover them if you lose your credentials. It's the choice when a regulator requires a single-tenant HSM, key generation and storage entirely under your control, or support for specific crypto operations. A KMS custom key store pairs the convenience of KMS with key material held in CloudHSM — a compromise for anyone who wants normal service integrations while keeping their own keys.</p>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>Wording in the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"control the key policy, rotate, audit usage\"</td><td>customer managed key</td></tr><tr><td>\"single-tenant HSM, FIPS 140-2 level 3\"</td><td>CloudHSM</td></tr><tr><td>\"automatically rotate database credentials\"</td><td>Secrets Manager</td></tr><tr><td>\"store configuration values cheaply\"</td><td>Parameter Store</td></tr><tr><td>\"KMS request costs spiked on S3\"</td><td>S3 Bucket Keys</td></tr><tr><td>\"encrypted data replicated to another Region\"</td><td>multi-Region key</td></tr><tr><td>\"revoke access to encrypted data immediately\"</td><td>disable the key</td></tr><tr><td>\"certificate for CloudFront\"</td><td>ACM in us-east-1</td></tr></tbody></table></div>\n<h2>Numbers to remember</h2>\n<ul><li>KMS encrypts data directly only up to 4 KB, so everything else goes through envelope encryption: GenerateDataKey returns the data key in both plaintext and encrypted form;</li><li>automatic rotation of a customer managed key happens once a year, and the old key material is kept around to decrypt data encrypted earlier;</li><li>deleting a key is only possible after a waiting period of 7 to 30 days;</li><li>AWS managed keys are rotated by the service itself, and their policy can't be changed;</li><li>multi-Region keys are the only way to decrypt data in another Region with the same key; a regular key is strictly regional;</li><li>an S3 Bucket Key cuts calls to KMS by roughly 99% and noticeably lowers the bill;</li><li>access to a key is governed by the key policy: without permission there, an IAM policy alone grants no access;</li><li>CloudHSM provides a dedicated module at FIPS 140-2 level 3 when full hardware control is required.</li></ul>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> You need to encrypt files in S3 and know who accessed the key and when. → SSE-KMS with a customer key: access shows up in CloudTrail. SSE-S3 keeps no such log.</p>\n<p><strong>2.</strong> Data is copied to another Region and must be readable there without re-encryption. → A multi-Region key: the key's replica in the destination Region decrypts the same data.</p>\n<p><strong>3.</strong> KMS costs rose because of millions of small objects in a bucket. → Turn on S3 Bucket Key.</p>\n<p><strong>4.</strong> You need to store a database password and rotate it automatically once a month. → Secrets Manager with built-in rotation; Parameter Store is cheaper, but you'd have to write the rotation yourself.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"887\" href=\"#\">questions 887</a> — banning unencrypted volumes: the account attribute \"always encrypt new EBS volumes,\" Config only detects; the dump key was corrected.</li><li><a class=\"qref\" data-q=\"936\" href=\"#\">questions 936</a> — secret rotation: Secrets Manager plus a Lambda layer to fetch the secret.</li><li><a class=\"qref\" data-q=\"571\" href=\"#\">questions 571</a> — ACM doesn't issue certificates chained from a third-party CA, only imports ready-made ones; the dump's options are technically impossible.</li><li><a class=\"qref\" data-q=\"974\" href=\"#\">questions 974</a> — the CloudFront certificate is issued in us-east-1.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>73</td><td>94%</td><td>2</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>1</td><td>1%</td><td>1</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>4</td><td>5%</td><td>0</td></tr></tbody></table></div><p>Total questions in this chapter: <b>78</b>. With a written walkthrough: <b>78</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#256</b> — RES: deletion protection with version retention</li><li><b>#382</b> — SEC: encrypting data in transit between tiers</li><li><b>#791</b> — SEC: environment variables must not be visible</li></ul>","text":" Encryption, KMS, and secrets Encryption in AWS splits into two jobs: data at rest and data in transit. The first is almost always covered by KMS together with the storage service in question; the second by a TLS certificate from ACM plus a policy that rejects unencrypted connections. KMS: key types Type Owned by Rotation Key policy Visible in CloudTrail AWS owned AWS, shared across the service AWS no no AWS managed ( aws/s3 ) your account, created by the service yearly, automatic not editable yes Customer managed (CMK) you enable it, yearly or on a schedule yours yes Imported key material you manual, re import only yours yes CloudHSM backed / External store you manual yours yes A customer managed key is the right choice when you need your own access policy, an audit trail of key usage, rotation on your own schedule, cross account access, or the ability to revoke access to data quickly. Phrasing like \"control over the key policy,\" \"ability to disable/delete the key,\" or \"audit key usage\" points straight at it. Deleting a key only happens after a 7 to 30 day waiting period. Disabling a key makes the data unreadable instantly — that's the answer to \"immediately stop access to encrypted data.\" How envelope encryption works 1. The service asks KMS for a data key ( GenerateDataKey ). 2. KMS returns both a plaintext and an encrypted copy of the key. 3. Data is encrypted locally with the plaintext key, which is then wiped from memory. 4. The encrypted key is stored alongside the data; on read it's decrypted through Decrypt . The consequences follow from this: KMS never sees the actual data, direct encryption through KMS is capped at 4 KB, and load on KMS is measured in requests. Bulk operations run into KMS quotas — S3 Bucket Keys, the data key cache in the AWS Encryption SDK, and GenerateDataKeyWithoutPlaintext for batch writes all bring that down. Multi Region keys Multi Region keys are a set of keys that share key material across different Regions. You need them wherever encrypted data crosses a Region boundary: CRR of an encrypted bucket, DynamoDB global tables, copying snapshots into a disaster recovery Region. A regular CMK is tied to its own Region, and a snapshot copy in another Region has to be re encrypted with a local key — that's usually the correct answer in DR questions. Encryption by service S3: SSE S3 by default, SSE KMS for control and auditing, SSE C for your own keys, Bucket Keys to cut cost; a policy can reject unencrypted writes. EBS: volume and snapshot encryption, the key is chosen at creation; you can't turn on encryption for an existing unencrypted volume directly — you snapshot it and restore the snapshot into an encrypted volume. The account attribute \"encryption by default\" forces encryption on all new volumes. RDS and Aurora: encryption is enabled at creation time; to encrypt an existing database, you take a snapshot, copy it with encryption enabled, and restore from that copy. A read replica inherits the source's encryption state. DynamoDB: encryption is always on, only the key type is a choice. EFS and FSx: encryption at rest at creation time, in transit via TLS at mount time. SQS, SNS, Kinesis, CloudWatch Logs: SSE KMS is turned on at the resource level. Redshift, EMR, OpenSearch, Glue: cluster and intermediate data encryption is configured at creation. ACM and traffic ACM issues free public certificates and auto renews the ones validated through DNS. ACM certificates can't be exported and installed on your own server — that's what ACM Private CA is for. A certificate for CloudFront is always issued or imported in us east 1; for ALB and API Gateway, in the resource's own Region. You can import a certificate from a third party CA into ACM, but the service can't issue one chained from someone else's CA — more than one trap question is built on that fact. Secrets Manager and Parameter Store Secrets Manager SSM Parameter Store Cost per secret and per call standard parameters are free Rotation built in, via Lambda none, manual only Cross account resource policy via RAM/policy, limited Cross Region replication yes no Integration with RDS/Redshift/DocumentDB yes, ready made rotators no Size up to 64 KB 4 KB (advanced up to 8 KB) If the requirement mentions \"automatic rotation,\" the answer is Secrets Manager. If the emphasis is \"store configuration and non secret parameters cheaply,\" the answer is Parameter Store. Hardcoding credentials in code, environment variables, or an AMI is always the wrong option. What the exam asks 1. \"All new EBS volumes must be encrypted, no exceptions allowed\" — the account attribute \"encrypt by default,\" not detection through Config after the fact. 2. \"An existing RDS database needs to be encrypted\" — snapshot, encrypted copy, restore. 3. \"Keys must be kept in a device under our control at FIPS 140 2 level 3\" — CloudHSM or a KMS custom key store. 4. \"Audit every operation on the key\" — a customer managed key plus CloudTrail. 5. \"KMS costs spiked from millions of small S3 objects\" — S3 Bucket Keys. 6. \"Database passwords must rotate automatically every 30 days\" — Secrets Manager with rotation. 7. \"Data is replicated to another Region and must stay encrypted\" — a multi Region key, or the destination Region's own key on copy. 8. \"The application reaches KMS from a private subnet with no internet access\" — an interface endpoint for KMS. Key policy and access The key policy is a resource based policy without which an identity policy alone does nothing on KMS: if the key policy doesn't delegate rights to the account, no amount of user permissions will help. That sets KMS apart from most services and shows up regularly in questions about cross account access to encrypted data. The standard pattern for cross account access: the key policy lists the consumer account with the actions kms:Decrypt and kms:DescribeKey , and on the consumer side the role gets the same actions in its identity policy. It's further narrowed with conditions like kms:ViaService (use the key only through a specific service) and kms:EncryptionContext (only for a specific resource). Grants are a temporary delegation of key rights to services acting on your behalf; a service issues and revokes them, and they're more convenient than editing the policy for short lived operations. CloudHSM and regulatory requirements CloudHSM is a dedicated hardware module under your sole control, FIPS 140 2 level 3, accessed via PKCS 11 and JCE. AWS has no access to the keys and can't recover them if you lose your credentials. It's the choice when a regulator requires a single tenant HSM, key generation and storage entirely under your control, or support for specific crypto operations. A KMS custom key store pairs the convenience of KMS with key material held in CloudHSM — a compromise for anyone who wants normal service integrations while keeping their own keys. How to read the wording Wording in the question Answer \"control the key policy, rotate, audit usage\" customer managed key \"single tenant HSM, FIPS 140 2 level 3\" CloudHSM \"automatically rotate database credentials\" Secrets Manager \"store configuration values cheaply\" Parameter Store \"KMS request costs spiked on S3\" S3 Bucket Keys \"encrypted data replicated to another Region\" multi Region key \"revoke access to encrypted data immediately\" disable the key \"certificate for CloudFront\" ACM in us east 1 Numbers to remember KMS encrypts data directly only up to 4 KB, so everything else goes through envelope encryption: GenerateDataKey returns the data key in both plaintext and encrypted form; automatic rotation of a customer managed key happens once a year, and the old key material is kept around to decrypt data encrypted earlier; deleting a key is only possible after a waiting period of 7 to 30 days; AWS managed keys are rotated by the service itself, and their policy can't be changed; multi Region keys are the only way to decrypt data in another Region with the same key; a regular key is strictly regional; an S3 Bucket Key cuts calls to KMS by roughly 99% and noticeably lowers the bill; access to a key is governed by the key policy: without permission there, an IAM policy alone grants no access; CloudHSM provides a dedicated module at FIPS 140 2 level 3 when full hardware control is required. Mini practicum 1. You need to encrypt files in S3 and know who accessed the key and when. → SSE KMS with a customer key: access shows up in CloudTrail. SSE S3 keeps no such log. 2. Data is copied to another Region and must be readable there without re encryption. → A multi Region key: the key's replica in the destination Region decrypts the same data. 3. KMS costs rose because of millions of small objects in a bucket. → Turn on S3 Bucket Key. 4. You need to store a database password and rotate it automatically once a month. → Secrets Manager with built in rotation; Parameter Store is cheaper, but you'd have to write the rotation yourself. Bank audit {{q:887}} — banning unencrypted volumes: the account attribute \"always encrypt new EBS volumes,\" Config only detects; the dump key was corrected. {{q:936}} — secret rotation: Secrets Manager plus a Lambda layer to fetch the secret. {{q:571}} — ACM doesn't issue certificates chained from a third party CA, only imports ready made ones; the dump's options are technically impossible. {{q:974}} — the CloudFront certificate is issued in us east 1. "},{"id":"rds","title":"RDS — managed relational databases","order":7,"svc":["RDS","DMS"],"questions":[13,14,16,21,30,31,39,49,55,61,67,69,76,86,87,90,93,95,108,111,121,125,133,134,136,138,145,152,160,161,175,178,179,182,187,191,193,195,197,204,211,225,228,235,236,241,244,247,259,268,269,273,276,281,287,292,298,300,304,308,314,330,334,337,338,339,343,350,353,354,361,365,372,376,379,380,381,386,388,389,390,391,392,394,401,406,409,411,416,420,431,435,436,438,440,444,449,450,451,458,464,466,479,481,486,507,511,513,518,526,527,529,536,537,539,540,541,547,564,565,572,574,575,578,579,588,589,590,596,599,601,607,609,614,629,633,643,649,650,653,661,663,666,669,674,683,685,693,705,706,711,723,726,732,733,735,742,743,755,764,771,776,786,788,810,811,816,818,822,824,830,838,843,847,848,849,850,851,853,859,861,863,865,879,886,896,898,901,909,910,914,917,921,933,939,940,943,944,947,948,951,952,958,959,964,977,978,979,981,989,992,995,997,999,1010],"html":"<h1>RDS</h1>\n<p>A managed relational database engine: AWS handles installation, patching, backups, failover, and monitoring; you're responsible for schema, queries, and parameters. Engines: MySQL, PostgreSQL, MariaDB, Oracle, SQL Server, Db2, and the Aurora family (a separate chapter).</p>\n<h2>Multi-AZ: two different mechanisms</h2>\n<div class='table-wrap'><table><thead><tr><th></th><th>Multi-AZ instance</th><th>Multi-AZ DB cluster</th></tr></thead><tbody><tr><td>Composition</td><td>primary + standby</td><td>writer + 2 readable standbys</td></tr><tr><td>Standby is readable</td><td>no</td><td>yes, with a reader endpoint</td></tr><tr><td>Zones</td><td>2</td><td>3</td></tr><tr><td>Failover</td><td>60–120 seconds</td><td>usually under 35 seconds</td></tr><tr><td>Engines</td><td>all</td><td>MySQL and PostgreSQL</td></tr><tr><td>Purpose</td><td>availability</td><td>availability plus reads</td></tr></tbody></table></div>\n<p>Key trap: in a classic Multi-AZ setup, the standby doesn't serve reads. If a requirement calls for both high availability and read offloading, the answer is either a Multi-AZ DB cluster or a Multi-AZ instance plus a read replica — not \"read from the standby\".</p>\n<p>Failover happens automatically on the loss of an AZ, an instance, the network, or during maintenance; the application reconnects using the same DNS name, so the connection string never needs to change.</p>\n<h2>Read replicas</h2>\n<ul><li>asynchronous replication, up to 15 replicas, possible in a different AZ, region, or account;</li><li>each replica has its own endpoint — the application has to route read queries to it itself;</li><li>a replica can be promoted to a standalone database — a way to migrate or split load, but not an automatic failover;</li><li>lag is tracked with the ReplicaLag metric; if strict consistency is required, reads from a replica are not acceptable;</li><li>a replica in another region reduces latency for remote readers and forms part of a DR plan.</li></ul>\n<h2>Backup and recovery</h2>\n<ul><li>Automated backups: retention from 1 to 35 days, include transaction logs and give point-in-time recovery accurate to roughly 5 minutes;</li><li>manual snapshots persist until deleted and survive deletion of the database;</li><li>restoring always creates a new instance with a new endpoint;</li><li>copying a snapshot to another region is the basic DR mechanism; with encryption, a key in the target region is required;</li><li>AWS Backup centralizes retention policies for RDS alongside other services.</li></ul>\n<p>RPO is set by the logs (minutes); RTO is the time to restore from a snapshot (tens of minutes or more). If the requirement calls for RPO and RTO in seconds, RDS snapshots won't cut it — you need Aurora Global Database or replication.</p>\n<h2>Performance and scaling</h2>\n<ul><li>vertically — change the instance class (with downtime, or via a Multi-AZ failover);</li><li>horizontally for reads — read replicas or an ElastiCache cache;</li><li>for writes — application-side sharding, switching engines, or moving to Aurora;</li><li>storage autoscaling grows the volume automatically; it can't be shrunk;</li><li>storage types: gp3 for most workloads, io1/io2 for guaranteed IOPS, magnetic is deprecated;</li><li>Performance Insights shows load by query and wait event, Enhanced Monitoring gives OS-level metrics down to a 1-second interval.</li></ul>\n<p>RDS Proxy solves the problem of thousands of short-lived connections (typical for Lambda): connection pooling, faster failover, and integration with Secrets Manager and IAM authentication. It doesn't speed up the queries themselves and doesn't replace a read replica — it's about connections, not read performance.</p>\n<h2>Security</h2>\n<ul><li>the database sits in a private subnet, and the security group allows access only from the application's security group;</li><li>encryption at rest is enabled at creation; TLS in transit is an engine parameter;</li><li>IAM authentication removes passwords for applications; administrator passwords live in Secrets Manager with rotation;</li><li>auditing: engine logs are shipped to CloudWatch Logs, API events to CloudTrail;</li><li>Multi-AZ and snapshots don't waive the encryption requirement: a snapshot of an unencrypted database is also unencrypted.</li></ul>\n<h2>Migration</h2>\n<p>DMS moves data from an on-premises or cloud database with minimal downtime, supporting full load and CDC. Schema Conversion Tool converts schema and code when switching engines (Oracle to PostgreSQL, SQL Server to MySQL). The \"DMS + SCT\" pair is the standard answer for a heterogeneous migration; \"just DMS\" for a homogeneous one.</p>\n<h2>What the exam asks</h2>\n<ol><li>\"Reports load the database, need fresh data with a small lag\" — a read replica.</li><li>\"Survive an AZ failure with no data loss\" — Multi-AZ (synchronous replication).</li><li>\"Lambda exhausts connections to the database\" — RDS Proxy.</li><li>\"Restore state to 10 minutes ago\" — point-in-time recovery.</li><li>\"A monthly report loads the database once a month, otherwise it's idle\" — either an on-demand replica or exporting to S3 and Athena.</li><li>\"Migrate Oracle to PostgreSQL with minimal downtime\" — SCT for the schema, DMS with CDC for the data.</li><li>\"The database must survive a region failure with an RPO of minutes\" — a cross-region replica or Aurora Global Database.</li><li>\"We don't want to manage patches and backups\" — RDS instead of a database on EC2.</li></ol>\n<h2>Maintenance and windows</h2>\n<p>Engine patches and class changes apply during the maintenance window; with Multi-AZ, AWS updates the standby first, then fails over, so downtime is bounded by the failover time. Blue/Green Deployments create a full copy of the environment, apply changes on the green side, and cut over traffic within seconds — the right answer to \"upgrade a major version with minimal downtime\".</p>\n<p>Reserved Instances for RDS work the same way as for EC2 and cover steady workloads; dev databases are cheaper stopped overnight (RDS can be stopped for up to 7 days, after which the instance starts automatically) or moved to Aurora Serverless v2.</p>\n<h2>Choosing between RDS, Aurora, and a database on EC2</h2>\n<div class='table-wrap'><table><thead><tr><th>Requirement</th><th>Answer</th></tr></thead><tbody><tr><td>Standard engine, minimal administration</td><td>RDS</td></tr><tr><td>Maximum throughput and fast failover</td><td>Aurora</td></tr><tr><td>Need file system access, non-standard extensions, custom patching</td><td>database on EC2</td></tr><tr><td>Global readers with about a second of replication lag</td><td>Aurora Global Database</td></tr><tr><td>Unpredictable load with idle periods</td><td>Aurora Serverless v2</td></tr><tr><td>Oracle or SQL Server license tied to sockets</td><td>RDS with BYOL or EC2 on a Dedicated Host</td></tr></tbody></table></div>\n<p>Any wording like \"reduce operational overhead\" tied to a relational database on EC2 is an invitation to migrate to RDS or Aurora, not to improve the maintenance scripts.</p>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>In the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"offload reporting queries\"</td><td>a read replica</td></tr><tr><td>\"survive an AZ failure with no data loss\"</td><td>Multi-AZ (synchronous replication)</td></tr><tr><td>\"Lambda exhausts database connections\"</td><td>RDS Proxy</td></tr><tr><td>\"restore to any point in the last week\"</td><td>PITR from automated backups</td></tr><tr><td>\"minimal downtime major version upgrade\"</td><td>Blue/Green Deployments</td></tr><tr><td>\"migrate Oracle to PostgreSQL\"</td><td>SCT plus DMS with CDC</td></tr><tr><td>\"encrypt an existing database\"</td><td>snapshot, copy with a key, restore</td></tr><tr><td>\"cross-Region disaster recovery, RPO minutes\"</td><td>a cross-region replica or Aurora Global Database</td></tr></tbody></table></div>\n<h2>Numbers to remember</h2>\n<ul><li>automated backups are kept for 0 to 35 days (7 by default); point-in-time restore is accurate to roughly 5 minutes;</li><li>failover in a Multi-AZ DB instance configuration takes 60–120 seconds, in a Multi-AZ DB cluster usually under 35 seconds, and the cluster form is only available for MySQL and PostgreSQL;</li><li>up to 15 read replicas for MySQL, MariaDB, and PostgreSQL, and up to 5 for Oracle and SQL Server;</li><li>RDS Proxy cuts failover time by roughly two-thirds and maintains a connection pool;</li><li>storage autoscaling grows the volume with no downtime; the limit is set when you enable it;</li><li>default ports: MySQL and MariaDB 3306, PostgreSQL 5432, SQL Server 1433, Oracle 1521;</li><li>encryption is enabled at instance creation: for a database already running, it's added via a snapshot and restore;</li><li>manual snapshots live until deleted; automated ones are removed once retention expires.</li></ul>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> Monthly reports load down the production database. → A read replica, redirecting reporting queries to it.</p>\n<p><strong>2.</strong> A serverless application gets connection failures during traffic spikes. → RDS Proxy: a connection pool instead of a thousand new sessions.</p>\n<p><strong>3.</strong> Need to roll back five minutes after a bad data update. → Point-in-time recovery from automated backups, not a snapshot and not a replica.</p>\n<p><strong>4.</strong> A third-party extension needs privileged access to the operating system. → RDS Custom: a managed service with administrator access, not a self-managed install on EC2.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"536\" href=\"#\">questions 536</a> — disputed: a Multi-AZ DB cluster with two readable standbys solves both HA and reads with three instances instead of four.</li><li><a class=\"qref\" data-q=\"268\" href=\"#\">questions 268</a> — disputed: RDS Proxy addresses the connection pool, not read performance; part of the community picks ElastiCache instead.</li><li><a class=\"qref\" data-q=\"511\" href=\"#\">questions 511</a> — low confidence: the Aurora On-Demand and RDS Single-AZ option wordings nearly duplicate each other.</li><li><a class=\"qref\" data-q=\"350\" href=\"#\">questions 350</a> — low confidence: the question mixes availability requirements with reporting performance.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>48</td><td>22%</td><td>10</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>70</td><td>33%</td><td>14</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>51</td><td>24%</td><td>31</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>46</td><td>21%</td><td>5</td></tr></tbody></table></div><p>Total questions in this chapter: <b>215</b>. With a written walkthrough: <b>215</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#16</b> — PERF: choosing a BI platform for a data mart</li><li><b>#31</b> — SEC: enforcing resource tag compliance</li><li><b>#39</b> — PERF: slow inserts due to volume performance</li><li><b>#55</b> — SEC: database access only from private subnets</li><li><b>#67</b> — RES: reprocessing queue messages</li><li><b>#87</b> — RES: avoiding data loss during upgrade</li><li><b>#90</b> — PERF: script interferes with developers' database usage</li><li><b>#108</b> — RES: fanning out an event to multiple systems</li></ul>","text":" RDS A managed relational database engine: AWS handles installation, patching, backups, failover, and monitoring; you're responsible for schema, queries, and parameters. Engines: MySQL, PostgreSQL, MariaDB, Oracle, SQL Server, Db2, and the Aurora family (a separate chapter). Multi AZ: two different mechanisms Multi AZ instance Multi AZ DB cluster Composition primary + standby writer + 2 readable standbys Standby is readable no yes, with a reader endpoint Zones 2 3 Failover 60–120 seconds usually under 35 seconds Engines all MySQL and PostgreSQL Purpose availability availability plus reads Key trap: in a classic Multi AZ setup, the standby doesn't serve reads. If a requirement calls for both high availability and read offloading, the answer is either a Multi AZ DB cluster or a Multi AZ instance plus a read replica — not \"read from the standby\". Failover happens automatically on the loss of an AZ, an instance, the network, or during maintenance; the application reconnects using the same DNS name, so the connection string never needs to change. Read replicas asynchronous replication, up to 15 replicas, possible in a different AZ, region, or account; each replica has its own endpoint — the application has to route read queries to it itself; a replica can be promoted to a standalone database — a way to migrate or split load, but not an automatic failover; lag is tracked with the ReplicaLag metric; if strict consistency is required, reads from a replica are not acceptable; a replica in another region reduces latency for remote readers and forms part of a DR plan. Backup and recovery Automated backups: retention from 1 to 35 days, include transaction logs and give point in time recovery accurate to roughly 5 minutes; manual snapshots persist until deleted and survive deletion of the database; restoring always creates a new instance with a new endpoint; copying a snapshot to another region is the basic DR mechanism; with encryption, a key in the target region is required; AWS Backup centralizes retention policies for RDS alongside other services. RPO is set by the logs (minutes); RTO is the time to restore from a snapshot (tens of minutes or more). If the requirement calls for RPO and RTO in seconds, RDS snapshots won't cut it — you need Aurora Global Database or replication. Performance and scaling vertically — change the instance class (with downtime, or via a Multi AZ failover); horizontally for reads — read replicas or an ElastiCache cache; for writes — application side sharding, switching engines, or moving to Aurora; storage autoscaling grows the volume automatically; it can't be shrunk; storage types: gp3 for most workloads, io1/io2 for guaranteed IOPS, magnetic is deprecated; Performance Insights shows load by query and wait event, Enhanced Monitoring gives OS level metrics down to a 1 second interval. RDS Proxy solves the problem of thousands of short lived connections (typical for Lambda): connection pooling, faster failover, and integration with Secrets Manager and IAM authentication. It doesn't speed up the queries themselves and doesn't replace a read replica — it's about connections, not read performance. Security the database sits in a private subnet, and the security group allows access only from the application's security group; encryption at rest is enabled at creation; TLS in transit is an engine parameter; IAM authentication removes passwords for applications; administrator passwords live in Secrets Manager with rotation; auditing: engine logs are shipped to CloudWatch Logs, API events to CloudTrail; Multi AZ and snapshots don't waive the encryption requirement: a snapshot of an unencrypted database is also unencrypted. Migration DMS moves data from an on premises or cloud database with minimal downtime, supporting full load and CDC. Schema Conversion Tool converts schema and code when switching engines (Oracle to PostgreSQL, SQL Server to MySQL). The \"DMS + SCT\" pair is the standard answer for a heterogeneous migration; \"just DMS\" for a homogeneous one. What the exam asks 1. \"Reports load the database, need fresh data with a small lag\" — a read replica. 2. \"Survive an AZ failure with no data loss\" — Multi AZ (synchronous replication). 3. \"Lambda exhausts connections to the database\" — RDS Proxy. 4. \"Restore state to 10 minutes ago\" — point in time recovery. 5. \"A monthly report loads the database once a month, otherwise it's idle\" — either an on demand replica or exporting to S3 and Athena. 6. \"Migrate Oracle to PostgreSQL with minimal downtime\" — SCT for the schema, DMS with CDC for the data. 7. \"The database must survive a region failure with an RPO of minutes\" — a cross region replica or Aurora Global Database. 8. \"We don't want to manage patches and backups\" — RDS instead of a database on EC2. Maintenance and windows Engine patches and class changes apply during the maintenance window; with Multi AZ, AWS updates the standby first, then fails over, so downtime is bounded by the failover time. Blue/Green Deployments create a full copy of the environment, apply changes on the green side, and cut over traffic within seconds — the right answer to \"upgrade a major version with minimal downtime\". Reserved Instances for RDS work the same way as for EC2 and cover steady workloads; dev databases are cheaper stopped overnight (RDS can be stopped for up to 7 days, after which the instance starts automatically) or moved to Aurora Serverless v2. Choosing between RDS, Aurora, and a database on EC2 Requirement Answer Standard engine, minimal administration RDS Maximum throughput and fast failover Aurora Need file system access, non standard extensions, custom patching database on EC2 Global readers with about a second of replication lag Aurora Global Database Unpredictable load with idle periods Aurora Serverless v2 Oracle or SQL Server license tied to sockets RDS with BYOL or EC2 on a Dedicated Host Any wording like \"reduce operational overhead\" tied to a relational database on EC2 is an invitation to migrate to RDS or Aurora, not to improve the maintenance scripts. How to read the wording In the question Answer \"offload reporting queries\" a read replica \"survive an AZ failure with no data loss\" Multi AZ (synchronous replication) \"Lambda exhausts database connections\" RDS Proxy \"restore to any point in the last week\" PITR from automated backups \"minimal downtime major version upgrade\" Blue/Green Deployments \"migrate Oracle to PostgreSQL\" SCT plus DMS with CDC \"encrypt an existing database\" snapshot, copy with a key, restore \"cross Region disaster recovery, RPO minutes\" a cross region replica or Aurora Global Database Numbers to remember automated backups are kept for 0 to 35 days (7 by default); point in time restore is accurate to roughly 5 minutes; failover in a Multi AZ DB instance configuration takes 60–120 seconds, in a Multi AZ DB cluster usually under 35 seconds, and the cluster form is only available for MySQL and PostgreSQL; up to 15 read replicas for MySQL, MariaDB, and PostgreSQL, and up to 5 for Oracle and SQL Server; RDS Proxy cuts failover time by roughly two thirds and maintains a connection pool; storage autoscaling grows the volume with no downtime; the limit is set when you enable it; default ports: MySQL and MariaDB 3306, PostgreSQL 5432, SQL Server 1433, Oracle 1521; encryption is enabled at instance creation: for a database already running, it's added via a snapshot and restore; manual snapshots live until deleted; automated ones are removed once retention expires. Mini practicum 1. Monthly reports load down the production database. → A read replica, redirecting reporting queries to it. 2. A serverless application gets connection failures during traffic spikes. → RDS Proxy: a connection pool instead of a thousand new sessions. 3. Need to roll back five minutes after a bad data update. → Point in time recovery from automated backups, not a snapshot and not a replica. 4. A third party extension needs privileged access to the operating system. → RDS Custom: a managed service with administrator access, not a self managed install on EC2. Bank audit {{q:536}} — disputed: a Multi AZ DB cluster with two readable standbys solves both HA and reads with three instances instead of four. {{q:268}} — disputed: RDS Proxy addresses the connection pool, not read performance; part of the community picks ElastiCache instead. {{q:511}} — low confidence: the Aurora On Demand and RDS Single AZ option wordings nearly duplicate each other. {{q:350}} — low confidence: the question mixes availability requirements with reporting performance. "},{"id":"aurora","title":"Aurora — cloud-native MySQL and PostgreSQL engine","order":8,"svc":["Aurora"],"questions":[11,14,25,69,87,93,94,129,136,144,145,157,161,175,183,217,229,234,235,244,273,276,300,314,336,337,338,340,341,343,349,378,381,411,440,462,502,507,511,526,527,540,565,572,574,596,601,622,637,650,694,705,711,730,733,755,764,771,776,788,804,824,827,834,851,854,864,879,886,896,935,944,946,955,964,978,979,994,995,997],"html":"<h1>Aurora</h1>\n<p>Aurora is protocol-compatible with MySQL and PostgreSQL, but its storage is its own: a distributed volume that grows up to 128 TB, with six copies of every block across three Availability Zones. The cluster survives the loss of two copies with no data loss, and the loss of three copies with no loss of read availability. That's where all of its properties come from: fast failover, cheap replicas, cloning, and point-in-time recovery.</p>\n<h2>Cluster architecture</h2>\n<ul><li>writer — the single instance that accepts writes;</li><li>up to 15 reader instances reading the same volume, typically tens of milliseconds behind;</li><li>storage is shared and self-healing; replicas don't copy the data;</li><li>if the writer fails, one of the readers becomes the writer, usually within seconds, with the order set by instance priority (tier).</li></ul>\n<p>Endpoints — what gets tested most often:</p>\n<div class='table-wrap'><table><thead><tr><th>Endpoint</th><th>What it points to</th><th>When to use it</th></tr></thead><tbody><tr><td>Cluster (writer)</td><td>the current writer</td><td>writes; follows failover</td></tr><tr><td>Reader</td><td>load-balances across readers</td><td>application reads</td></tr><tr><td>Custom</td><td>a defined subset of instances</td><td>a dedicated pool for reporting or BI</td></tr><tr><td>Instance</td><td>a specific instance</td><td>diagnostics, targeted tasks</td></tr></tbody></table></div>\n<p>The application should use the reader endpoint for reads; if an answer option routes reads to the cluster endpoint, that option is wrong.</p>\n<h2>Storage types and pricing</h2>\n<p>Aurora has only two storage configuration types: Standard and I/O-Optimized. The terms gp2, gp3, and Provisioned IOPS belong to RDS and EBS, not to an Aurora cluster — a ready- made trap. Standard bills I/O operations separately; I/O-Optimized folds them into instance pricing and pays off once I/O is roughly 25 percent or more of the bill.</p>\n<p>Overall cost structure: instances (or ACUs in Serverless), storage per GB, I/O operations under Standard, backups beyond the cluster size, and cross-AZ/cross-Region traffic.</p>\n<h2>Serverless v2</h2>\n<p>Serverless v2 scales instance capacity continuously in ACU units (0.5 ACU and up, with scale-to-zero available for certain configurations since 2024) without dropping connections. It suits unpredictable, \"spiky\" workloads, dev environments, and multi-tenant applications. An important detail for cost questions: a regular provisioned cluster can be stopped for at most 7 days before it starts itself back up, while Serverless v2 scales itself down when idle.</p>\n<h2>Global Database</h2>\n<p>Aurora Global Database replicates data to up to 10 secondary regions with typical replication lag under a second. A secondary region provides local reads and lets you promote it in about a minute: RPO is measured in seconds, RTO in minutes. This is the standard answer to \"global application with regional reads and fast recovery from a regional outage.\" A cross-Region RDS read replica is slower and takes longer to fail over.</p>\n<h2>Clones, backups, recovery</h2>\n<ul><li>Backtrack (MySQL-compatible edition only) rewinds a cluster by several hours in minutes, without restoring a snapshot — the answer to \"roll back an accidental DELETE as fast as possible\";</li><li>fast database cloning creates a copy-on-write copy in minutes with almost no extra storage — the answer to \"need a copy of prod for testing\";</li><li>continuous backup to S3 provides PITR within the retention period;</li><li>snapshots can be copied across regions and accounts.</li></ul>\n<h2>Additional features</h2>\n<ul><li>Aurora Auto Scaling adds readers based on a load metric;</li><li>Parallel Query (MySQL-compatible edition) speeds up analytical queries by pushing filtering down into the storage layer;</li><li>Aurora Machine Learning calls SageMaker and Comprehend directly from SQL;</li><li>Zero-ETL integration with Redshift moves data into analytics without a pipeline;</li><li>Babelfish accepts connections from SQL Server applications into Aurora PostgreSQL — the answer to \"migrate off SQL Server without rewriting client code.\"</li></ul>\n<h2>Aurora vs. RDS</h2>\n<div class='table-wrap'><table><thead><tr><th>Requirement</th><th>Choice</th></tr></thead><tbody><tr><td>Maximum throughput and up to 15 replicas</td><td>Aurora</td></tr><tr><td>Failover in seconds</td><td>Aurora</td></tr><tr><td>Oracle, SQL Server, Db2 engines</td><td>RDS</td></tr><tr><td>Lowest cost for a small, stable database</td><td>RDS</td></tr><tr><td>On-the-fly environment copies for testing</td><td>Aurora (fast clone)</td></tr><tr><td>Global reads with RPO in seconds</td><td>Aurora Global Database</td></tr><tr><td>Workload running two hours a week</td><td>Aurora Serverless v2 or a stopped RDS instance</td></tr></tbody></table></div>\n<h2>What the exam asks</h2>\n<ol><li>\"Reporting queries must not interfere with writes\" — a reader endpoint or a custom endpoint on dedicated instances.</li><li>\"Need a copy of the production database for testing by tonight\" — fast clone.</li><li>\"A developer dropped a table an hour ago\" — Backtrack (MySQL) or PITR.</li><li>\"Application spans three regions, local reads, recover from a regional outage within minutes\" — Global Database.</li><li>\"Workload is unpredictable, we don't want to pay for idle time\" — Serverless v2.</li><li>\"The bill grew because of I/O operations\" — I/O-Optimized.</li><li>\"Need to scale writes\" — scale the writer vertically or shard; replicas don't scale writes.</li><li>\"Compatibility with a SQL Server application without rewriting it\" — Babelfish.</li></ol>\n<h2>Numbers to remember</h2>\n<ul><li>6 copies of data across 3 AZs; losing 2 copies doesn't block writes, losing 3 blocks reads;</li><li>up to 15 reader instances, lag of tens of milliseconds;</li><li>the volume grows automatically up to 128 TB, in 10 GB increments;</li><li>failover is usually under 30 seconds, vs. 60–120 seconds for a Multi-AZ RDS instance;</li><li>Global Database: up to 10 secondary regions, replication lag around a second, promotion in about a minute;</li><li>Backtrack goes back up to 72 hours (MySQL-compatible edition only);</li><li>Serverless v2 scales in 0.5 ACU increments without dropping connections.</li></ul>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>Wording in the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"read-heavy, reports slow down writes\"</td><td>reader endpoint or additional readers</td></tr><tr><td>\"need a copy of production for testing quickly\"</td><td>fast clone</td></tr><tr><td>\"accidentally dropped a table an hour ago\"</td><td>Backtrack or PITR</td></tr><tr><td>\"users in three continents, local reads\"</td><td>Global Database</td></tr><tr><td>\"unpredictable, spiky workload\"</td><td>Serverless v2</td></tr><tr><td>\"I/O charges dominate the bill\"</td><td>I/O-Optimized</td></tr><tr><td>\"must support Oracle\"</td><td>that's not Aurora, it's RDS</td></tr><tr><td>\"application uses SQL Server drivers\"</td><td>Babelfish on Aurora PostgreSQL</td></tr></tbody></table></div>\n<h2>What Aurora doesn't solve</h2>\n<p>Writes only scale vertically: reader instances don't accept writes, and the question \"how do we scale writes\" is answered by a bigger writer, splitting data across clusters, or moving to DynamoDB if the data model allows it. Aurora doesn't turn a relational workload serverless for free: Serverless v2 is billed per ACU-hour and, under constant load, costs more than a provisioned cluster.</p>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> An Aurora MySQL cluster backs an online store. Analysts complain their reports take minutes and slow down checkout. What do you do? → A custom endpoint on dedicated reader instances: reports go to their own nodes, the writer stays free for orders.</p>\n<p><strong>2.</strong> A developer needs a 4 TB copy of the production database by end of day, on a tight budget. → Fast database clone: the copy is created in minutes via copy-on-write and takes up almost no extra space.</p>\n<p><strong>3.</strong> The cluster runs two hours a week and sits idle the rest of the time. → Aurora Serverless v2: scales down when idle; a provisioned cluster can be stopped for at most 7 days before it starts itself back up.</p>\n<p><strong>4.</strong> The application runs in three regions, needs local reads and recovery from a regional outage within minutes. → Aurora Global Database: replication around a second, promotion of the secondary region in about a minute.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"827\" href=\"#\">questions 827</a> — Aurora only has Standard and I/O-Optimized; options with General Purpose and Provisioned IOPS belong to RDS; the dump key has been corrected.</li><li><a class=\"qref\" data-q=\"574\" href=\"#\">questions 574</a> — disputed: two hours of use per week; Serverless v2 doesn't always scale to zero, and a provisioned cluster can only be stopped for up to 7 days.</li><li><a class=\"qref\" data-q=\"851\" href=\"#\">questions 851</a> — disputed: Aurora Serverless scales down when idle, which more precisely matches the requirement \"resources are only needed while running.\"</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>9</td><td>11%</td><td>2</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>33</td><td>41%</td><td>8</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>21</td><td>26%</td><td>15</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>17</td><td>21%</td><td>0</td></tr></tbody></table></div><p>Total questions in this chapter: <b>80</b>. With a written walkthrough: <b>80</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#25</b> — RES: buffer between API and database instead of scaling quotas</li><li><b>#87</b> — RES: avoiding data loss during upgrade</li><li><b>#94</b> — PERF: elastic processing under variable load</li><li><b>#129</b> — PERF: removing the scaling ceiling and capacity planning</li><li><b>#136</b> — RES: database stays online and in sync during migration</li><li><b>#175</b> — PERF: timeouts from database connection exhaustion</li><li><b>#235</b> — RES: keeping databases in sync during a phased migration</li><li><b>#244</b> — RES: separating tiers for availability</li></ul>","text":" Aurora Aurora is protocol compatible with MySQL and PostgreSQL, but its storage is its own: a distributed volume that grows up to 128 TB, with six copies of every block across three Availability Zones. The cluster survives the loss of two copies with no data loss, and the loss of three copies with no loss of read availability. That's where all of its properties come from: fast failover, cheap replicas, cloning, and point in time recovery. Cluster architecture writer — the single instance that accepts writes; up to 15 reader instances reading the same volume, typically tens of milliseconds behind; storage is shared and self healing; replicas don't copy the data; if the writer fails, one of the readers becomes the writer, usually within seconds, with the order set by instance priority (tier). Endpoints — what gets tested most often: Endpoint What it points to When to use it Cluster (writer) the current writer writes; follows failover Reader load balances across readers application reads Custom a defined subset of instances a dedicated pool for reporting or BI Instance a specific instance diagnostics, targeted tasks The application should use the reader endpoint for reads; if an answer option routes reads to the cluster endpoint, that option is wrong. Storage types and pricing Aurora has only two storage configuration types: Standard and I/O Optimized. The terms gp2, gp3, and Provisioned IOPS belong to RDS and EBS, not to an Aurora cluster — a ready made trap. Standard bills I/O operations separately; I/O Optimized folds them into instance pricing and pays off once I/O is roughly 25 percent or more of the bill. Overall cost structure: instances (or ACUs in Serverless), storage per GB, I/O operations under Standard, backups beyond the cluster size, and cross AZ/cross Region traffic. Serverless v2 Serverless v2 scales instance capacity continuously in ACU units (0.5 ACU and up, with scale to zero available for certain configurations since 2024) without dropping connections. It suits unpredictable, \"spiky\" workloads, dev environments, and multi tenant applications. An important detail for cost questions: a regular provisioned cluster can be stopped for at most 7 days before it starts itself back up, while Serverless v2 scales itself down when idle. Global Database Aurora Global Database replicates data to up to 10 secondary regions with typical replication lag under a second. A secondary region provides local reads and lets you promote it in about a minute: RPO is measured in seconds, RTO in minutes. This is the standard answer to \"global application with regional reads and fast recovery from a regional outage.\" A cross Region RDS read replica is slower and takes longer to fail over. Clones, backups, recovery Backtrack (MySQL compatible edition only) rewinds a cluster by several hours in minutes, without restoring a snapshot — the answer to \"roll back an accidental DELETE as fast as possible\"; fast database cloning creates a copy on write copy in minutes with almost no extra storage — the answer to \"need a copy of prod for testing\"; continuous backup to S3 provides PITR within the retention period; snapshots can be copied across regions and accounts. Additional features Aurora Auto Scaling adds readers based on a load metric; Parallel Query (MySQL compatible edition) speeds up analytical queries by pushing filtering down into the storage layer; Aurora Machine Learning calls SageMaker and Comprehend directly from SQL; Zero ETL integration with Redshift moves data into analytics without a pipeline; Babelfish accepts connections from SQL Server applications into Aurora PostgreSQL — the answer to \"migrate off SQL Server without rewriting client code.\" Aurora vs. RDS Requirement Choice Maximum throughput and up to 15 replicas Aurora Failover in seconds Aurora Oracle, SQL Server, Db2 engines RDS Lowest cost for a small, stable database RDS On the fly environment copies for testing Aurora (fast clone) Global reads with RPO in seconds Aurora Global Database Workload running two hours a week Aurora Serverless v2 or a stopped RDS instance What the exam asks 1. \"Reporting queries must not interfere with writes\" — a reader endpoint or a custom endpoint on dedicated instances. 2. \"Need a copy of the production database for testing by tonight\" — fast clone. 3. \"A developer dropped a table an hour ago\" — Backtrack (MySQL) or PITR. 4. \"Application spans three regions, local reads, recover from a regional outage within minutes\" — Global Database. 5. \"Workload is unpredictable, we don't want to pay for idle time\" — Serverless v2. 6. \"The bill grew because of I/O operations\" — I/O Optimized. 7. \"Need to scale writes\" — scale the writer vertically or shard; replicas don't scale writes. 8. \"Compatibility with a SQL Server application without rewriting it\" — Babelfish. Numbers to remember 6 copies of data across 3 AZs; losing 2 copies doesn't block writes, losing 3 blocks reads; up to 15 reader instances, lag of tens of milliseconds; the volume grows automatically up to 128 TB, in 10 GB increments; failover is usually under 30 seconds, vs. 60–120 seconds for a Multi AZ RDS instance; Global Database: up to 10 secondary regions, replication lag around a second, promotion in about a minute; Backtrack goes back up to 72 hours (MySQL compatible edition only); Serverless v2 scales in 0.5 ACU increments without dropping connections. How to read the wording Wording in the question Answer \"read heavy, reports slow down writes\" reader endpoint or additional readers \"need a copy of production for testing quickly\" fast clone \"accidentally dropped a table an hour ago\" Backtrack or PITR \"users in three continents, local reads\" Global Database \"unpredictable, spiky workload\" Serverless v2 \"I/O charges dominate the bill\" I/O Optimized \"must support Oracle\" that's not Aurora, it's RDS \"application uses SQL Server drivers\" Babelfish on Aurora PostgreSQL What Aurora doesn't solve Writes only scale vertically: reader instances don't accept writes, and the question \"how do we scale writes\" is answered by a bigger writer, splitting data across clusters, or moving to DynamoDB if the data model allows it. Aurora doesn't turn a relational workload serverless for free: Serverless v2 is billed per ACU hour and, under constant load, costs more than a provisioned cluster. Mini practicum 1. An Aurora MySQL cluster backs an online store. Analysts complain their reports take minutes and slow down checkout. What do you do? → A custom endpoint on dedicated reader instances: reports go to their own nodes, the writer stays free for orders. 2. A developer needs a 4 TB copy of the production database by end of day, on a tight budget. → Fast database clone: the copy is created in minutes via copy on write and takes up almost no extra space. 3. The cluster runs two hours a week and sits idle the rest of the time. → Aurora Serverless v2: scales down when idle; a provisioned cluster can be stopped for at most 7 days before it starts itself back up. 4. The application runs in three regions, needs local reads and recovery from a regional outage within minutes. → Aurora Global Database: replication around a second, promotion of the secondary region in about a minute. Bank audit {{q:827}} — Aurora only has Standard and I/O Optimized; options with General Purpose and Provisioned IOPS belong to RDS; the dump key has been corrected. {{q:574}} — disputed: two hours of use per week; Serverless v2 doesn't always scale to zero, and a provisioned cluster can only be stopped for up to 7 days. {{q:851}} — disputed: Aurora Serverless scales down when idle, which more precisely matches the requirement \"resources are only needed while running.\" "},{"id":"dynamodb","title":"DynamoDB — managed NoSQL","order":9,"svc":["DynamoDB"],"questions":[7,13,21,25,33,63,71,78,79,94,114,132,175,176,177,181,183,191,196,197,198,200,203,207,223,236,266,268,269,278,279,280,295,317,323,337,348,354,361,362,366,372,378,381,390,400,411,428,434,458,461,472,489,490,507,520,521,523,537,541,556,561,562,565,567,572,575,578,607,608,622,637,650,661,666,670,683,693,694,713,726,727,731,735,755,756,768,776,785,799,817,819,834,845,848,849,850,864,874,901,917,933,962,964,968,979,991,995,1002,1005,1012,1017],"html":"<h1>DynamoDB</h1>\n<p>A key-value and document store with predictable single-digit-millisecond latency and no servers to administer. Data is automatically partitioned across three Availability Zones, and capacity and storage grow without manual intervention.</p>\n<h2>Data model</h2>\n<ul><li>a table consists of items up to 400 KB;</li><li>primary key: either a partition key alone, or a partition key plus a sort key;</li><li>the partition key determines the partition — its cardinality drives load uniformity; a \"hot\" key runs into a partition's limit;</li><li>attributes aren't declared up front, except the ones used in keys and indexes;</li><li>queries: <code>GetItem</code> by key, <code>Query</code> by partition key with a condition on the sort key, <code>Scan</code> across the whole table (expensive, use only deliberately).</li></ul>\n<h2>Indexes</h2>\n<div class='table-wrap'><table><thead><tr><th></th><th>LSI</th><th>GSI</th></tr></thead><tbody><tr><td>When it's created</td><td>only at table creation</td><td>anytime</td></tr><tr><td>Partition key</td><td>same as the table's</td><td>any</td></tr><tr><td>Consistency</td><td>strong consistency possible</td><td>eventual only</td></tr><tr><td>Capacity</td><td>shared with the table</td><td>its own</td></tr><tr><td>Limit</td><td>5 per table, 10 GB per partition key</td><td>20 per table</td></tr></tbody></table></div>\n<p>Practical takeaway: nearly every answer to \"need to look up by a different attribute\" is a GSI. LSI shows up in answers only when strongly consistent reads on an alternate sort key are required and the index could have been set up at table creation.</p>\n<h2>Capacity modes</h2>\n<ul><li>On-demand: you pay per request, absorbs spikes instantly, ideal for unpredictable load and new applications;</li><li>Provisioned: cheaper for steady load, you set RCUs and WCUs, complemented by auto scaling and Reserved Capacity.</li></ul>\n<p>1 RCU = one strongly consistent read of an item up to 4 KB per second (or two eventually consistent reads); 1 WCU = one write of an item up to 1 KB per second. Transactional operations cost double. When load jumps sharply from thousands to millions of users, on-demand wins: auto scaling for provisioned capacity reacts with a delay of minutes.</p>\n<h2>Consistency and transactions</h2>\n<p>By default, reads are eventually consistent — a replica may lag by fractions of a second. Strongly consistent reads are requested via a flag and cost twice as much; they're unavailable on GSIs. TransactWriteItems and TransactGetItems provide ACID guarantees within a Region for up to 100 items. Conditional writes (<code>ConditionExpression</code>) are the standard way to implement optimistic locking and idempotency.</p>\n<h2>Streams and event processing</h2>\n<p>DynamoDB Streams retains changes for 24 hours and invokes Lambda for each record: auditing, aggregation, replication into OpenSearch, sending notifications. Kinesis Data Streams for DynamoDB gives longer retention and different consumers. A requirement to \"react to data changes\" is solved with a stream, not by polling the table on a schedule.</p>\n<h2>Performance and caching</h2>\n<ul><li>DAX — an in-memory cache in front of the table, cuts read latency down to microseconds, works transparently with the DynamoDB API, and lives in a VPC. The answer to \"reads of hot data must be faster than milliseconds\" without rewriting the application;</li><li>ElastiCache is a better fit when you're caching aggregates and query results rather than table items;</li><li>Adaptive capacity smooths out unevenness, but won't save you from a truly hot key: design a partition key that spreads load (for example, by adding a suffix).</li></ul>\n<h2>Storage, TTL, and backups</h2>\n<ul><li>TTL deletes expired items for free — the typical answer to \"automatically delete records older than N days\";</li><li>PITR restores state to any second within the last 35 days;</li><li>full backups can be taken on demand, integrated with AWS Backup;</li><li>export to S3 without affecting table performance, followed by Athena or Glue;</li><li>the Standard-IA table class stores infrequently read data more cheaply.</li></ul>\n<h2>Global tables</h2>\n<p>Global tables provide multi-Region replication with active writes in every Region, using \"last writer wins\" conflict resolution, and require Streams to be enabled. The answer to \"users worldwide write and read locally, a Region may fail.\"</p>\n<h2>What the exam asks</h2>\n<ol><li>\"Product catalog with unpredictable load and lookups by key\" — DynamoDB on-demand.</li><li>\"Need to search by a non-key attribute\" — GSI.</li><li>\"Store web application sessions\" — DynamoDB with TTL (or ElastiCache when latency requirements are strict).</li><li>\"React to every record change\" — Streams plus Lambda.</li><li>\"Microsecond read latency without code changes\" — DAX.</li><li>\"Multi-Region application writing in every Region\" — global tables.</li><li>\"Analytics over DynamoDB data\" — export to S3 and Athena, not a Scan against the table.</li><li>\"Complex joins and reporting\" — that's not DynamoDB, it's a relational database.</li></ol>\n<h2>Numbers to remember</h2>\n<ul><li>item up to 400 KB, partition key up to 2048 bytes, sort key up to 1024 bytes;</li><li>1 RCU = 1 strongly consistent read of 4 KB per second (2 eventual), 1 WCU = write of 1 KB;</li><li>transactions cost double and cover up to 100 items;</li><li>a partition sustains around 3000 RCU and 1000 WCU;</li><li>Query returns up to 1 MB per call, then pagination kicks in;</li><li>20 GSIs and 5 LSIs per table, LSI limited to 10 GB per partition key value;</li><li>Streams retain changes for 24 hours, PITR for 35 days;</li><li>BatchGetItem up to 100 items, BatchWriteItem up to 25.</li></ul>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>Wording in the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"key-value access, single-digit millisecond\"</td><td>DynamoDB</td></tr><tr><td>\"microsecond reads without code changes\"</td><td>DAX</td></tr><tr><td>\"query by an attribute that is not the key\"</td><td>GSI</td></tr><tr><td>\"delete records older than 90 days automatically\"</td><td>TTL</td></tr><tr><td>\"react to every item change\"</td><td>Streams plus Lambda</td></tr><tr><td>\"write locally in multiple Regions\"</td><td>global tables</td></tr><tr><td>\"unpredictable traffic, no capacity planning\"</td><td>on-demand</td></tr><tr><td>\"complex joins and ad-hoc SQL\"</td><td>relational database, not DynamoDB</td></tr></tbody></table></div>\n<h2>Key design</h2>\n<p>Modeling mistakes cause more failures than capacity shortfalls. Signs of a bad key: a single partition key value serves most of the traffic (for example, a date used as the key for an event feed), Scan is used as the routine way to read data, every new query requires a new GSI. Techniques: suffixes and hashes in the key to spread load, a composite sort key like <code>ORDER#2026-05-14</code> for range queries, single-table design with an entity-type attribute, moving aggregates into separate items updated by a Streams handler.</p>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> A gaming app grew from thousands to millions of users in a week, spikes are unpredictable, downtime isn't acceptable. → On-demand capacity: absorbs spikes instantly, whereas auto scaling for provisioned capacity reacts on the order of minutes.</p>\n<p><strong>2.</strong> You need to find orders by customer email, but the table's key is the order ID. → GSI with partition key email; an LSI doesn't fit — it can't be added after the table is created and it requires the same partition key.</p>\n<p><strong>3.</strong> Every record change must land in a search index. → DynamoDB Streams plus a Lambda function writing into OpenSearch.</p>\n<p><strong>4.</strong> Analysts need a full export of the table for BI without affecting the application. → Export to S3 and Athena queries; a Scan against the table would burn capacity and slow down the application.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"622\" href=\"#\">questions 622</a> — disputed: when load spikes from thousands to millions of users, on-demand scales instantly, while auto scaling for provisioned capacity lags behind.</li><li><a class=\"qref\" data-q=\"372\" href=\"#\">questions 372</a> — disputed: pairing S3 with DynamoDB is cheaper and more scalable, but the source data doesn't always reduce to a simple key.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>18</td><td>16%</td><td>3</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>34</td><td>30%</td><td>12</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>41</td><td>37%</td><td>26</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>19</td><td>17%</td><td>2</td></tr></tbody></table></div><p>Total questions in this chapter: <b>112</b>. With a written walkthrough: <b>112</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#25</b> — RES: buffer between API and database instead of scaling quotas</li><li><b>#79</b> — COST: unpredictable spikes without overpaying</li><li><b>#94</b> — PERF: elastic processing under variable load</li><li><b>#175</b> — PERF: timeouts from database connection exhaustion</li><li><b>#177</b> — PERF: read latency without application changes</li><li><b>#181</b> — RES: messaging between microservices without ordering</li><li><b>#191</b> — PERF: reports slowing down order processing</li><li><b>#196</b> — COST: retaining only the last 30 days of data</li></ul>","text":" DynamoDB A key value and document store with predictable single digit millisecond latency and no servers to administer. Data is automatically partitioned across three Availability Zones, and capacity and storage grow without manual intervention. Data model a table consists of items up to 400 KB; primary key: either a partition key alone, or a partition key plus a sort key; the partition key determines the partition — its cardinality drives load uniformity; a \"hot\" key runs into a partition's limit; attributes aren't declared up front, except the ones used in keys and indexes; queries: GetItem by key, Query by partition key with a condition on the sort key, Scan across the whole table (expensive, use only deliberately). Indexes LSI GSI When it's created only at table creation anytime Partition key same as the table's any Consistency strong consistency possible eventual only Capacity shared with the table its own Limit 5 per table, 10 GB per partition key 20 per table Practical takeaway: nearly every answer to \"need to look up by a different attribute\" is a GSI. LSI shows up in answers only when strongly consistent reads on an alternate sort key are required and the index could have been set up at table creation. Capacity modes On demand: you pay per request, absorbs spikes instantly, ideal for unpredictable load and new applications; Provisioned: cheaper for steady load, you set RCUs and WCUs, complemented by auto scaling and Reserved Capacity. 1 RCU = one strongly consistent read of an item up to 4 KB per second (or two eventually consistent reads); 1 WCU = one write of an item up to 1 KB per second. Transactional operations cost double. When load jumps sharply from thousands to millions of users, on demand wins: auto scaling for provisioned capacity reacts with a delay of minutes. Consistency and transactions By default, reads are eventually consistent — a replica may lag by fractions of a second. Strongly consistent reads are requested via a flag and cost twice as much; they're unavailable on GSIs. TransactWriteItems and TransactGetItems provide ACID guarantees within a Region for up to 100 items. Conditional writes ( ConditionExpression ) are the standard way to implement optimistic locking and idempotency. Streams and event processing DynamoDB Streams retains changes for 24 hours and invokes Lambda for each record: auditing, aggregation, replication into OpenSearch, sending notifications. Kinesis Data Streams for DynamoDB gives longer retention and different consumers. A requirement to \"react to data changes\" is solved with a stream, not by polling the table on a schedule. Performance and caching DAX — an in memory cache in front of the table, cuts read latency down to microseconds, works transparently with the DynamoDB API, and lives in a VPC. The answer to \"reads of hot data must be faster than milliseconds\" without rewriting the application; ElastiCache is a better fit when you're caching aggregates and query results rather than table items; Adaptive capacity smooths out unevenness, but won't save you from a truly hot key: design a partition key that spreads load (for example, by adding a suffix). Storage, TTL, and backups TTL deletes expired items for free — the typical answer to \"automatically delete records older than N days\"; PITR restores state to any second within the last 35 days; full backups can be taken on demand, integrated with AWS Backup; export to S3 without affecting table performance, followed by Athena or Glue; the Standard IA table class stores infrequently read data more cheaply. Global tables Global tables provide multi Region replication with active writes in every Region, using \"last writer wins\" conflict resolution, and require Streams to be enabled. The answer to \"users worldwide write and read locally, a Region may fail.\" What the exam asks 1. \"Product catalog with unpredictable load and lookups by key\" — DynamoDB on demand. 2. \"Need to search by a non key attribute\" — GSI. 3. \"Store web application sessions\" — DynamoDB with TTL (or ElastiCache when latency requirements are strict). 4. \"React to every record change\" — Streams plus Lambda. 5. \"Microsecond read latency without code changes\" — DAX. 6. \"Multi Region application writing in every Region\" — global tables. 7. \"Analytics over DynamoDB data\" — export to S3 and Athena, not a Scan against the table. 8. \"Complex joins and reporting\" — that's not DynamoDB, it's a relational database. Numbers to remember item up to 400 KB, partition key up to 2048 bytes, sort key up to 1024 bytes; 1 RCU = 1 strongly consistent read of 4 KB per second (2 eventual), 1 WCU = write of 1 KB; transactions cost double and cover up to 100 items; a partition sustains around 3000 RCU and 1000 WCU; Query returns up to 1 MB per call, then pagination kicks in; 20 GSIs and 5 LSIs per table, LSI limited to 10 GB per partition key value; Streams retain changes for 24 hours, PITR for 35 days; BatchGetItem up to 100 items, BatchWriteItem up to 25. How to read the wording Wording in the question Answer \"key value access, single digit millisecond\" DynamoDB \"microsecond reads without code changes\" DAX \"query by an attribute that is not the key\" GSI \"delete records older than 90 days automatically\" TTL \"react to every item change\" Streams plus Lambda \"write locally in multiple Regions\" global tables \"unpredictable traffic, no capacity planning\" on demand \"complex joins and ad hoc SQL\" relational database, not DynamoDB Key design Modeling mistakes cause more failures than capacity shortfalls. Signs of a bad key: a single partition key value serves most of the traffic (for example, a date used as the key for an event feed), Scan is used as the routine way to read data, every new query requires a new GSI. Techniques: suffixes and hashes in the key to spread load, a composite sort key like ORDER 2026 05 14 for range queries, single table design with an entity type attribute, moving aggregates into separate items updated by a Streams handler. Mini practicum 1. A gaming app grew from thousands to millions of users in a week, spikes are unpredictable, downtime isn't acceptable. → On demand capacity: absorbs spikes instantly, whereas auto scaling for provisioned capacity reacts on the order of minutes. 2. You need to find orders by customer email, but the table's key is the order ID. → GSI with partition key email; an LSI doesn't fit — it can't be added after the table is created and it requires the same partition key. 3. Every record change must land in a search index. → DynamoDB Streams plus a Lambda function writing into OpenSearch. 4. Analysts need a full export of the table for BI without affecting the application. → Export to S3 and Athena queries; a Scan against the table would burn capacity and slow down the application. Bank audit {{q:622}} — disputed: when load spikes from thousands to millions of users, on demand scales instantly, while auto scaling for provisioned capacity lags behind. {{q:372}} — disputed: pairing S3 with DynamoDB is cheaper and more scalable, but the source data doesn't always reduce to a simple key. "},{"id":"lambda","title":"Lambda and serverless computing","order":10,"svc":["Lambda","API Gateway"],"questions":[4,7,10,13,18,21,25,31,32,33,45,46,50,51,56,59,61,63,65,69,72,75,77,78,82,87,94,98,100,104,105,107,108,110,112,114,116,117,119,124,130,132,139,140,142,143,145,148,149,152,154,156,159,161,171,175,180,181,182,183,184,190,192,195,196,197,200,201,203,204,206,207,211,214,220,225,226,227,228,236,239,255,257,258,266,267,268,272,274,278,283,285,289,295,297,303,311,312,315,316,317,319,322,323,335,336,337,339,342,345,351,354,355,360,366,369,375,377,379,380,393,397,399,403,404,408,412,417,422,427,428,430,434,441,444,447,452,457,458,460,463,468,478,483,490,491,492,498,501,507,508,513,516,522,523,528,530,531,532,541,544,547,550,561,567,571,573,576,579,587,591,597,600,602,603,608,609,611,613,621,623,630,635,636,637,638,640,653,654,657,661,669,680,682,683,685,687,689,698,711,715,717,720,721,724,727,735,736,739,742,752,756,758,760,768,769,770,772,774,775,784,785,791,792,793,794,796,798,799,800,802,807,808,809,814,819,820,829,833,839,845,847,848,853,863,865,869,871,874,877,879,881,882,883,885,888,897,898,904,907,910,911,912,913,914,917,920,929,930,931,936,944,947,951,954,959,961,964,967,981,988,991,994,995,1005,1008,1014,1019],"html":"<h1>Lambda</h1>\n<p>A function runs in response to an event; you pay for execution time and number of invocations, with no servers to manage. Typical roles in an architecture: an event handler for S3, DynamoDB Streams, SQS, EventBridge; an API backend behind API Gateway or an ALB; glue between services; scheduled periodic tasks.</p>\n<h2>Limits the exam tests</h2>\n<div class='table-wrap'><table><thead><tr><th>Parameter</th><th>Value</th></tr></thead><tbody><tr><td>Maximum execution time</td><td>15 minutes</td></tr><tr><td>Memory</td><td>128 MB — 10,240 MB</td></tr><tr><td>vCPU</td><td>proportional to memory, up to 6 at maximum</td></tr><tr><td>Package size</td><td>50 MB zip (compressed), 250 MB unzipped, 10 GB container image</td></tr><tr><td><code>/tmp</code></td><td>512 MB — 10,240 MB</td></tr><tr><td>Payload</td><td>6 MB synchronous, 256 KB asynchronous</td></tr><tr><td>Account concurrency</td><td>1000 by default, raised via quotas</td></tr></tbody></table></div>\n<p>A requirement that \"processing takes longer than 15 minutes\" automatically rules out Lambda: the answer moves to ECS/Fargate, AWS Batch, or Step Functions broken into steps.</p>\n<h2>Invocation models</h2>\n<ul><li>Synchronous (API Gateway, ALB, direct invoke): the error is returned to the caller, retries are the caller's responsibility.</li><li>Asynchronous (S3, SNS, EventBridge): two built-in retries, then a destination or a dead-letter queue.</li><li>Poll-based (SQS, Kinesis, DynamoDB Streams): Lambda polls the source itself, in batches; order is guaranteed for FIFO and for a stream shard.</li></ul>\n<p>Error handling: a DLQ or Lambda Destinations (success and failure are routed to SQS, SNS, EventBridge, or another function). Handler idempotency is a requirement, not a nice-to-have.</p>\n<h2>Concurrency</h2>\n<ul><li>Reserved concurrency reserves part of the account-wide limit for a function while also capping it — a way to protect a database from overload and stop one function from eating the whole account limit;</li><li>Provisioned concurrency keeps initialized environments ready — the answer to \"cold starts are unacceptable\", especially for Java and .NET;</li><li>SnapStart dramatically speeds up Java startup by snapshotting an initialized environment;</li><li>once the limit is exhausted, calls get throttled (429); asynchronous events are queued and retried.</li></ul>\n<h2>Function in a VPC</h2>\n<p>By default, functions run outside your VPC and have internet access. Connecting to a VPC is needed to reach RDS, ElastiCache, or internal services. Once connected, internet access disappears: you need a NAT Gateway for public APIs or interface endpoints for AWS services. This is exactly the trap where \"the function can't reach an external API\" is solved with NAT, not by editing a security group.</p>\n<h2>Layers, images, environment</h2>\n<p>A layer is shared code and dependencies, up to 5 layers per function; handy for SDKs, client libraries, and the Secrets Manager/Parameter Store extension. A container image up to 10 GB suits large ML dependencies. Environment variables are encrypted with KMS; secrets belong in Secrets Manager, fetched through the caching extension rather than hardcoded.</p>\n<p>Versions and aliases give you a controlled rollout: a <code>prod</code> alias with weights routes a percentage of traffic to a new version (canary), and rollback is instant.</p>\n<h2>Cost</h2>\n<p>Charge = number of requests + GB-seconds. More memory raises both the price per millisecond and the speed: a function with 1024 MB is often cheaper than one with 128 MB, because it finishes many times faster. AWS Lambda Power Tuning finds the optimum. Compute Savings Plans cover Lambda alongside EC2 and Fargate.</p>\n<h2>API Gateway</h2>\n<div class='table-wrap'><table><thead><tr><th>API type</th><th>Protocol</th><th>Features</th></tr></thead><tbody><tr><td>REST</td><td>HTTP</td><td>the most features: caching, usage plans, API keys, WAF, request validation</td></tr><tr><td>HTTP</td><td>HTTP</td><td>cheaper and faster, JWT authorization, fewer features</td></tr><tr><td>WebSocket</td><td>WSS</td><td>two-way communication, chat, notifications</td></tr></tbody></table></div>\n<p>Capabilities the exam tests: throttling and usage plans for partners, response caching to cut backend load, authorization via IAM, Cognito, or a Lambda authorizer, direct service integrations (e.g., putting a message on SQS with no function involved), private APIs via an interface endpoint, stages (dev, prod), and canary releases.</p>\n<p>A Lambda function URL is a direct HTTPS endpoint for a function, with or without IAM authorization. It's the right answer wherever you need a single call to a single function and don't need routing, throttling, keys, or transformations: API Gateway would be an unnecessary extra service there.</p>\n<h2>Step Functions</h2>\n<p>A state orchestrator: sequences, parallel branches, conditions, retries, timeouts, compensations. Standard workflow — up to a year, billed per transition, exactly-once; Express — up to 5 minutes, billed by duration, for high-frequency events. The answer to \"a complex multi-step process with retries and branching\" and to \"processing takes longer than 15 minutes but the logic breaks into steps\".</p>\n<h2>What the exam asks</h2>\n<ol><li>\"Process an image on upload to S3\" — an S3 event plus Lambda.</li><li>\"A spike in requests breaks the database\" — reserved concurrency and RDS Proxy.</li><li>\"The first request after idle time is too slow\" — provisioned concurrency or SnapStart.</li><li>\"The function can't see the database in a VPC\" — connect it to the VPC and open the security group.</li><li>\"A function in a VPC lost internet access\" — a NAT Gateway or an endpoint.</li><li>\"Need a single HTTPS endpoint for one function, minimum services\" — a function URL.</li><li>\"The job runs for 40 minutes\" — Fargate, Batch, or Step Functions, not Lambda.</li><li>\"Messages must be processed in strict order\" — SQS FIFO with a handler whose concurrency is bounded by the message group.</li></ol>\n<h2>Numbers to remember</h2>\n<ul><li>15 minutes max, 10,240 MB memory, up to 6 vCPUs at maximum memory;</li><li>50 MB zip package and 250 MB unzipped, image up to 10 GB;</li><li>payload 6 MB synchronous and 256 KB asynchronous;</li><li><code>/tmp</code> from 512 MB up to 10 GB;</li><li>1000 concurrent executions by default per account and region;</li><li>asynchronous invocations retry twice, events are kept for up to 6 hours;</li><li>batch size up to 10,000 records for Kinesis and DynamoDB Streams, up to 10,000 for SQS in standard queue mode;</li><li>API Gateway: 29-second integration timeout, 10 MB payload.</li></ul>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>In the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"no servers to manage, pay per request\"</td><td>Lambda</td></tr><tr><td>\"processing takes 30 minutes\"</td><td>Fargate, Batch, or Step Functions</td></tr><tr><td>\"cold start unacceptable\"</td><td>provisioned concurrency, SnapStart</td></tr><tr><td>\"protect the database from too many connections\"</td><td>reserved concurrency plus RDS Proxy</td></tr><tr><td>\"function cannot reach the internet\"</td><td>NAT Gateway or an endpoint</td></tr><tr><td>\"single function needs an HTTPS endpoint\"</td><td>a function URL</td></tr><tr><td>\"throttle partner APIs, issue API keys\"</td><td>API Gateway usage plans</td></tr><tr><td>\"coordinate multiple steps with retries\"</td><td>Step Functions</td></tr></tbody></table></div>\n<h2>Observability and debugging</h2>\n<p>Function logs go to CloudWatch Logs, and metrics like Invocations, Errors, Throttles, Duration, and ConcurrentExecutions go to CloudWatch. X-Ray shows call traces across services and helps find where time is lost. Typical alarms: rising Throttles (hitting the concurrency limit), rising Errors alongside a DLQ (problem events), Duration near the timeout limit. For events that can't be lost, a DLQ or Destinations is mandatory: without one, an asynchronous event disappears after two retries.</p>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> A function processes files from a bucket; during a marketing campaign some events got lost. → An SQS queue in front of the function: the buffer absorbs the spike, nothing is lost.</p>\n<p><strong>2.</strong> Java functions take a long time to start, there's no hard latency requirement, and budget matters. → SnapStart: a snapshot of the initialized environment without paying for a standing reserve.</p>\n<p><strong>3.</strong> Employees log in simultaneously in the morning, and the first requests are slow. → Scheduled provisioned concurrency before the start of the workday.</p>\n<p><strong>4.</strong> A job runs for an hour and is written in a non-standard language. → This isn't Lambda: the 15-minute execution limit means the job belongs on AWS Batch or containers.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"239\" href=\"#\">questions 239</a> — a single microservice with AWS_IAM authorization: a Lambda function URL, API Gateway would add an unneeded service; dump key corrected.</li><li><a class=\"qref\" data-q=\"809\" href=\"#\">questions 809</a> — a Batch job completion event is caught by an EventBridge rule, not EventBridge Scheduler; calling an external HTTP API is done via an API destination.</li><li><a class=\"qref\" data-q=\"110\" href=\"#\">questions 110</a> — decoupling image upload from processing: a queue plus Lambda, key corrected during the CD audit.</li><li><a class=\"qref\" data-q=\"422\" href=\"#\">questions 422</a> — low confidence: SQS with Fargate vs. SQS with Lambda, the choice depends on the size of the ML models, which the text doesn't state directly.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>81</td><td>28%</td><td>20</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>72</td><td>25%</td><td>35</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>80</td><td>28%</td><td>55</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>55</td><td>19%</td><td>6</td></tr></tbody></table></div><p>Total questions in this chapter: <b>288</b>. With a written walkthrough: <b>288</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#10</b> — RES: guaranteeing order processing sequence</li><li><b>#25</b> — RES: buffer between API and database instead of scaling quotas</li><li><b>#31</b> — SEC: enforcing resource tag compliance</li><li><b>#45</b> — RES: retries and a dead-letter queue</li><li><b>#50</b> — SEC: urgent vulnerability patching</li><li><b>#51</b> — RES: event-driven report generation and distribution</li><li><b>#59</b> — PERF: ingesting and processing clickstream data</li><li><b>#65</b> — SEC: detecting protected health data</li></ul>","text":" Lambda A function runs in response to an event; you pay for execution time and number of invocations, with no servers to manage. Typical roles in an architecture: an event handler for S3, DynamoDB Streams, SQS, EventBridge; an API backend behind API Gateway or an ALB; glue between services; scheduled periodic tasks. Limits the exam tests Parameter Value Maximum execution time 15 minutes Memory 128 MB — 10,240 MB vCPU proportional to memory, up to 6 at maximum Package size 50 MB zip (compressed), 250 MB unzipped, 10 GB container image /tmp 512 MB — 10,240 MB Payload 6 MB synchronous, 256 KB asynchronous Account concurrency 1000 by default, raised via quotas A requirement that \"processing takes longer than 15 minutes\" automatically rules out Lambda: the answer moves to ECS/Fargate, AWS Batch, or Step Functions broken into steps. Invocation models Synchronous (API Gateway, ALB, direct invoke): the error is returned to the caller, retries are the caller's responsibility. Asynchronous (S3, SNS, EventBridge): two built in retries, then a destination or a dead letter queue. Poll based (SQS, Kinesis, DynamoDB Streams): Lambda polls the source itself, in batches; order is guaranteed for FIFO and for a stream shard. Error handling: a DLQ or Lambda Destinations (success and failure are routed to SQS, SNS, EventBridge, or another function). Handler idempotency is a requirement, not a nice to have. Concurrency Reserved concurrency reserves part of the account wide limit for a function while also capping it — a way to protect a database from overload and stop one function from eating the whole account limit; Provisioned concurrency keeps initialized environments ready — the answer to \"cold starts are unacceptable\", especially for Java and .NET; SnapStart dramatically speeds up Java startup by snapshotting an initialized environment; once the limit is exhausted, calls get throttled (429); asynchronous events are queued and retried. Function in a VPC By default, functions run outside your VPC and have internet access. Connecting to a VPC is needed to reach RDS, ElastiCache, or internal services. Once connected, internet access disappears: you need a NAT Gateway for public APIs or interface endpoints for AWS services. This is exactly the trap where \"the function can't reach an external API\" is solved with NAT, not by editing a security group. Layers, images, environment A layer is shared code and dependencies, up to 5 layers per function; handy for SDKs, client libraries, and the Secrets Manager/Parameter Store extension. A container image up to 10 GB suits large ML dependencies. Environment variables are encrypted with KMS; secrets belong in Secrets Manager, fetched through the caching extension rather than hardcoded. Versions and aliases give you a controlled rollout: a prod alias with weights routes a percentage of traffic to a new version (canary), and rollback is instant. Cost Charge = number of requests + GB seconds. More memory raises both the price per millisecond and the speed: a function with 1024 MB is often cheaper than one with 128 MB, because it finishes many times faster. AWS Lambda Power Tuning finds the optimum. Compute Savings Plans cover Lambda alongside EC2 and Fargate. API Gateway API type Protocol Features REST HTTP the most features: caching, usage plans, API keys, WAF, request validation HTTP HTTP cheaper and faster, JWT authorization, fewer features WebSocket WSS two way communication, chat, notifications Capabilities the exam tests: throttling and usage plans for partners, response caching to cut backend load, authorization via IAM, Cognito, or a Lambda authorizer, direct service integrations (e.g., putting a message on SQS with no function involved), private APIs via an interface endpoint, stages (dev, prod), and canary releases. A Lambda function URL is a direct HTTPS endpoint for a function, with or without IAM authorization. It's the right answer wherever you need a single call to a single function and don't need routing, throttling, keys, or transformations: API Gateway would be an unnecessary extra service there. Step Functions A state orchestrator: sequences, parallel branches, conditions, retries, timeouts, compensations. Standard workflow — up to a year, billed per transition, exactly once; Express — up to 5 minutes, billed by duration, for high frequency events. The answer to \"a complex multi step process with retries and branching\" and to \"processing takes longer than 15 minutes but the logic breaks into steps\". What the exam asks 1. \"Process an image on upload to S3\" — an S3 event plus Lambda. 2. \"A spike in requests breaks the database\" — reserved concurrency and RDS Proxy. 3. \"The first request after idle time is too slow\" — provisioned concurrency or SnapStart. 4. \"The function can't see the database in a VPC\" — connect it to the VPC and open the security group. 5. \"A function in a VPC lost internet access\" — a NAT Gateway or an endpoint. 6. \"Need a single HTTPS endpoint for one function, minimum services\" — a function URL. 7. \"The job runs for 40 minutes\" — Fargate, Batch, or Step Functions, not Lambda. 8. \"Messages must be processed in strict order\" — SQS FIFO with a handler whose concurrency is bounded by the message group. Numbers to remember 15 minutes max, 10,240 MB memory, up to 6 vCPUs at maximum memory; 50 MB zip package and 250 MB unzipped, image up to 10 GB; payload 6 MB synchronous and 256 KB asynchronous; /tmp from 512 MB up to 10 GB; 1000 concurrent executions by default per account and region; asynchronous invocations retry twice, events are kept for up to 6 hours; batch size up to 10,000 records for Kinesis and DynamoDB Streams, up to 10,000 for SQS in standard queue mode; API Gateway: 29 second integration timeout, 10 MB payload. How to read the wording In the question Answer \"no servers to manage, pay per request\" Lambda \"processing takes 30 minutes\" Fargate, Batch, or Step Functions \"cold start unacceptable\" provisioned concurrency, SnapStart \"protect the database from too many connections\" reserved concurrency plus RDS Proxy \"function cannot reach the internet\" NAT Gateway or an endpoint \"single function needs an HTTPS endpoint\" a function URL \"throttle partner APIs, issue API keys\" API Gateway usage plans \"coordinate multiple steps with retries\" Step Functions Observability and debugging Function logs go to CloudWatch Logs, and metrics like Invocations, Errors, Throttles, Duration, and ConcurrentExecutions go to CloudWatch. X Ray shows call traces across services and helps find where time is lost. Typical alarms: rising Throttles (hitting the concurrency limit), rising Errors alongside a DLQ (problem events), Duration near the timeout limit. For events that can't be lost, a DLQ or Destinations is mandatory: without one, an asynchronous event disappears after two retries. Mini practicum 1. A function processes files from a bucket; during a marketing campaign some events got lost. → An SQS queue in front of the function: the buffer absorbs the spike, nothing is lost. 2. Java functions take a long time to start, there's no hard latency requirement, and budget matters. → SnapStart: a snapshot of the initialized environment without paying for a standing reserve. 3. Employees log in simultaneously in the morning, and the first requests are slow. → Scheduled provisioned concurrency before the start of the workday. 4. A job runs for an hour and is written in a non standard language. → This isn't Lambda: the 15 minute execution limit means the job belongs on AWS Batch or containers. Bank audit {{q:239}} — a single microservice with AWS_IAM authorization: a Lambda function URL, API Gateway would add an unneeded service; dump key corrected. {{q:809}} — a Batch job completion event is caught by an EventBridge rule, not EventBridge Scheduler; calling an external HTTP API is done via an API destination. {{q:110}} — decoupling image upload from processing: a queue plus Lambda, key corrected during the CD audit. {{q:422}} — low confidence: SQS with Fargate vs. SQS with Lambda, the choice depends on the size of the ML models, which the text doesn't state directly. "},{"id":"integration","title":"Integration — SQS, SNS, EventBridge, Step Functions","order":11,"svc":["SQS","SNS","EventBridge","Step Functions","Amazon MQ"],"questions":[7,8,10,13,18,25,26,40,41,45,46,51,61,62,67,75,78,81,82,87,94,98,105,108,110,111,136,138,139,148,149,152,155,161,164,167,181,195,201,203,206,207,210,211,226,228,232,238,248,255,257,278,279,283,297,311,316,317,322,328,329,335,342,344,351,360,362,363,364,369,375,380,386,393,397,400,404,412,413,422,452,454,460,462,483,489,491,492,513,517,528,531,533,555,569,570,603,611,616,630,636,653,669,682,689,711,717,727,735,739,752,765,769,774,784,785,793,794,798,799,802,809,814,820,833,845,846,853,865,869,877,879,897,904,911,917,920,929,931,954,959,963,967,991,1001,1005,1012,1014],"html":"<h1>Application integration</h1>\n<p>Decoupling components is a theme that runs through the whole exam. Words like \"decouple\", \"loosely coupled\", \"buffer\", \"spikes\", \"producer must not wait\" almost always point to a queue or a bus, not to adding more instances.</p>\n<h2>SQS</h2>\n<p>A queue that holds messages for up to 14 days (4 days by default), with a message size up to 256 KB (larger payloads go through the extended client into S3). A consumer picks up a message, it becomes invisible for the visibility timeout, and is deleted once processing succeeds.</p>\n<div class='table-wrap'><table><thead><tr><th></th><th>Standard</th><th>FIFO</th></tr></thead><tbody><tr><td>Order</td><td>not guaranteed</td><td>strict within a message group</td></tr><tr><td>Delivery</td><td>at least once</td><td>exactly once</td></tr><tr><td>Throughput</td><td>practically unlimited</td><td>up to 3000 messages/sec with batching</td></tr><tr><td>Deduplication</td><td>none</td><td>by content hash or an explicit id</td></tr></tbody></table></div>\n<p>Mechanics the exam tests:</p>\n<ul><li>the visibility timeout must exceed the processing time, otherwise the message returns to the queue and gets processed twice — a common cause of duplicates;</li><li>long polling (<code>WaitTimeSeconds</code> up to 20) reduces empty responses and cost;</li><li>a dead-letter queue after N failed attempts isolates \"poison\" messages;</li><li>a delay queue and per-message timers postpone delivery;</li><li>SSE-KMS encryption and a queue policy handle cross-account access;</li><li>consumers scale off the <code>ApproximateNumberOfMessagesVisible</code> metric.</li></ul>\n<h2>SNS</h2>\n<p>Publish/subscribe: one message, many recipients. Subscribers can be SQS, Lambda, HTTP(S), email, SMS, mobile push, or Kinesis Data Firehose. A filter policy lets a subscriber receive only matching messages. FIFO topics pair with FIFO queues.</p>\n<p>Fan-out (SNS to several SQS queues) is the standard pattern when one event needs to reach several independent handlers, each with its own buffering and retries.</p>\n<h2>EventBridge</h2>\n<p>An event bus with content-based routing: rules match a JSON pattern and send the event to targets (Lambda, SQS, SNS, Step Functions, an API destination, and others). What sets it apart from SNS and drives the choice:</p>\n<ul><li>AWS service events land on the default bus automatically — you can react to an EC2 state change, a Batch job completing, or a Config change;</li><li>partner event buses accept SaaS events (Zendesk, Datadog, Shopify);</li><li>a schema registry, event archive, and replay;</li><li>EventBridge Scheduler triggers targets on a schedule or one-off — it's about time, not about matching events;</li><li>an API destination calls an external HTTP API using stored credentials.</li></ul>\n<p>Rule of thumb: need to broadcast your own message to many subscribers with minimal latency — SNS; need to route events (including system events) by content and keep a history — EventBridge.</p>\n<h2>Step Functions</h2>\n<p>A state machine for processes: steps, branching, parallel execution, Map for processing collections, built-in retries and error handling, and integration with hundreds of AWS APIs with no code. Standard is for long-running processes, exactly-once, billed per transition; Express is for short, frequent runs, billed by duration. The answer to \"orchestrate several Lambda functions with retries and rollbacks\" and to \"need visibility into which step failed\".</p>\n<h2>Amazon MQ</h2>\n<p>Managed ActiveMQ and RabbitMQ with the JMS, AMQP, MQTT, and STOMP protocols. The only reason to pick it over SQS/SNS is migrating an existing application that relies on standard messaging protocols and cannot be rewritten.</p>\n<h2>Kinesis vs. queues</h2>\n<p>Kinesis Data Streams is an ordered stream with replay and multiple independent consumers; data is retained for up to 365 days. With SQS, a message disappears once one consumer processes it. If the question mentions \"real-time analytics\", \"multiple consumers read the same data\", \"replay\" — it's Kinesis; if it says \"work queue\", \"task per message\", \"buffer between tiers\" — it's SQS.</p>\n<h2>Patterns that keep coming up in questions</h2>\n<div class='table-wrap'><table><thead><tr><th>Requirement</th><th>Solution</th></tr></thead><tbody><tr><td>Traffic spikes break the handler</td><td>SQS in front of the handler, autoscaling on queue depth</td></tr><tr><td>Three systems need to process one order</td><td>SNS fan-out to three SQS queues</td></tr><tr><td>Per-customer order matters</td><td>SQS FIFO with a message group per client</td></tr><tr><td>Long multi-step processing</td><td>Step Functions</td></tr><tr><td>React to infrastructure events</td><td>EventBridge rule</td></tr><tr><td>Migrating a JMS application to the cloud</td><td>Amazon MQ</td></tr><tr><td>Analytics on a clickstream</td><td>Kinesis Data Streams or Firehose</td></tr></tbody></table></div>\n<h2>What the exam asks</h2>\n<ol><li>\"Messages are lost when the handler crashes\" — a queue instead of a direct call.</li><li>\"Some messages are processed twice\" — visibility timeout, idempotency, FIFO if the requirement is strict.</li><li>\"The same message needs to reach four subscribers\" — SNS with SQS subscriptions.</li><li>\"Processing needs to be delayed by 15 minutes\" — a delay queue or EventBridge Scheduler.</li><li>\"Problem messages block the queue\" — a DLQ.</li><li>\"A message is 10 MB\" — S3 plus a reference in the message (extended client).</li><li>\"Need to call an external SaaS API on an event\" — EventBridge API destination.</li><li>\"Need visibility into steps and retries across services\" — Step Functions.</li></ol>\n<h2>Numbers to remember</h2>\n<ul><li>SQS: message up to 256 KB, retention from 60 seconds to 14 days (4 days by default), default visibility timeout 30 seconds, up to 12 hours, long polling up to 20 seconds;</li><li>FIFO: up to 3000 messages/sec with batching, deduplication window of 5 minutes;</li><li>SNS: up to 12.5 million subscriptions per topic, message up to 256 KB;</li><li>EventBridge: event up to 256 KB, archive and replay, up to 5 targets per rule;</li><li>Step Functions Standard: up to 1 year to run, Express: up to 5 minutes;</li><li>Kinesis Data Streams: 1 MB/s write and 2 MB/s read per shard, retention up to 365 days.</li></ul>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>In the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"decouple\", \"buffer spikes\"</td><td>SQS</td></tr><tr><td>\"fan out to multiple subscribers\"</td><td>SNS with SQS subscriptions</td></tr><tr><td>\"react to AWS service events\"</td><td>EventBridge rule</td></tr><tr><td>\"schedule a one-time or recurring task\"</td><td>EventBridge Scheduler</td></tr><tr><td>\"exactly once, strict order\"</td><td>SQS FIFO</td></tr><tr><td>\"multiple consumers read the same stream, replay\"</td><td>Kinesis Data Streams</td></tr><tr><td>\"existing app uses JMS/AMQP\"</td><td>Amazon MQ</td></tr><tr><td>\"visibility into a multi-step workflow\"</td><td>Step Functions</td></tr></tbody></table></div>\n<h2>Diagnosing common failures</h2>\n<div class='table-wrap'><table><thead><tr><th>Symptom</th><th>Cause</th><th>What to do</th></tr></thead><tbody><tr><td>Messages processed twice</td><td>processing takes longer than the visibility timeout</td><td>increase the timeout, make the handler idempotent</td></tr><tr><td>Queue keeps growing</td><td>not enough consumers</td><td>autoscale on queue depth, batch</td></tr><tr><td>One message blocks processing</td><td>a \"poison\" message</td><td>DLQ with maxReceiveCount</td></tr><tr><td>Subscriber gets extra messages</td><td>no filter</td><td>filter policy on the SNS subscription</td></tr><tr><td>Events get lost</td><td>asynchronous call with no DLQ</td><td>Lambda Destinations or a DLQ</td></tr><tr><td>Order is broken</td><td>standard queue</td><td>FIFO with a message group id</td></tr></tbody></table></div>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> A web app calls an image handler synchronously; during traffic spikes users get timeouts. → Put the job on SQS and process it asynchronously, scaling consumers off queue depth.</p>\n<p><strong>2.</strong> An \"order paid\" event needs to reach billing, warehouse, and analytics. → An SNS topic with three SQS subscriptions: each consumer gets its own queue, retries, and DLQ.</p>\n<p><strong>3.</strong> Some messages are processed twice. → Check the visibility timeout against processing time and make the handler idempotent; if the requirement is strict, use a FIFO queue.</p>\n<p><strong>4.</strong> Need to run an export every day at 3 AM and react to an AWS Batch job finishing. → The schedule is EventBridge Scheduler; reacting to the event is an EventBridge rule matching the Batch event pattern.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"110\" href=\"#\">questions 110</a> — decoupling image upload from processing via a queue; key corrected.</li><li><a class=\"qref\" data-q=\"931\" href=\"#\">questions 931</a> — cross-account Lambda subscription to SNS: a function policy plus a topic policy; an SQS buffer in the dump changed the calling source.</li><li><a class=\"qref\" data-q=\"809\" href=\"#\">questions 809</a> — EventBridge rule vs. EventBridge Scheduler: a rule matches an event, a scheduler runs on time.</li><li><a class=\"qref\" data-q=\"390\" href=\"#\">questions 390</a> — disputed: session state storage, sticky sessions don't store data.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>34</td><td>23%</td><td>11</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>57</td><td>39%</td><td>37</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>33</td><td>22%</td><td>24</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>24</td><td>16%</td><td>4</td></tr></tbody></table></div><p>Total questions in this chapter: <b>148</b>. With a written walkthrough: <b>148</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#10</b> — RES: guaranteeing order processing sequence</li><li><b>#25</b> — RES: buffer between API and database instead of scaling quotas</li><li><b>#45</b> — RES: retries and a dead-letter queue</li><li><b>#51</b> — RES: event-driven report generation and distribution</li><li><b>#67</b> — RES: reprocessing queue messages</li><li><b>#75</b> — RES: transactions lost under tier overload</li><li><b>#87</b> — RES: avoiding data loss during upgrade</li><li><b>#94</b> — PERF: elastic processing under variable load</li></ul>","text":" Application integration Decoupling components is a theme that runs through the whole exam. Words like \"decouple\", \"loosely coupled\", \"buffer\", \"spikes\", \"producer must not wait\" almost always point to a queue or a bus, not to adding more instances. SQS A queue that holds messages for up to 14 days (4 days by default), with a message size up to 256 KB (larger payloads go through the extended client into S3). A consumer picks up a message, it becomes invisible for the visibility timeout, and is deleted once processing succeeds. Standard FIFO Order not guaranteed strict within a message group Delivery at least once exactly once Throughput practically unlimited up to 3000 messages/sec with batching Deduplication none by content hash or an explicit id Mechanics the exam tests: the visibility timeout must exceed the processing time, otherwise the message returns to the queue and gets processed twice — a common cause of duplicates; long polling ( WaitTimeSeconds up to 20) reduces empty responses and cost; a dead letter queue after N failed attempts isolates \"poison\" messages; a delay queue and per message timers postpone delivery; SSE KMS encryption and a queue policy handle cross account access; consumers scale off the ApproximateNumberOfMessagesVisible metric. SNS Publish/subscribe: one message, many recipients. Subscribers can be SQS, Lambda, HTTP(S), email, SMS, mobile push, or Kinesis Data Firehose. A filter policy lets a subscriber receive only matching messages. FIFO topics pair with FIFO queues. Fan out (SNS to several SQS queues) is the standard pattern when one event needs to reach several independent handlers, each with its own buffering and retries. EventBridge An event bus with content based routing: rules match a JSON pattern and send the event to targets (Lambda, SQS, SNS, Step Functions, an API destination, and others). What sets it apart from SNS and drives the choice: AWS service events land on the default bus automatically — you can react to an EC2 state change, a Batch job completing, or a Config change; partner event buses accept SaaS events (Zendesk, Datadog, Shopify); a schema registry, event archive, and replay; EventBridge Scheduler triggers targets on a schedule or one off — it's about time, not about matching events; an API destination calls an external HTTP API using stored credentials. Rule of thumb: need to broadcast your own message to many subscribers with minimal latency — SNS; need to route events (including system events) by content and keep a history — EventBridge. Step Functions A state machine for processes: steps, branching, parallel execution, Map for processing collections, built in retries and error handling, and integration with hundreds of AWS APIs with no code. Standard is for long running processes, exactly once, billed per transition; Express is for short, frequent runs, billed by duration. The answer to \"orchestrate several Lambda functions with retries and rollbacks\" and to \"need visibility into which step failed\". Amazon MQ Managed ActiveMQ and RabbitMQ with the JMS, AMQP, MQTT, and STOMP protocols. The only reason to pick it over SQS/SNS is migrating an existing application that relies on standard messaging protocols and cannot be rewritten. Kinesis vs. queues Kinesis Data Streams is an ordered stream with replay and multiple independent consumers; data is retained for up to 365 days. With SQS, a message disappears once one consumer processes it. If the question mentions \"real time analytics\", \"multiple consumers read the same data\", \"replay\" — it's Kinesis; if it says \"work queue\", \"task per message\", \"buffer between tiers\" — it's SQS. Patterns that keep coming up in questions Requirement Solution Traffic spikes break the handler SQS in front of the handler, autoscaling on queue depth Three systems need to process one order SNS fan out to three SQS queues Per customer order matters SQS FIFO with a message group per client Long multi step processing Step Functions React to infrastructure events EventBridge rule Migrating a JMS application to the cloud Amazon MQ Analytics on a clickstream Kinesis Data Streams or Firehose What the exam asks 1. \"Messages are lost when the handler crashes\" — a queue instead of a direct call. 2. \"Some messages are processed twice\" — visibility timeout, idempotency, FIFO if the requirement is strict. 3. \"The same message needs to reach four subscribers\" — SNS with SQS subscriptions. 4. \"Processing needs to be delayed by 15 minutes\" — a delay queue or EventBridge Scheduler. 5. \"Problem messages block the queue\" — a DLQ. 6. \"A message is 10 MB\" — S3 plus a reference in the message (extended client). 7. \"Need to call an external SaaS API on an event\" — EventBridge API destination. 8. \"Need visibility into steps and retries across services\" — Step Functions. Numbers to remember SQS: message up to 256 KB, retention from 60 seconds to 14 days (4 days by default), default visibility timeout 30 seconds, up to 12 hours, long polling up to 20 seconds; FIFO: up to 3000 messages/sec with batching, deduplication window of 5 minutes; SNS: up to 12.5 million subscriptions per topic, message up to 256 KB; EventBridge: event up to 256 KB, archive and replay, up to 5 targets per rule; Step Functions Standard: up to 1 year to run, Express: up to 5 minutes; Kinesis Data Streams: 1 MB/s write and 2 MB/s read per shard, retention up to 365 days. How to read the wording In the question Answer \"decouple\", \"buffer spikes\" SQS \"fan out to multiple subscribers\" SNS with SQS subscriptions \"react to AWS service events\" EventBridge rule \"schedule a one time or recurring task\" EventBridge Scheduler \"exactly once, strict order\" SQS FIFO \"multiple consumers read the same stream, replay\" Kinesis Data Streams \"existing app uses JMS/AMQP\" Amazon MQ \"visibility into a multi step workflow\" Step Functions Diagnosing common failures Symptom Cause What to do Messages processed twice processing takes longer than the visibility timeout increase the timeout, make the handler idempotent Queue keeps growing not enough consumers autoscale on queue depth, batch One message blocks processing a \"poison\" message DLQ with maxReceiveCount Subscriber gets extra messages no filter filter policy on the SNS subscription Events get lost asynchronous call with no DLQ Lambda Destinations or a DLQ Order is broken standard queue FIFO with a message group id Mini practicum 1. A web app calls an image handler synchronously; during traffic spikes users get timeouts. → Put the job on SQS and process it asynchronously, scaling consumers off queue depth. 2. An \"order paid\" event needs to reach billing, warehouse, and analytics. → An SNS topic with three SQS subscriptions: each consumer gets its own queue, retries, and DLQ. 3. Some messages are processed twice. → Check the visibility timeout against processing time and make the handler idempotent; if the requirement is strict, use a FIFO queue. 4. Need to run an export every day at 3 AM and react to an AWS Batch job finishing. → The schedule is EventBridge Scheduler; reacting to the event is an EventBridge rule matching the Batch event pattern. Bank audit {{q:110}} — decoupling image upload from processing via a queue; key corrected. {{q:931}} — cross account Lambda subscription to SNS: a function policy plus a topic policy; an SQS buffer in the dump changed the calling source. {{q:809}} — EventBridge rule vs. EventBridge Scheduler: a rule matches an event, a scheduler runs on time. {{q:390}} — disputed: session state storage, sticky sessions don't store data. "},{"id":"file-storage","title":"File storage: EFS and FSx","order":12,"svc":["EFS","FSx"],"questions":[5,9,48,52,54,63,64,85,97,99,102,118,127,162,186,221,243,249,252,260,277,283,287,288,299,301,305,332,336,344,346,357,384,407,409,421,475,487,496,500,502,506,546,564,566,567,580,587,598,617,618,622,632,635,646,648,656,658,673,680,691,698,699,702,717,719,747,753,760,767,779,795,800,806,822,837,842,844,852,861,895,934,938,944,972,986,990,994,1003,1014],"html":"<h1>File storage</h1>\n<p>When several servers need the same mutable set of files with ordinary file semantics, S3 doesn't fit — you need network file access. The choice comes down to protocol and to whose stack it is: Linux, Windows, or something specialized.</p>\n<h2>EFS</h2>\n<p>An elastic NFS v4.1 file system for Linux. It grows and shrinks on its own, mounts simultaneously from thousands of clients across multiple AZs, and bills for the storage actually used.</p>\n<ul><li>availability modes: Regional (multiple AZs, the standard for fault tolerance) and One Zone (cheaper, but dependent on a single zone);</li><li>storage classes: Standard and Infrequent Access, moved between by a lifecycle policy, plus Archive for rarely touched data;</li><li>performance modes: General Purpose (low latency, the default) and Max I/O (higher throughput, higher latency);</li><li>throughput modes: Bursting, Provisioned, and Elastic;</li><li>access is mounted through a mount target in each AZ; the mount target's security group must allow port 2049 from clients;</li><li>Access Points define an entry point with a fixed user and directory — handy for multi-tenant applications and for Lambda;</li><li>encryption at rest (KMS) and in transit (TLS), a file system policy for access control, IAM integration;</li><li>an EFS replica in another Region is made through EFS replication or via AWS Backup.</li></ul>\n<p>When EFS is the right answer: a content directory shared by multiple web servers, home directories, shared data for ECS/EKS containers, file processing where the application only knows file access.</p>\n<h2>FSx: four different services</h2>\n<div class='table-wrap'><table><thead><tr><th>Option</th><th>Protocol</th><th>Profile</th><th>Typical scenario</th></tr></thead><tbody><tr><td>FSx for Windows File Server</td><td>SMB</td><td>Active Directory integration, NTFS ACLs, DFS</td><td>Windows file shares, migration from file servers</td></tr><tr><td>FSx for Lustre</td><td>Lustre (POSIX)</td><td>hundreds of GB/s, ties into S3</td><td>HPC, ML training, large dataset processing</td></tr><tr><td>FSx for NetApp ONTAP</td><td>NFS, SMB, iSCSI</td><td>snapshots, deduplication, SnapMirror</td><td>NetApp migration, multi-protocol access</td></tr><tr><td>FSx for OpenZFS</td><td>NFS</td><td>ZFS snapshots, low latency</td><td>migration from ZFS servers, dev environments</td></tr></tbody></table></div>\n<p>Key cues in the question text: \"Windows applications,\" \"SMB,\" \"Active Directory\" — FSx for Windows; \"high-performance computing,\" \"machine learning training,\" \"process data from S3 at high speed\" — FSx for Lustre; \"NetApp,\" \"multi-protocol,\" \"SnapMirror\" — ONTAP; \"ZFS snapshots\" — OpenZFS.</p>\n<p>FSx for Lustre can link to an S3 bucket: objects appear as files, and results export back out. That's the standard answer to \"data lives in S3, but the compute cluster needs fast POSIX access.\"</p>\n<h2>Comparison with other storage types</h2>\n<div class='table-wrap'><table><thead><tr><th>Trait</th><th>S3</th><th>EFS</th><th>FSx Windows</th><th>EBS</th></tr></thead><tbody><tr><td>Access</td><td>HTTP API</td><td>NFS</td><td>SMB</td><td>block device</td></tr><tr><td>Shared across many servers at once</td><td>yes</td><td>yes</td><td>yes</td><td>only io1/io2 Multi-Attach in one AZ</td></tr><tr><td>Across AZs</td><td>yes</td><td>yes (Regional)</td><td>yes (Multi-AZ)</td><td>no</td></tr><tr><td>Partial overwrite</td><td>no</td><td>yes</td><td>yes</td><td>yes</td></tr><tr><td>Price</td><td>lowest</td><td>medium</td><td>medium and up</td><td>depends on volume type</td></tr></tbody></table></div>\n<h2>Backup</h2>\n<p>EFS and FSx integrate with AWS Backup: schedules, retention periods, copies to another Region and account, Vault Lock for immutable copies. For EFS, an \"EFS-to-EFS backup\" CloudFormation solution used to be recommended — that's a legacy template, not a service, and it's the wrong answer in current questions.</p>\n<h2>Performance and cost</h2>\n<ul><li>EFS is billed for storage and, in Provisioned mode, for throughput; moving cold files to IA cuts the bill substantially;</li><li>One Zone options are roughly half the price but don't survive a zone failure;</li><li>FSx is billed for provisioned capacity, throughput, and backups;</li><li>Lustre in scratch mode is cheaper and isn't replicated — good for temporary computation; persistent is for long-lived data.</li></ul>\n<h2>What the exam asks</h2>\n<ol><li>\"Several EC2 instances in different AZs write to the same directory\" — EFS.</li><li>\"A Windows application requires SMB and domain ACLs\" — FSx for Windows File Server.</li><li>\"Model training reads terabytes from S3 at maximum speed\" — FSx for Lustre with a data repository association.</li><li>\"An application cluster in one AZ needs a shared volume with maximum IOPS\" — io2 Multi-Attach, or instance store if losing the data is acceptable.</li><li>\"Migrate an on-premises NetApp system while keeping snapshots\" — FSx for NetApp ONTAP.</li><li>\"Lower the cost of shared storage where 80 percent of files haven't been touched in months\" — EFS lifecycle into Infrequent Access.</li><li>\"Replicate the file system to another Region\" — EFS replication or AWS Backup.</li><li>\"Files only need to be accessed as objects through an API\" — that's S3, no file service needed.</li></ol>\n<h2>Numbers to remember</h2>\n<ul><li>EFS: NFS v4.1, port 2049, up to thousands of clients, no fixed file system size;</li><li>EFS IA is roughly 10x cheaper than Standard, with a per-access charge;</li><li>EFS One Zone is roughly half the price of Regional, but lives in a single AZ;</li><li>FSx for Windows: SMB, AD integration, Multi-AZ as a separate option at creation;</li><li>FSx for Lustre: throughput in the hundreds of GB/s, scratch (no replication) and persistent (replicated) file systems;</li><li>FSx for Lustre links to an S3 bucket and imports object metadata as files.</li></ul>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>Wording in the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"shared file system for Linux instances across AZs\"</td><td>EFS</td></tr><tr><td>\"SMB shares, Active Directory, NTFS ACLs\"</td><td>FSx for Windows</td></tr><tr><td>\"HPC, machine learning training, sub-millisecond, from S3\"</td><td>FSx for Lustre</td></tr><tr><td>\"NetApp, SnapMirror, multi-protocol\"</td><td>FSx for NetApp ONTAP</td></tr><tr><td>\"ZFS snapshots\"</td><td>FSx for OpenZFS</td></tr><tr><td>\"lower cost for files not accessed in months\"</td><td>EFS lifecycle into IA</td></tr><tr><td>\"object storage over HTTP\"</td><td>S3, no file service needed</td></tr><tr><td>\"single instance needs a fast local disk\"</td><td>EBS io2 or instance store</td></tr></tbody></table></div>\n<h2>Mounting and permissions</h2>\n<p>EFS is mounted via the file system's DNS name; for Lambda and containers, an Access Point with a fixed UID and root directory is used instead. Access is restricted by two layers: the security group on the mount target (port 2049 from client groups) and the file system policy with the actions <code>elasticfilesystem:ClientMount</code> and <code>ClientWrite</code>. For FSx for Windows, permissions are governed by domain ACLs, and the AD service account must have rights to join the domain — a detail that comes up in questions about migrating file servers.</p>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> A dozen web servers across three AZs need to serve the same directory of user-uploaded files. → EFS Regional: it mounts in every zone and grows automatically.</p>\n<p><strong>2.</strong> A Windows application requires an SMB share with domain-group permissions. → FSx for Windows File Server integrated with Active Directory.</p>\n<p><strong>3.</strong> A model-training cluster reads 20 TB from S3 and is bottlenecked on speed. → FSx for Lustre linked to the bucket: objects appear as files, throughput in the hundreds of GB/s.</p>\n<p><strong>4.</strong> 80 percent of the files in EFS haven't been opened in six months, and the bill keeps growing. → An EFS lifecycle policy into Infrequent Access, and Archive if needed.</p>\n<h2>Related chapters</h2>\n<ul><li>Object storage and choosing between file and object — the S3 chapter.</li><li>Block volumes, when access is needed from a single instance — the EBS chapter.</li><li>Migrating file shares to the cloud and hybrid access — the data migration chapter.</li><li>Mounting shared volumes into tasks and pods — the containers chapter.</li><li>Centralized backups for EFS and FSx — the governance chapter.</li></ul>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"842\" href=\"#\">questions 842</a> — cross-Region replication of EFS: done with AWS Backup, and the \"EFS-to-EFS backup\" solution is legacy; the dump key was corrected.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>10</td><td>11%</td><td>1</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>27</td><td>30%</td><td>6</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>36</td><td>40%</td><td>28</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>17</td><td>19%</td><td>1</td></tr></tbody></table></div><p>Total questions in this chapter: <b>90</b>. With a written walkthrough: <b>90</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#5</b> — RES: shared access to documents from both instances</li><li><b>#85</b> — SEC: document immutability for regulatory compliance</li><li><b>#99</b> — PERF: file storage with Lustre clients</li><li><b>#102</b> — PERF: automating data migration to EFS</li><li><b>#186</b> — PERF: shared Windows file system across zones</li><li><b>#249</b> — PERF: file storage with SMB clients</li><li><b>#252</b> — RES: shared file access with built-in redundancy</li><li><b>#283</b> — PERF: single dataset for NFS and SMB</li></ul>","text":" File storage When several servers need the same mutable set of files with ordinary file semantics, S3 doesn't fit — you need network file access. The choice comes down to protocol and to whose stack it is: Linux, Windows, or something specialized. EFS An elastic NFS v4.1 file system for Linux. It grows and shrinks on its own, mounts simultaneously from thousands of clients across multiple AZs, and bills for the storage actually used. availability modes: Regional (multiple AZs, the standard for fault tolerance) and One Zone (cheaper, but dependent on a single zone); storage classes: Standard and Infrequent Access, moved between by a lifecycle policy, plus Archive for rarely touched data; performance modes: General Purpose (low latency, the default) and Max I/O (higher throughput, higher latency); throughput modes: Bursting, Provisioned, and Elastic; access is mounted through a mount target in each AZ; the mount target's security group must allow port 2049 from clients; Access Points define an entry point with a fixed user and directory — handy for multi tenant applications and for Lambda; encryption at rest (KMS) and in transit (TLS), a file system policy for access control, IAM integration; an EFS replica in another Region is made through EFS replication or via AWS Backup. When EFS is the right answer: a content directory shared by multiple web servers, home directories, shared data for ECS/EKS containers, file processing where the application only knows file access. FSx: four different services Option Protocol Profile Typical scenario FSx for Windows File Server SMB Active Directory integration, NTFS ACLs, DFS Windows file shares, migration from file servers FSx for Lustre Lustre (POSIX) hundreds of GB/s, ties into S3 HPC, ML training, large dataset processing FSx for NetApp ONTAP NFS, SMB, iSCSI snapshots, deduplication, SnapMirror NetApp migration, multi protocol access FSx for OpenZFS NFS ZFS snapshots, low latency migration from ZFS servers, dev environments Key cues in the question text: \"Windows applications,\" \"SMB,\" \"Active Directory\" — FSx for Windows; \"high performance computing,\" \"machine learning training,\" \"process data from S3 at high speed\" — FSx for Lustre; \"NetApp,\" \"multi protocol,\" \"SnapMirror\" — ONTAP; \"ZFS snapshots\" — OpenZFS. FSx for Lustre can link to an S3 bucket: objects appear as files, and results export back out. That's the standard answer to \"data lives in S3, but the compute cluster needs fast POSIX access.\" Comparison with other storage types Trait S3 EFS FSx Windows EBS Access HTTP API NFS SMB block device Shared across many servers at once yes yes yes only io1/io2 Multi Attach in one AZ Across AZs yes yes (Regional) yes (Multi AZ) no Partial overwrite no yes yes yes Price lowest medium medium and up depends on volume type Backup EFS and FSx integrate with AWS Backup: schedules, retention periods, copies to another Region and account, Vault Lock for immutable copies. For EFS, an \"EFS to EFS backup\" CloudFormation solution used to be recommended — that's a legacy template, not a service, and it's the wrong answer in current questions. Performance and cost EFS is billed for storage and, in Provisioned mode, for throughput; moving cold files to IA cuts the bill substantially; One Zone options are roughly half the price but don't survive a zone failure; FSx is billed for provisioned capacity, throughput, and backups; Lustre in scratch mode is cheaper and isn't replicated — good for temporary computation; persistent is for long lived data. What the exam asks 1. \"Several EC2 instances in different AZs write to the same directory\" — EFS. 2. \"A Windows application requires SMB and domain ACLs\" — FSx for Windows File Server. 3. \"Model training reads terabytes from S3 at maximum speed\" — FSx for Lustre with a data repository association. 4. \"An application cluster in one AZ needs a shared volume with maximum IOPS\" — io2 Multi Attach, or instance store if losing the data is acceptable. 5. \"Migrate an on premises NetApp system while keeping snapshots\" — FSx for NetApp ONTAP. 6. \"Lower the cost of shared storage where 80 percent of files haven't been touched in months\" — EFS lifecycle into Infrequent Access. 7. \"Replicate the file system to another Region\" — EFS replication or AWS Backup. 8. \"Files only need to be accessed as objects through an API\" — that's S3, no file service needed. Numbers to remember EFS: NFS v4.1, port 2049, up to thousands of clients, no fixed file system size; EFS IA is roughly 10x cheaper than Standard, with a per access charge; EFS One Zone is roughly half the price of Regional, but lives in a single AZ; FSx for Windows: SMB, AD integration, Multi AZ as a separate option at creation; FSx for Lustre: throughput in the hundreds of GB/s, scratch (no replication) and persistent (replicated) file systems; FSx for Lustre links to an S3 bucket and imports object metadata as files. How to read the wording Wording in the question Answer \"shared file system for Linux instances across AZs\" EFS \"SMB shares, Active Directory, NTFS ACLs\" FSx for Windows \"HPC, machine learning training, sub millisecond, from S3\" FSx for Lustre \"NetApp, SnapMirror, multi protocol\" FSx for NetApp ONTAP \"ZFS snapshots\" FSx for OpenZFS \"lower cost for files not accessed in months\" EFS lifecycle into IA \"object storage over HTTP\" S3, no file service needed \"single instance needs a fast local disk\" EBS io2 or instance store Mounting and permissions EFS is mounted via the file system's DNS name; for Lambda and containers, an Access Point with a fixed UID and root directory is used instead. Access is restricted by two layers: the security group on the mount target (port 2049 from client groups) and the file system policy with the actions elasticfilesystem:ClientMount and ClientWrite . For FSx for Windows, permissions are governed by domain ACLs, and the AD service account must have rights to join the domain — a detail that comes up in questions about migrating file servers. Mini practicum 1. A dozen web servers across three AZs need to serve the same directory of user uploaded files. → EFS Regional: it mounts in every zone and grows automatically. 2. A Windows application requires an SMB share with domain group permissions. → FSx for Windows File Server integrated with Active Directory. 3. A model training cluster reads 20 TB from S3 and is bottlenecked on speed. → FSx for Lustre linked to the bucket: objects appear as files, throughput in the hundreds of GB/s. 4. 80 percent of the files in EFS haven't been opened in six months, and the bill keeps growing. → An EFS lifecycle policy into Infrequent Access, and Archive if needed. Related chapters Object storage and choosing between file and object — the S3 chapter. Block volumes, when access is needed from a single instance — the EBS chapter. Migrating file shares to the cloud and hybrid access — the data migration chapter. Mounting shared volumes into tasks and pods — the containers chapter. Centralized backups for EFS and FSx — the governance chapter. Bank audit {{q:842}} — cross Region replication of EFS: done with AWS Backup, and the \"EFS to EFS backup\" solution is legacy; the dump key was corrected. "},{"id":"ebs","title":"EBS — block volumes and snapshots","order":13,"svc":["EBS"],"questions":[1,5,11,20,52,63,71,80,100,102,114,118,121,127,161,162,178,186,221,234,252,277,287,288,312,320,324,335,353,357,371,382,391,394,401,410,419,421,425,456,465,487,502,528,535,564,566,568,580,602,605,607,620,632,649,656,662,675,681,682,689,691,694,698,699,746,753,757,760,762,837,838,841,852,867,887,906,919,937,938,972,986,990,998,1014],"html":"<h1>EBS</h1>\n<p>A network block volume attached to an instance in the same Availability Zone. A volume is a disk: file system, database, log. Data is replicated within the AZ, so a zone failure means the volume becomes unavailable; protection against that comes from snapshots and application-level replication.</p>\n<h2>Volume types</h2>\n<div class='table-wrap'><table><thead><tr><th>Type</th><th>Class</th><th>IOPS</th><th>Throughput</th><th>Purpose</th></tr></thead><tbody><tr><td>gp3</td><td>SSD</td><td>3000 baseline, up to 16,000</td><td>125 MB/s baseline, up to 1000</td><td>general-purpose default choice</td></tr><tr><td>gp2</td><td>SSD</td><td>3 IOPS per GB, up to 16,000</td><td>up to 250 MB/s</td><td>previous generation</td></tr><tr><td>io2 / io2 Block Express</td><td>SSD</td><td>up to 256,000</td><td>up to 4000 MB/s</td><td>critical databases, maximum durability</td></tr><tr><td>io1</td><td>SSD</td><td>up to 64,000</td><td>up to 1000 MB/s</td><td>previous-generation provisioned IOPS</td></tr><tr><td>st1</td><td>HDD</td><td>up to 500</td><td>up to 500 MB/s</td><td>large sequential workloads, logs, big data</td></tr><tr><td>sc1</td><td>HDD</td><td>up to 250</td><td>up to 250 MB/s</td><td>cold data, lowest cost</td></tr></tbody></table></div>\n<p>Practical takeaways:</p>\n<ul><li>gp3 lets you change IOPS and throughput independently of size, so \"need more IOPS but not more space\" is solved by moving from gp2 to gp3, not by inflating the volume;</li><li>HDD types can't be used as boot volumes and are poor for random access;</li><li>io2 Block Express delivers the highest numbers and 99.999% durability;</li><li>Multi-Attach (io1/io2) attaches a volume to several instances in the same AZ, but requires a cluster-aware file system — it is not a replacement for EFS.</li></ul>\n<h2>Modification and maintenance</h2>\n<p>Elastic Volumes change size, type, and performance parameters on the fly, without stopping the instance; you cannot shrink a volume. After expanding a volume you still need to extend the file system inside the OS — a step answer choices love to mention.</p>\n<p>Initializing a volume from a snapshot: blocks are pulled from S3 on first access, which causes a slowdown; Fast Snapshot Restore removes that latency for an extra fee.</p>\n<h2>Snapshots</h2>\n<ul><li>incremental, stored in S3 (in AWS's internal storage), billed for the blocks actually changed;</li><li>copied across Regions and accounts, and can be re-encrypted during the copy;</li><li>Data Lifecycle Manager or AWS Backup create them on a schedule and delete them by policy;</li><li>Recycle Bin protects against accidental snapshot deletion;</li><li>snapshots can be archived to the Archive tier for long-term storage at low cost (restore takes hours).</li></ul>\n<p>Snapshots are the primary mechanism for migrating a volume between AZs and Regions: create a snapshot, create a volume in the target zone, or copy it to the target Region.</p>\n<h2>Encryption</h2>\n<p>Encryption is enabled at volume creation and propagates to snapshots and volumes created from them. An unencrypted volume can't be encrypted in place: you take a snapshot, copy it while specifying a key, and create a new volume from the copy. The account setting \"encryption by default\" guarantees that all new volumes and snapshots in a Region are encrypted — this is the answer to a requirement that \"unencrypted volumes must not be created.\"</p>\n<h2>EBS versus instance store</h2>\n<div class='table-wrap'><table><thead><tr><th></th><th>EBS</th><th>Instance store</th></tr></thead><tbody><tr><td>Data on stop/terminate</td><td>persists (except root by default)</td><td>lost</td></tr><tr><td>Host failure</td><td>volume intact</td><td>data lost</td></tr><tr><td>Performance</td><td>high, depends on type</td><td>maximum, local NVMe</td></tr><tr><td>Snapshots</td><td>yes</td><td>no</td></tr><tr><td>Price</td><td>per GB and IOPS</td><td>included in instance price</td></tr></tbody></table></div>\n<p>Instance store is chosen for caching, temporary data, buffers, and clusters replicated at the application level (Cassandra, Kafka), where losing a node is acceptable.</p>\n<h2>Monitoring and diagnostics</h2>\n<p>CloudWatch metrics: VolumeReadOps, VolumeWriteOps, VolumeQueueLength, BurstBalance for gp2 and HDD types. Exhausted BurstBalance is the direct cause of \"disks suddenly slow down after a few hours of operation,\" fixed by switching to gp3 or io2. A throughput ceiling can also sit on the instance side: every instance type has an EBS-bandwidth limit, so \"we increased the volume's IOPS but it didn't get any faster\" is solved by switching to an EBS-optimized instance type with a higher limit.</p>\n<h2>What the exam asks</h2>\n<ol><li>\"Need maximum, predictable IOPS for a database\" — io2 (Block Express).</li><li>\"Cheaply store large sequential logs\" — st1.</li><li>\"Not enough IOPS, but plenty of space\" — gp3 with provisioned IOPS.</li><li>\"Move a volume to another AZ\" — snapshot, then create a volume in the target zone.</li><li>\"Existing volumes need to be encrypted\" — snapshot, copy with a key, new volume.</li><li>\"Data must survive instance termination\" — EBS with <code>DeleteOnTermination=false</code>.</li><li>\"Shared file access from multiple instances across AZs\" — not EBS, but EFS.</li><li>\"Automatic daily disk backups retained for 30 days\" — Data Lifecycle Manager or AWS Backup.</li></ol>\n<h2>Numbers to remember</h2>\n<ul><li>gp3: 3000 IOPS and 125 MB/s baseline regardless of size, maximum 16,000 IOPS and 1000 MB/s;</li><li>gp2: 3 IOPS per GB, burst up to 3000 for volumes under 1 TB;</li><li>io2 Block Express: up to 256,000 IOPS, 4000 MB/s, ratio up to 1000 IOPS per GB;</li><li>st1 and sc1 start at 125 GB, optimized for sequential access;</li><li>a volume lives in a single AZ; migration only happens through a snapshot;</li><li>snapshots are incremental, the first one contains all occupied blocks;</li><li>Multi-Attach: up to 16 instances in one AZ, io1 and io2 only.</li></ul>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>Wording in the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"highest and most consistent IOPS for a database\"</td><td>io2 Block Express</td></tr><tr><td>\"throughput for large sequential logs at low cost\"</td><td>st1</td></tr><tr><td>\"need more IOPS but not more capacity\"</td><td>gp3</td></tr><tr><td>\"move the volume to another AZ\"</td><td>snapshot, then create a volume in the target zone</td></tr><tr><td>\"all new volumes must be encrypted\"</td><td>encryption by default in account settings</td></tr><tr><td>\"data must survive instance termination\"</td><td>disable DeleteOnTermination</td></tr><tr><td>\"shared access from many instances across AZs\"</td><td>EFS, not EBS</td></tr><tr><td>\"temporary scratch space with maximum speed\"</td><td>instance store</td></tr></tbody></table></div>\n<h2>Operations checklist</h2>\n<p>Expanding a volume: resize it, then extend the partition and file system inside the OS. Changing type: Elastic Volumes on the fly, the optimizing state can last hours, but the volume stays available. Moving between accounts: share the snapshot (for an encrypted one, also share the KMS key), copy the snapshot into your own account, create the volume. Regular backups: a Data Lifecycle Manager policy or an AWS Backup plan with copying to another Region for DR.</p>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> A 200 GB gp2 database is capped at 600 IOPS, with plenty of space left. → Switch to gp3 and set the required IOPS independently of size.</p>\n<p><strong>2.</strong> An existing unencrypted volume needs to be encrypted. → Snapshot, copy the snapshot specifying a KMS key, create a new volume, and swap it in.</p>\n<p><strong>3.</strong> An instance was restarted and its temporary data disappeared. → The data was on instance store; use an EBS volume for durability.</p>\n<p><strong>4.</strong> You need to guarantee that unencrypted volumes can no longer be created in the Region. → Enable encryption by default in the account's and Region's EC2 settings.</p>\n<h2>Related chapters</h2>\n<ul><li>Choosing an instance type and its EBS throughput limits — the EC2 chapter.</li><li>Shared file access from multiple servers — the EFS and FSx chapter.</li><li>Encrypting volumes and snapshots with KMS keys — the encryption chapter.</li><li>Backup policies and Vault Lock — the governance chapter.</li><li>Volume and snapshot cost, and right-sizing — the cost chapter.</li></ul>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"887\" href=\"#\">questions 887</a> — preventing unencrypted volume creation is done with an account setting, not an AWS Config detective rule; the dump key was corrected.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>16</td><td>19%</td><td>1</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>32</td><td>38%</td><td>8</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>20</td><td>24%</td><td>13</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>17</td><td>20%</td><td>2</td></tr></tbody></table></div><p>Total questions in this chapter: <b>85</b>. With a written walkthrough: <b>85</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#1</b> — PERF: speed of aggregating data into a single bucket</li><li><b>#5</b> — RES: shared access to documents from both instances</li><li><b>#20</b> — PERF: fast clone with high I/O for testing</li><li><b>#102</b> — PERF: automating data migration to EFS</li><li><b>#186</b> — PERF: shared Windows file system across zones</li><li><b>#252</b> — RES: shared file access with built-in redundancy</li><li><b>#288</b> — PERF: shared file access without application changes</li><li><b>#312</b> — RES: nightly backups and recovery in another region</li></ul>","text":" EBS A network block volume attached to an instance in the same Availability Zone. A volume is a disk: file system, database, log. Data is replicated within the AZ, so a zone failure means the volume becomes unavailable; protection against that comes from snapshots and application level replication. Volume types Type Class IOPS Throughput Purpose gp3 SSD 3000 baseline, up to 16,000 125 MB/s baseline, up to 1000 general purpose default choice gp2 SSD 3 IOPS per GB, up to 16,000 up to 250 MB/s previous generation io2 / io2 Block Express SSD up to 256,000 up to 4000 MB/s critical databases, maximum durability io1 SSD up to 64,000 up to 1000 MB/s previous generation provisioned IOPS st1 HDD up to 500 up to 500 MB/s large sequential workloads, logs, big data sc1 HDD up to 250 up to 250 MB/s cold data, lowest cost Practical takeaways: gp3 lets you change IOPS and throughput independently of size, so \"need more IOPS but not more space\" is solved by moving from gp2 to gp3, not by inflating the volume; HDD types can't be used as boot volumes and are poor for random access; io2 Block Express delivers the highest numbers and 99.999% durability; Multi Attach (io1/io2) attaches a volume to several instances in the same AZ, but requires a cluster aware file system — it is not a replacement for EFS. Modification and maintenance Elastic Volumes change size, type, and performance parameters on the fly, without stopping the instance; you cannot shrink a volume. After expanding a volume you still need to extend the file system inside the OS — a step answer choices love to mention. Initializing a volume from a snapshot: blocks are pulled from S3 on first access, which causes a slowdown; Fast Snapshot Restore removes that latency for an extra fee. Snapshots incremental, stored in S3 (in AWS's internal storage), billed for the blocks actually changed; copied across Regions and accounts, and can be re encrypted during the copy; Data Lifecycle Manager or AWS Backup create them on a schedule and delete them by policy; Recycle Bin protects against accidental snapshot deletion; snapshots can be archived to the Archive tier for long term storage at low cost (restore takes hours). Snapshots are the primary mechanism for migrating a volume between AZs and Regions: create a snapshot, create a volume in the target zone, or copy it to the target Region. Encryption Encryption is enabled at volume creation and propagates to snapshots and volumes created from them. An unencrypted volume can't be encrypted in place: you take a snapshot, copy it while specifying a key, and create a new volume from the copy. The account setting \"encryption by default\" guarantees that all new volumes and snapshots in a Region are encrypted — this is the answer to a requirement that \"unencrypted volumes must not be created.\" EBS versus instance store EBS Instance store Data on stop/terminate persists (except root by default) lost Host failure volume intact data lost Performance high, depends on type maximum, local NVMe Snapshots yes no Price per GB and IOPS included in instance price Instance store is chosen for caching, temporary data, buffers, and clusters replicated at the application level (Cassandra, Kafka), where losing a node is acceptable. Monitoring and diagnostics CloudWatch metrics: VolumeReadOps, VolumeWriteOps, VolumeQueueLength, BurstBalance for gp2 and HDD types. Exhausted BurstBalance is the direct cause of \"disks suddenly slow down after a few hours of operation,\" fixed by switching to gp3 or io2. A throughput ceiling can also sit on the instance side: every instance type has an EBS bandwidth limit, so \"we increased the volume's IOPS but it didn't get any faster\" is solved by switching to an EBS optimized instance type with a higher limit. What the exam asks 1. \"Need maximum, predictable IOPS for a database\" — io2 (Block Express). 2. \"Cheaply store large sequential logs\" — st1. 3. \"Not enough IOPS, but plenty of space\" — gp3 with provisioned IOPS. 4. \"Move a volume to another AZ\" — snapshot, then create a volume in the target zone. 5. \"Existing volumes need to be encrypted\" — snapshot, copy with a key, new volume. 6. \"Data must survive instance termination\" — EBS with DeleteOnTermination=false . 7. \"Shared file access from multiple instances across AZs\" — not EBS, but EFS. 8. \"Automatic daily disk backups retained for 30 days\" — Data Lifecycle Manager or AWS Backup. Numbers to remember gp3: 3000 IOPS and 125 MB/s baseline regardless of size, maximum 16,000 IOPS and 1000 MB/s; gp2: 3 IOPS per GB, burst up to 3000 for volumes under 1 TB; io2 Block Express: up to 256,000 IOPS, 4000 MB/s, ratio up to 1000 IOPS per GB; st1 and sc1 start at 125 GB, optimized for sequential access; a volume lives in a single AZ; migration only happens through a snapshot; snapshots are incremental, the first one contains all occupied blocks; Multi Attach: up to 16 instances in one AZ, io1 and io2 only. How to read the wording Wording in the question Answer \"highest and most consistent IOPS for a database\" io2 Block Express \"throughput for large sequential logs at low cost\" st1 \"need more IOPS but not more capacity\" gp3 \"move the volume to another AZ\" snapshot, then create a volume in the target zone \"all new volumes must be encrypted\" encryption by default in account settings \"data must survive instance termination\" disable DeleteOnTermination \"shared access from many instances across AZs\" EFS, not EBS \"temporary scratch space with maximum speed\" instance store Operations checklist Expanding a volume: resize it, then extend the partition and file system inside the OS. Changing type: Elastic Volumes on the fly, the optimizing state can last hours, but the volume stays available. Moving between accounts: share the snapshot (for an encrypted one, also share the KMS key), copy the snapshot into your own account, create the volume. Regular backups: a Data Lifecycle Manager policy or an AWS Backup plan with copying to another Region for DR. Mini practicum 1. A 200 GB gp2 database is capped at 600 IOPS, with plenty of space left. → Switch to gp3 and set the required IOPS independently of size. 2. An existing unencrypted volume needs to be encrypted. → Snapshot, copy the snapshot specifying a KMS key, create a new volume, and swap it in. 3. An instance was restarted and its temporary data disappeared. → The data was on instance store; use an EBS volume for durability. 4. You need to guarantee that unencrypted volumes can no longer be created in the Region. → Enable encryption by default in the account's and Region's EC2 settings. Related chapters Choosing an instance type and its EBS throughput limits — the EC2 chapter. Shared file access from multiple servers — the EFS and FSx chapter. Encrypting volumes and snapshots with KMS keys — the encryption chapter. Backup policies and Vault Lock — the governance chapter. Volume and snapshot cost, and right sizing — the cost chapter. Bank audit {{q:887}} — preventing unencrypted volume creation is done with an account setting, not an AWS Config detective rule; the dump key was corrected. "},{"id":"hybrid-network","title":"Hybrid networking: Direct Connect, VPN, Transit Gateway, PrivateLink","order":14,"svc":["Direct Connect","Transit Gateway","PrivateLink","Outposts"],"questions":[6,19,43,68,76,115,123,135,184,215,240,352,374,398,435,439,445,447,448,451,474,499,504,510,558,572,599,610,614,659,667,686,703,718,722,728,734,747,775,778,805,810,811,818,821,831,835,839,858,860,866,893,899,932,949,950,957,971,983,984,992,998,1004],"html":"<h1>Hybrid connectivity</h1>\n<p>The task is to connect an on-premises network to AWS in a way that meets requirements for throughput, latency consistency, privacy, and cost. Four building blocks: Site-to-Site VPN, Direct Connect, Transit Gateway, and PrivateLink.</p>\n<h2>Site-to-Site VPN</h2>\n<p>An IPSec tunnel over the internet between a customer gateway and a virtual private gateway or Transit Gateway. Always two tunnels to different AWS endpoints for fault tolerance.</p>\n<ul><li>comes up in minutes, billed hourly plus data transfer;</li><li>a single tunnel's throughput is around 1.25 Gbps, and it scales with ECMP through Transit Gateway;</li><li>latency and jitter depend on the internet — there's no predictability;</li><li>encryption is built in;</li><li>typical roles: a quick start, a backup for Direct Connect, connecting branch offices.</li></ul>\n<p>Accelerated Site-to-Site VPN routes the tunnel through the Global Accelerator network, reducing the impact of the public internet.</p>\n<h2>Direct Connect</h2>\n<p>A dedicated physical connection from your equipment into an AWS point of presence: 1, 10, 100 Gbps (dedicated) or sub-rates through a partner (hosted).</p>\n<ul><li>predictable latency and throughput, cheaper outbound data transfer;</li><li>takes weeks or months to provision — the wrong answer whenever the requirement is \"needed in two days\";</li><li>doesn't encrypt traffic on its own: if encryption on top of DX is required, you run IPSec VPN over it or use MACsec on supported ports;</li><li>virtual interface types: private VIF — into a VPC through a VGW, transit VIF — into a Transit Gateway, public VIF — to public AWS endpoints (S3, DynamoDB) bypassing the internet;</li><li>fault tolerance: two connections at different points of presence, or DX plus VPN as a backup (the standard low-cost option), or Direct Connect SiteLink between your own sites;</li><li>a Link Aggregation Group bundles ports into a single logical link.</li></ul>\n<h2>What to choose</h2>\n<div class='table-wrap'><table><thead><tr><th>Requirement</th><th>Answer</th></tr></thead><tbody><tr><td>Connect an office to a VPC within a day</td><td>Site-to-Site VPN</td></tr><tr><td>Consistent latency for a latency-sensitive application</td><td>Direct Connect</td></tr><tr><td>Lowest possible cost with heavy outbound traffic</td><td>Direct Connect</td></tr><tr><td>Fast and encrypted, budget limited</td><td>VPN</td></tr><tr><td>DX already exists, need a backup</td><td>VPN as backup</td></tr><tr><td>Connect 50 VPCs and an on-premises network</td><td>Transit Gateway</td></tr><tr><td>Give a partner access to just one application</td><td>PrivateLink</td></tr></tbody></table></div>\n<h2>Transit Gateway</h2>\n<p>A regional hub: VPCs, VPN, Direct Connect gateway, and other TGWs connect through attachments, and routing is defined with route tables. Capabilities that get tested: transitive routing (unlike peering), segmenting environments with separate route tables, inter-Region peering, multicast, centralized internet egress, and centralized traffic inspection through Network Firewall or a GWLB.</p>\n<p>Cost: an hourly charge per attachment plus a charge for processed traffic, so for just two VPCs, peering is usually cheaper.</p>\n<h2>PrivateLink</h2>\n<p>An interface endpoint with a private IP in your subnet that leads to an AWS service, a partner service, or your own service behind an NLB (an endpoint service). The traits it gets chosen for: the networks don't connect, CIDR ranges can overlap, traffic is one-directional (consumer to provider), and access is restricted by an endpoint policy.</p>\n<p>Classic scenario: a SaaS company gives customers access to its API without VPN or peering; or a large organization publishes an internal service to hundreds of accounts.</p>\n<h2>Hybrid DNS</h2>\n<p>For on-premises servers to resolve AWS private zones, you create a Route 53 Resolver inbound endpoint; for AWS resources to resolve corporate zones, an outbound endpoint with forwarding rules. Rules are shared between accounts through RAM. This pair of endpoints is the standard answer to \"names don't resolve between the data center and the cloud.\"</p>\n<h2>On-premises extensions of AWS</h2>\n<ul><li>Outposts — an AWS rack or server in your data center, the same APIs, for data residency and latency requirements toward local systems;</li><li>Local Zones — AWS sites in major cities for single-digit-millisecond latency;</li><li>Wavelength — zones inside 5G carrier networks;</li><li>Storage Gateway and DataSync — see the data migration chapter;</li><li>ECS Anywhere and EKS Anywhere run container orchestrators on your own hardware.</li></ul>\n<h2>What the exam asks</h2>\n<ol><li>\"We need consistent latency and high throughput to the data center\" — Direct Connect, plus VPN if encryption is required.</li><li>\"DX is already being built, but the connection is needed now\" — VPN, temporarily.</li><li>\"Connect dozens of VPCs without a peering mesh\" — Transit Gateway.</li><li>\"A partner needs access only to our API, networks can't be connected\" — PrivateLink.</li><li>\"Backup for Direct Connect at minimal cost\" — Site-to-Site VPN.</li><li>\"The application must run on-site because of data requirements\" — Outposts.</li><li>\"On-premises servers can't see RDS names in the VPC\" — Resolver inbound endpoint.</li><li>\"Access S3 from the data center bypassing the internet\" — public VIF over Direct Connect or an interface endpoint, the choice depending on cost and requirements.</li></ol>\n<h2>Numbers to remember</h2>\n<ul><li>Site-to-Site VPN: always two tunnels, about 1.25 Gbps per tunnel, comes up within minutes;</li><li>Direct Connect: 1, 10, 100 Gbps dedicated, sub-rates hosted, lead time of weeks to months;</li><li>Direct Connect doesn't encrypt traffic on its own;</li><li>Transit Gateway: thousands of attachments, billed per attachment and per processed traffic, supports inter-Region peering;</li><li>PrivateLink: one-directional access, consumer → provider, CIDR ranges can overlap;</li><li>VPC peering: not transitive, CIDR ranges must not overlap.</li></ul>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>Wording in the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"consistent latency, dedicated bandwidth\"</td><td>Direct Connect</td></tr><tr><td>\"connect quickly, encrypted, low cost\"</td><td>Site-to-Site VPN</td></tr><tr><td>\"backup for Direct Connect\"</td><td>VPN as a backup path</td></tr><tr><td>\"hundreds of VPCs, central routing\"</td><td>Transit Gateway</td></tr><tr><td>\"expose one service to customers without connecting networks\"</td><td>PrivateLink</td></tr><tr><td>\"overlapping CIDR ranges after a merger\"</td><td>PrivateLink or NAT, not peering</td></tr><tr><td>\"resolve on-premises names from AWS\"</td><td>Resolver outbound endpoint with rules</td></tr><tr><td>\"run AWS services in our data center\"</td><td>Outposts</td></tr></tbody></table></div>\n<h2>How to work through a hybrid question</h2>\n<ol><li>What are we connecting: network to network (VPN, DX, TGW) or consumer to service (PrivateLink)?</li><li>Timeline: days — VPN; months acceptable — Direct Connect.</li><li>Encryption requirement: DX alone doesn't provide it.</li><li>Scale: two or three VPCs — peering; dozens — Transit Gateway.</li><li>Address overlap: if CIDR ranges match, routing is impossible in principle.</li><li>Redundancy: two DX connections at different points of presence, or DX plus VPN.</li></ol>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> A company is migrating its ERP to AWS and needs consistent latency to the data center, with go-live in six months. → Direct Connect; Site-to-Site VPN while it's being built.</p>\n<p><strong>2.</strong> An audit requirement: traffic between the data center and AWS must be encrypted, and the link must be dedicated. → Direct Connect plus IPSec VPN on top of it (or MACsec on supported ports).</p>\n<p><strong>3.</strong> The organization has 40 VPCs across two Regions and two data centers. → Transit Gateway in each Region, inter-Region peering, attachments for VPN and DX.</p>\n<p><strong>4.</strong> A SaaS provider gives access to its API, networks can't be connected, and addressing overlaps. → PrivateLink: an interface endpoint in your VPC, no routing needed between the networks.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"835\" href=\"#\">questions 835</a> — disputed: access to S3 over Direct Connect, public VIF versus interface endpoint; a question of pricing, not technical feasibility.</li><li><a class=\"qref\" data-q=\"754\" href=\"#\">questions 754</a> — disputed: a static IP requirement points to Global Accelerator, since CloudFront has no static addresses.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>24</td><td>38%</td><td>5</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>3</td><td>5%</td><td>0</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>19</td><td>30%</td><td>15</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>17</td><td>27%</td><td>2</td></tr></tbody></table></div><p>Total questions in this chapter: <b>63</b>. With a written walkthrough: <b>63</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#6</b> — PERF: transfer speed with limited bandwidth</li><li><b>#115</b> — SEC: private route for file transfer</li><li><b>#135</b> — SEC: private connectivity to a provider's service</li><li><b>#184</b> — PERF: function network access to an on-premises data center network</li><li><b>#439</b> — PERF: running out of IP addresses in a VPC</li><li><b>#445</b> — PERF: transferring 700 TB without stopping access</li><li><b>#451</b> — SEC: shared responsibility between AWS and the customer</li><li><b>#474</b> — PERF: VPC connectivity across regions</li></ul>","text":" Hybrid connectivity The task is to connect an on premises network to AWS in a way that meets requirements for throughput, latency consistency, privacy, and cost. Four building blocks: Site to Site VPN, Direct Connect, Transit Gateway, and PrivateLink. Site to Site VPN An IPSec tunnel over the internet between a customer gateway and a virtual private gateway or Transit Gateway. Always two tunnels to different AWS endpoints for fault tolerance. comes up in minutes, billed hourly plus data transfer; a single tunnel's throughput is around 1.25 Gbps, and it scales with ECMP through Transit Gateway; latency and jitter depend on the internet — there's no predictability; encryption is built in; typical roles: a quick start, a backup for Direct Connect, connecting branch offices. Accelerated Site to Site VPN routes the tunnel through the Global Accelerator network, reducing the impact of the public internet. Direct Connect A dedicated physical connection from your equipment into an AWS point of presence: 1, 10, 100 Gbps (dedicated) or sub rates through a partner (hosted). predictable latency and throughput, cheaper outbound data transfer; takes weeks or months to provision — the wrong answer whenever the requirement is \"needed in two days\"; doesn't encrypt traffic on its own: if encryption on top of DX is required, you run IPSec VPN over it or use MACsec on supported ports; virtual interface types: private VIF — into a VPC through a VGW, transit VIF — into a Transit Gateway, public VIF — to public AWS endpoints (S3, DynamoDB) bypassing the internet; fault tolerance: two connections at different points of presence, or DX plus VPN as a backup (the standard low cost option), or Direct Connect SiteLink between your own sites; a Link Aggregation Group bundles ports into a single logical link. What to choose Requirement Answer Connect an office to a VPC within a day Site to Site VPN Consistent latency for a latency sensitive application Direct Connect Lowest possible cost with heavy outbound traffic Direct Connect Fast and encrypted, budget limited VPN DX already exists, need a backup VPN as backup Connect 50 VPCs and an on premises network Transit Gateway Give a partner access to just one application PrivateLink Transit Gateway A regional hub: VPCs, VPN, Direct Connect gateway, and other TGWs connect through attachments, and routing is defined with route tables. Capabilities that get tested: transitive routing (unlike peering), segmenting environments with separate route tables, inter Region peering, multicast, centralized internet egress, and centralized traffic inspection through Network Firewall or a GWLB. Cost: an hourly charge per attachment plus a charge for processed traffic, so for just two VPCs, peering is usually cheaper. PrivateLink An interface endpoint with a private IP in your subnet that leads to an AWS service, a partner service, or your own service behind an NLB (an endpoint service). The traits it gets chosen for: the networks don't connect, CIDR ranges can overlap, traffic is one directional (consumer to provider), and access is restricted by an endpoint policy. Classic scenario: a SaaS company gives customers access to its API without VPN or peering; or a large organization publishes an internal service to hundreds of accounts. Hybrid DNS For on premises servers to resolve AWS private zones, you create a Route 53 Resolver inbound endpoint; for AWS resources to resolve corporate zones, an outbound endpoint with forwarding rules. Rules are shared between accounts through RAM. This pair of endpoints is the standard answer to \"names don't resolve between the data center and the cloud.\" On premises extensions of AWS Outposts — an AWS rack or server in your data center, the same APIs, for data residency and latency requirements toward local systems; Local Zones — AWS sites in major cities for single digit millisecond latency; Wavelength — zones inside 5G carrier networks; Storage Gateway and DataSync — see the data migration chapter; ECS Anywhere and EKS Anywhere run container orchestrators on your own hardware. What the exam asks 1. \"We need consistent latency and high throughput to the data center\" — Direct Connect, plus VPN if encryption is required. 2. \"DX is already being built, but the connection is needed now\" — VPN, temporarily. 3. \"Connect dozens of VPCs without a peering mesh\" — Transit Gateway. 4. \"A partner needs access only to our API, networks can't be connected\" — PrivateLink. 5. \"Backup for Direct Connect at minimal cost\" — Site to Site VPN. 6. \"The application must run on site because of data requirements\" — Outposts. 7. \"On premises servers can't see RDS names in the VPC\" — Resolver inbound endpoint. 8. \"Access S3 from the data center bypassing the internet\" — public VIF over Direct Connect or an interface endpoint, the choice depending on cost and requirements. Numbers to remember Site to Site VPN: always two tunnels, about 1.25 Gbps per tunnel, comes up within minutes; Direct Connect: 1, 10, 100 Gbps dedicated, sub rates hosted, lead time of weeks to months; Direct Connect doesn't encrypt traffic on its own; Transit Gateway: thousands of attachments, billed per attachment and per processed traffic, supports inter Region peering; PrivateLink: one directional access, consumer → provider, CIDR ranges can overlap; VPC peering: not transitive, CIDR ranges must not overlap. How to read the wording Wording in the question Answer \"consistent latency, dedicated bandwidth\" Direct Connect \"connect quickly, encrypted, low cost\" Site to Site VPN \"backup for Direct Connect\" VPN as a backup path \"hundreds of VPCs, central routing\" Transit Gateway \"expose one service to customers without connecting networks\" PrivateLink \"overlapping CIDR ranges after a merger\" PrivateLink or NAT, not peering \"resolve on premises names from AWS\" Resolver outbound endpoint with rules \"run AWS services in our data center\" Outposts How to work through a hybrid question 1. What are we connecting: network to network (VPN, DX, TGW) or consumer to service (PrivateLink)? 2. Timeline: days — VPN; months acceptable — Direct Connect. 3. Encryption requirement: DX alone doesn't provide it. 4. Scale: two or three VPCs — peering; dozens — Transit Gateway. 5. Address overlap: if CIDR ranges match, routing is impossible in principle. 6. Redundancy: two DX connections at different points of presence, or DX plus VPN. Mini practicum 1. A company is migrating its ERP to AWS and needs consistent latency to the data center, with go live in six months. → Direct Connect; Site to Site VPN while it's being built. 2. An audit requirement: traffic between the data center and AWS must be encrypted, and the link must be dedicated. → Direct Connect plus IPSec VPN on top of it (or MACsec on supported ports). 3. The organization has 40 VPCs across two Regions and two data centers. → Transit Gateway in each Region, inter Region peering, attachments for VPN and DX. 4. A SaaS provider gives access to its API, networks can't be connected, and addressing overlaps. → PrivateLink: an interface endpoint in your VPC, no routing needed between the networks. Bank audit {{q:835}} — disputed: access to S3 over Direct Connect, public VIF versus interface endpoint; a question of pricing, not technical feasibility. {{q:754}} — disputed: a static IP requirement points to Global Accelerator, since CloudFront has no static addresses. "},{"id":"data-transfer","title":"Data transfer — DataSync, Storage Gateway, Snow, Transfer Family","order":15,"svc":["DataSync","Storage Gateway","Snow Family","Transfer Family","DMS"],"questions":[1,6,9,43,76,97,99,102,113,136,155,175,186,188,197,205,215,235,243,249,259,277,281,292,293,301,302,304,305,307,324,331,334,338,346,398,407,409,414,421,426,435,440,445,456,457,482,496,500,506,527,528,539,540,546,547,565,568,583,588,589,598,604,605,607,617,626,629,638,659,666,673,680,691,693,702,703,705,711,717,718,719,728,747,753,763,767,771,778,786,788,800,805,806,816,818,832,844,848,849,865,875,880,886,895,896,897,898,909,918,933,948,990,1000,1010],"html":"<h1>Data transfer and synchronization</h1>\n<p>Questions in this block come down to two parameters: volume and whether a link exists. From there you narrow down whether it's a one-time transfer or ongoing hybrid operation, and which protocol the application needs.</p>\n<h2>Quick decision matrix</h2>\n<div class='table-wrap'><table><thead><tr><th>Situation</th><th>Answer</th></tr></thead><tbody><tr><td>Hundreds of TB or PB, link is insufficient</td><td>Snowball Edge; at 100 PB — Snowmobile</td></tr><tr><td>Tens of TB, link exists, one-time or scheduled</td><td>DataSync</td></tr><tr><td>On-premises apps need ongoing access to data in the cloud</td><td>Storage Gateway</td></tr><tr><td>Partners upload files over SFTP/FTPS</td><td>Transfer Family</td></tr><tr><td>Database migration with minimal downtime</td><td>DMS (plus SCT on an engine change)</td></tr><tr><td>Continuous event stream</td><td>Kinesis Data Firehose</td></tr><tr><td>Syncing S3 across Regions</td><td>S3 Replication</td></tr></tbody></table></div>\n<p>Rule of thumb: 1 TB over a 1 Gbps link ideally takes about 2.5 hours, in practice longer. If the math gives you weeks but the deadline is days, the answer is Snow.</p>\n<h2>DataSync</h2>\n<p>An agent (or an agentless service for cloud sources) moves files between NFS, SMB, HDFS, object storage, and AWS services: S3, EFS, FSx. It handles incremental sync, integrity verification, filters, scheduling, preservation of metadata and ACLs, encryption in transit, and operation through a VPC endpoint. Speed reaches tens of Gbps, and throttling is configurable.</p>\n<p>The answer to \"transfer 50 TB of files and then sync daily,\" \"migrate a file server to FSx,\" \"copy data between Regions and accounts with verification.\"</p>\n<h2>Storage Gateway</h2>\n<p>Hybrid storage: a local cache in front of \"unlimited\" capacity in S3.</p>\n<div class='table-wrap'><table><thead><tr><th>Type</th><th>Local protocol</th><th>Where the data lives</th><th>Scenario</th></tr></thead><tbody><tr><td>S3 File Gateway</td><td>NFS, SMB</td><td>objects in S3</td><td>file access to objects, document archiving</td></tr><tr><td>FSx File Gateway</td><td>SMB</td><td>FSx for Windows</td><td>local cache for Windows file shares</td></tr><tr><td>Volume Gateway (cached)</td><td>iSCSI</td><td>primary data in S3, hot data locally</td><td>extending disk space</td></tr><tr><td>Volume Gateway (stored)</td><td>iSCSI</td><td>primary data locally, copies in S3</td><td>backup with fast local access</td></tr><tr><td>Tape Gateway</td><td>iSCSI VTL</td><td>S3 and Glacier</td><td>replacing tape libraries, no change to backup software</td></tr></tbody></table></div>\n<p>Cues in question text: \"backup software writes to tape\" — Tape Gateway; \"need low-latency access to recently used data with unlimited cloud storage\" — cached Volume Gateway or File Gateway; \"Windows file shares with AD\" — FSx File Gateway.</p>\n<h2>Snow Family</h2>\n<ul><li>Snowcone — up to 8 or 14 TB, a portable device that can run in the field;</li><li>Snowball Edge Storage Optimized — around 80 TB of usable capacity;</li><li>Snowball Edge Compute Optimized — less storage, but has vCPUs, memory, and a GPU for on-site processing (edge computing, preprocessing before shipping);</li><li>Snowmobile — a shipping-container truck, tens of petabytes.</li></ul>\n<p>Data is encrypted with KMS, the device is tracked, and the return trip goes through the carrier. Snowball is also used for one-way migrations before a data center shutdown and for collecting data where there's no connectivity at all.</p>\n<h2>Transfer Family</h2>\n<p>Managed SFTP, FTPS, FTP, and AS2 endpoints on top of S3 and EFS. Users and their keys are stored in the service, in Directory Service, or with your own provider via Lambda. The answer to \"partners have been sending files over SFTP for years, we can't change their process, but we don't want to maintain a server.\"</p>\n<h2>S3 transfer mechanics</h2>\n<ul><li>Multipart upload and Transfer Acceleration for large files over the internet;</li><li>S3 Batch Operations — bulk actions across millions of objects (copying, changing storage class, invoking Lambda);</li><li>S3 Batch Replication — replicating objects that already exist;</li><li>Import/Export via Snow, when volume makes the network pointless.</li></ul>\n<h2>Server and database migration</h2>\n<p>Application Migration Service (MGN) replicates entire servers at the block level and cuts them over to EC2 with minimal downtime. Database Migration Service moves DBMS data using CDC. Migration Hub shows overall progress. For inventory and dependency assessment — Application Discovery Service.</p>\n<h2>What the exam asks</h2>\n<ol><li>\"Transfer 500 TB in two weeks, 500 Mbps link\" — Snowball Edge.</li><li>\"Copy NFS files into S3 daily with integrity verification\" — DataSync.</li><li>\"On-premises applications must see S3 as a file share\" — S3 File Gateway.</li><li>\"Retire tape libraries without changing the backup software\" — Tape Gateway.</li><li>\"Clients send files over SFTP\" — Transfer Family.</li><li>\"Move Oracle to Aurora PostgreSQL\" — SCT plus DMS.</li><li>\"Collect data from a ship with no connectivity\" — Snowcone or Snowball with onboard compute.</li><li>\"Keep S3 continuously in sync across Regions\" — Cross-Region Replication, not DataSync.</li></ol>\n<h2>Numbers to remember</h2>\n<ul><li>Snowcone up to 8 or 14 TB, Snowball Edge around 80 TB usable, Snowmobile up to 100 PB;</li><li>a Snow device travels to you and back, a typical cycle is about a week;</li><li>DataSync speeds up transfer many times over compared to plain copying and verifies the integrity of every file;</li><li>Storage Gateway keeps a local cache while the full dataset lives in S3;</li><li>Transfer Family is billed per hour of protocol endpoint uptime and per gigabyte transferred;</li><li>S3 Transfer Acceleration uses the CloudFront edge network and is billed separately.</li></ul>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>Wording in the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"petabytes, limited bandwidth, weeks deadline\"</td><td>Snow Family</td></tr><tr><td>\"ongoing incremental sync of NFS shares\"</td><td>DataSync</td></tr><tr><td>\"on-premises apps need low-latency access to cloud data\"</td><td>Storage Gateway</td></tr><tr><td>\"replace tape backups without changing software\"</td><td>Tape Gateway</td></tr><tr><td>\"partners upload via SFTP\"</td><td>Transfer Family</td></tr><tr><td>\"migrate database with minimal downtime\"</td><td>DMS with CDC</td></tr><tr><td>\"keep two buckets in sync across Regions\"</td><td>S3 Replication</td></tr><tr><td>\"collect data at a remote site with no connectivity\"</td><td>Snowcone or Snowball Edge</td></tr></tbody></table></div>\n<h2>Choosing in three steps</h2>\n<ol><li>Calculate the transfer time over the link: volume divided by real throughput. If that exceeds the project deadline, only Snow devices remain in play.</li><li>Determine whether this is a one-time transfer or an ongoing process. One-time — Snow or DataSync; ongoing hybrid access — Storage Gateway; ongoing exchange with partners — Transfer Family.</li><li>Check the application's protocol: NFS and SMB — DataSync or File Gateway, iSCSI — Volume Gateway, SFTP — Transfer Family, SQL — DMS.</li></ol>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> 600 TB of archives need to move into S3 within a month, link is 200 Mbps. → Several Snowball Edge devices: over the link, the transfer would take more than nine months.</p>\n<p><strong>2.</strong> Every day, 2 TB of new files from an NFS share must land in S3 with integrity verification. → Scheduled DataSync with incremental sync.</p>\n<p><strong>3.</strong> An on-premises application writes to an iSCSI volume and must keep working, but the data needs to be in the cloud. → Volume Gateway in cached mode: hot blocks locally, the full dataset in S3.</p>\n<p><strong>4.</strong> Partners have been sending files over SFTP for years, and their process can't change. → AWS Transfer Family on top of S3 with authentication via a directory or Lambda.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"113\" href=\"#\">questions 113</a> — low confidence: Snowball Edge with later processing in Glue versus Snowball with onboard compute.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>15</td><td>13%</td><td>2</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>25</td><td>22%</td><td>5</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>46</td><td>40%</td><td>33</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>29</td><td>25%</td><td>4</td></tr></tbody></table></div><p>Total questions in this chapter: <b>115</b>. With a written walkthrough: <b>115</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#1</b> — PERF: speed of aggregating data into a single bucket</li><li><b>#6</b> — PERF: transfer speed with limited bandwidth</li><li><b>#99</b> — PERF: file storage with Lustre clients</li><li><b>#102</b> — PERF: automating data migration to EFS</li><li><b>#113</b> — PERF: transferring 50 TB with limited bandwidth</li><li><b>#136</b> — RES: database stays online and in sync during migration</li><li><b>#175</b> — PERF: timeouts from database connection exhaustion</li><li><b>#186</b> — PERF: shared Windows file system across zones</li></ul>","text":" Data transfer and synchronization Questions in this block come down to two parameters: volume and whether a link exists. From there you narrow down whether it's a one time transfer or ongoing hybrid operation, and which protocol the application needs. Quick decision matrix Situation Answer Hundreds of TB or PB, link is insufficient Snowball Edge; at 100 PB — Snowmobile Tens of TB, link exists, one time or scheduled DataSync On premises apps need ongoing access to data in the cloud Storage Gateway Partners upload files over SFTP/FTPS Transfer Family Database migration with minimal downtime DMS (plus SCT on an engine change) Continuous event stream Kinesis Data Firehose Syncing S3 across Regions S3 Replication Rule of thumb: 1 TB over a 1 Gbps link ideally takes about 2.5 hours, in practice longer. If the math gives you weeks but the deadline is days, the answer is Snow. DataSync An agent (or an agentless service for cloud sources) moves files between NFS, SMB, HDFS, object storage, and AWS services: S3, EFS, FSx. It handles incremental sync, integrity verification, filters, scheduling, preservation of metadata and ACLs, encryption in transit, and operation through a VPC endpoint. Speed reaches tens of Gbps, and throttling is configurable. The answer to \"transfer 50 TB of files and then sync daily,\" \"migrate a file server to FSx,\" \"copy data between Regions and accounts with verification.\" Storage Gateway Hybrid storage: a local cache in front of \"unlimited\" capacity in S3. Type Local protocol Where the data lives Scenario S3 File Gateway NFS, SMB objects in S3 file access to objects, document archiving FSx File Gateway SMB FSx for Windows local cache for Windows file shares Volume Gateway (cached) iSCSI primary data in S3, hot data locally extending disk space Volume Gateway (stored) iSCSI primary data locally, copies in S3 backup with fast local access Tape Gateway iSCSI VTL S3 and Glacier replacing tape libraries, no change to backup software Cues in question text: \"backup software writes to tape\" — Tape Gateway; \"need low latency access to recently used data with unlimited cloud storage\" — cached Volume Gateway or File Gateway; \"Windows file shares with AD\" — FSx File Gateway. Snow Family Snowcone — up to 8 or 14 TB, a portable device that can run in the field; Snowball Edge Storage Optimized — around 80 TB of usable capacity; Snowball Edge Compute Optimized — less storage, but has vCPUs, memory, and a GPU for on site processing (edge computing, preprocessing before shipping); Snowmobile — a shipping container truck, tens of petabytes. Data is encrypted with KMS, the device is tracked, and the return trip goes through the carrier. Snowball is also used for one way migrations before a data center shutdown and for collecting data where there's no connectivity at all. Transfer Family Managed SFTP, FTPS, FTP, and AS2 endpoints on top of S3 and EFS. Users and their keys are stored in the service, in Directory Service, or with your own provider via Lambda. The answer to \"partners have been sending files over SFTP for years, we can't change their process, but we don't want to maintain a server.\" S3 transfer mechanics Multipart upload and Transfer Acceleration for large files over the internet; S3 Batch Operations — bulk actions across millions of objects (copying, changing storage class, invoking Lambda); S3 Batch Replication — replicating objects that already exist; Import/Export via Snow, when volume makes the network pointless. Server and database migration Application Migration Service (MGN) replicates entire servers at the block level and cuts them over to EC2 with minimal downtime. Database Migration Service moves DBMS data using CDC. Migration Hub shows overall progress. For inventory and dependency assessment — Application Discovery Service. What the exam asks 1. \"Transfer 500 TB in two weeks, 500 Mbps link\" — Snowball Edge. 2. \"Copy NFS files into S3 daily with integrity verification\" — DataSync. 3. \"On premises applications must see S3 as a file share\" — S3 File Gateway. 4. \"Retire tape libraries without changing the backup software\" — Tape Gateway. 5. \"Clients send files over SFTP\" — Transfer Family. 6. \"Move Oracle to Aurora PostgreSQL\" — SCT plus DMS. 7. \"Collect data from a ship with no connectivity\" — Snowcone or Snowball with onboard compute. 8. \"Keep S3 continuously in sync across Regions\" — Cross Region Replication, not DataSync. Numbers to remember Snowcone up to 8 or 14 TB, Snowball Edge around 80 TB usable, Snowmobile up to 100 PB; a Snow device travels to you and back, a typical cycle is about a week; DataSync speeds up transfer many times over compared to plain copying and verifies the integrity of every file; Storage Gateway keeps a local cache while the full dataset lives in S3; Transfer Family is billed per hour of protocol endpoint uptime and per gigabyte transferred; S3 Transfer Acceleration uses the CloudFront edge network and is billed separately. How to read the wording Wording in the question Answer \"petabytes, limited bandwidth, weeks deadline\" Snow Family \"ongoing incremental sync of NFS shares\" DataSync \"on premises apps need low latency access to cloud data\" Storage Gateway \"replace tape backups without changing software\" Tape Gateway \"partners upload via SFTP\" Transfer Family \"migrate database with minimal downtime\" DMS with CDC \"keep two buckets in sync across Regions\" S3 Replication \"collect data at a remote site with no connectivity\" Snowcone or Snowball Edge Choosing in three steps 1. Calculate the transfer time over the link: volume divided by real throughput. If that exceeds the project deadline, only Snow devices remain in play. 2. Determine whether this is a one time transfer or an ongoing process. One time — Snow or DataSync; ongoing hybrid access — Storage Gateway; ongoing exchange with partners — Transfer Family. 3. Check the application's protocol: NFS and SMB — DataSync or File Gateway, iSCSI — Volume Gateway, SFTP — Transfer Family, SQL — DMS. Mini practicum 1. 600 TB of archives need to move into S3 within a month, link is 200 Mbps. → Several Snowball Edge devices: over the link, the transfer would take more than nine months. 2. Every day, 2 TB of new files from an NFS share must land in S3 with integrity verification. → Scheduled DataSync with incremental sync. 3. An on premises application writes to an iSCSI volume and must keep working, but the data needs to be in the cloud. → Volume Gateway in cached mode: hot blocks locally, the full dataset in S3. 4. Partners have been sending files over SFTP for years, and their process can't change. → AWS Transfer Family on top of S3 with authentication via a directory or Lambda. Bank audit {{q:113}} — low confidence: Snowball Edge with later processing in Glue versus Snowball with onboard compute. "},{"id":"edge-cloudfront","title":"CloudFront, edge, and Global Accelerator","order":16,"svc":["CloudFront","Global Accelerator"],"questions":[12,21,29,38,59,83,104,116,120,129,131,132,141,142,155,158,165,166,172,173,183,203,205,216,239,261,264,265,266,272,280,286,288,291,302,310,313,328,333,345,352,357,358,367,396,399,408,416,431,441,443,447,461,473,486,502,513,523,530,538,541,542,568,577,592,600,622,623,625,647,654,684,691,700,701,725,738,749,754,773,795,796,815,840,855,871,881,894,905,914,915,944,961,971,974,984,1004,1012,1015],"html":"<h1>Content and edge</h1>\n<p>The AWS edge network solves three problems: bring content closer to the user, take load off the origin, and provide stable network delivery over the AWS backbone instead of the public internet.</p>\n<h2>CloudFront</h2>\n<p>A CDN spanning hundreds of points of presence. Works with HTTP and HTTPS, caches responses, and can compress, sign, and process requests with functions at the edge.</p>\n<p>Origin: S3 (through Origin Access Control), ALB, EC2, API Gateway, any public HTTP server, including ones outside AWS. An origin group provides failover to a backup source based on error codes — a simple way to meet a fault-tolerance requirement for content delivery.</p>\n<p>Key mechanics:</p>\n<ul><li>a cache policy defines the cache key (which headers, cookies, and query strings participate) and the TTL; an origin request policy defines what gets forwarded to the origin;</li><li>invalidation removes objects from the cache early; versioning file names (<code>app.v2.js</code>) is cheaper and more reliable than mass invalidations;</li><li>signed URLs and signed cookies gate paid content: a URL covers one object, cookies cover a set of objects;</li><li>Origin Access Control (OAC, the successor to OAI) gives CloudFront access to a private bucket while the bucket stays closed to the internet;</li><li>the Geo restriction setting blocks countries at the CDN level;</li><li>a certificate for a custom domain is issued in ACM in the us-east-1 Region regardless of where the origin is located;</li><li>WAF attaches to the distribution, Shield Advanced protects against volumetric attacks;</li><li>logs: standard access logs to S3 and real-time logs to Kinesis.</li></ul>\n<p>Functions at the edge:</p>\n<div class='table-wrap'><table><thead><tr><th></th><th>CloudFront Functions</th><th>Lambda@Edge</th></tr></thead><tbody><tr><td>Language</td><td>JavaScript</td><td>Node.js, Python</td></tr><tr><td>Execution time</td><td>under 1 ms</td><td>up to 5 or 30 seconds</td></tr><tr><td>Events</td><td>viewer request/response</td><td>all four, including origin</td></tr><tr><td>Access to network and request body</td><td>no</td><td>yes</td></tr><tr><td>Price</td><td>very low</td><td>higher</td></tr></tbody></table></div>\n<p>Simple header rewriting, redirects, and cache-key normalization — CloudFront Functions; calling an external API, A/B logic, working with the body — Lambda@Edge.</p>\n<h2>Global Accelerator</h2>\n<p>Two static anycast IP addresses; traffic enters the nearest point of presence and then travels over the AWS backbone to the nearest healthy endpoint (ALB, NLB, EC2, Elastic IP). Works with TCP and UDP, so it fits cases where CloudFront can't help: gaming, VoIP, IoT, custom protocols.</p>\n<p>Properties it's chosen for: static IPs for allowlisting, fast cross-Region failover based on health checks (seconds), weighted traffic distribution across Regions, client affinity.</p>\n<h2>CloudFront versus Global Accelerator</h2>\n<div class='table-wrap'><table><thead><tr><th>Requirement</th><th>Answer</th></tr></thead><tbody><tr><td>Caching HTTP content</td><td>CloudFront</td></tr><tr><td>Static IP addresses</td><td>Global Accelerator</td></tr><tr><td>UDP or arbitrary TCP</td><td>Global Accelerator</td></tr><tr><td>Serving video and static assets</td><td>CloudFront</td></tr><tr><td>Instant failover between Regions</td><td>Global Accelerator</td></tr><tr><td>Reducing load on the origin</td><td>CloudFront</td></tr><tr><td>Dynamic API with high latency due to distance</td><td>either works: CloudFront terminates TLS closer to the user and keeps a connection to the origin open, GA shortens the path over the backbone</td></tr></tbody></table></div>\n<h2>Cost</h2>\n<p>CloudFront saves on outbound traffic: serving from cache is cheaper than serving from S3 or EC2, plus there's no origin data-transfer charge on a cache hit. Price Class limits the set of edge locations in use and lowers cost when the audience is local. Global Accelerator is billed hourly plus a premium for data transferred.</p>\n<h2>Common patterns</h2>\n<ul><li>Static content in S3 with OAC + dynamic content on an ALB as a second origin, routed by path pattern.</li><li>API Gateway behind CloudFront with GET caching and WAF on the distribution.</li><li>Video: S3 as the origin, signed cookies for subscribers, MediaConvert for transcoding.</li><li>Multi-Region fault tolerance: Route 53 failover or Global Accelerator in front of regional load balancers.</li></ul>\n<h2>What the exam asks</h2>\n<ol><li>\"Users worldwide complain about slow image loading\" — CloudFront.</li><li>\"The bucket must stay private, but content is still served\" — OAC.</li><li>\"Need fixed IPs for a partner firewall\" — Global Accelerator or NLB.</li><li>\"A UDP game server with players in three Regions\" — Global Accelerator.</li><li>\"Paid content is available only to subscribers for 24 hours\" — signed URL or cookies.</li><li>\"Block access from specific countries\" — geo restriction (or a WAF rule).</li><li>\"The cache serves an old version of the site after a deploy\" — invalidation or file-name versioning.</li><li>\"The certificate doesn't show up in the CloudFront list\" — it wasn't issued in us-east-1.</li></ol>\n<h2>Numbers to remember</h2>\n<ul><li>hundreds of points of presence, cache by Region plus regional edge caches;</li><li>TTL is set by the cache policy and origin headers, 24 hours by default;</li><li>invalidation: the first 1000 paths per month are free, then it's billed;</li><li>CloudFront Functions — under 1 ms and viewer events only, Lambda@Edge — up to 5 seconds on viewer events and up to 30 seconds on origin events;</li><li>a certificate for a custom domain must come from ACM in us-east-1 only;</li><li>Global Accelerator provides 2 static anycast addresses and switches traffic within seconds.</li></ul>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>Wording in the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"global users, static and media content\"</td><td>CloudFront</td></tr><tr><td>\"keep the S3 bucket private\"</td><td>OAC</td></tr><tr><td>\"static IP addresses for allowlisting\"</td><td>Global Accelerator or NLB</td></tr><tr><td>\"UDP or non-HTTP protocol\"</td><td>Global Accelerator</td></tr><tr><td>\"restrict content to paying subscribers\"</td><td>signed URL or signed cookies</td></tr><tr><td>\"block specific countries\"</td><td>geo restriction or WAF</td></tr><tr><td>\"fast failover between Regions\"</td><td>Global Accelerator or Route 53 health checks</td></tr><tr><td>\"reduce load and egress cost from origin\"</td><td>CloudFront</td></tr></tbody></table></div>\n<h2>Checking the cache when walking through a question</h2>\n<p>The cache works well when the cache key is stable: extra headers, cookies, and query parameters in the key fragment the cache and drop the hit rate to zero. Cues in the answer choices: \"forward all headers and cookies\" is almost always a regression, \"cache policy with only required query strings\" is an improvement. Dynamic content benefits from CloudFront too: TLS is terminated near the user, and a persistent connection over the AWS backbone carries traffic to the origin, cutting down session-establishment time.</p>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> A site on an ALB in eu-central-1; half the users, in Asia, complain about slow image loading. → CloudFront in front of the ALB and S3: the cache sits closer to the user, and TLS is terminated at the edge.</p>\n<p><strong>2.</strong> Files from a private bucket need to be served only through the CDN. → Origin Access Control plus a bucket policy that allows access only from the distribution.</p>\n<p><strong>3.</strong> A partner requires a list of static IP addresses for allowlisting; the application runs over TCP. → Global Accelerator: two static anycast addresses and failover driven by health checks.</p>\n<p><strong>4.</strong> After a deploy, users see the old version of the JavaScript. → Version the file names or invalidate the paths; also clean up the cache policy while you're at it.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"647\" href=\"#\">questions 647</a> — VoIP runs over UDP, and CloudFront only serves HTTP/HTTPS; the correct answer is Global Accelerator, the dump key was corrected.</li><li><a class=\"qref\" data-q=\"754\" href=\"#\">questions 754</a> — disputed: static IP addresses are provided by Global Accelerator, not CloudFront.</li><li><a class=\"qref\" data-q=\"971\" href=\"#\">questions 971</a> — disputed: repeatedly serving the same files to servers across different Regions is CloudFront caching, not Transfer Acceleration.</li><li><a class=\"qref\" data-q=\"974\" href=\"#\">questions 974</a> — the CloudFront certificate always comes from us-east-1; the key was corrected to AD.</li><li><a class=\"qref\" data-q=\"447\" href=\"#\">questions 447</a> — CloudFront has no health checks of its own: active-active failover between Regions is built on Route 53.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>23</td><td>23%</td><td>5</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>17</td><td>17%</td><td>2</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>42</td><td>42%</td><td>22</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>17</td><td>17%</td><td>4</td></tr></tbody></table></div><p>Total questions in this chapter: <b>99</b>. With a written walkthrough: <b>99</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#59</b> — PERF: ingesting and processing clickstream data</li><li><b>#83</b> — PERF: speeding up site load times for Europe</li><li><b>#116</b> — PERF: static site instead of a CMS requiring patching</li><li><b>#120</b> — PERF: routing to instances across two regions</li><li><b>#129</b> — PERF: removing the scaling ceiling and capacity planning</li><li><b>#131</b> — SEC: blocking direct access to the bucket URL</li><li><b>#166</b> — PERF: serving static content to millions of users</li><li><b>#203</b> — PERF: scaling handlers as load grows</li></ul>","text":" Content and edge The AWS edge network solves three problems: bring content closer to the user, take load off the origin, and provide stable network delivery over the AWS backbone instead of the public internet. CloudFront A CDN spanning hundreds of points of presence. Works with HTTP and HTTPS, caches responses, and can compress, sign, and process requests with functions at the edge. Origin: S3 (through Origin Access Control), ALB, EC2, API Gateway, any public HTTP server, including ones outside AWS. An origin group provides failover to a backup source based on error codes — a simple way to meet a fault tolerance requirement for content delivery. Key mechanics: a cache policy defines the cache key (which headers, cookies, and query strings participate) and the TTL; an origin request policy defines what gets forwarded to the origin; invalidation removes objects from the cache early; versioning file names ( app.v2.js ) is cheaper and more reliable than mass invalidations; signed URLs and signed cookies gate paid content: a URL covers one object, cookies cover a set of objects; Origin Access Control (OAC, the successor to OAI) gives CloudFront access to a private bucket while the bucket stays closed to the internet; the Geo restriction setting blocks countries at the CDN level; a certificate for a custom domain is issued in ACM in the us east 1 Region regardless of where the origin is located; WAF attaches to the distribution, Shield Advanced protects against volumetric attacks; logs: standard access logs to S3 and real time logs to Kinesis. Functions at the edge: CloudFront Functions Lambda@Edge Language JavaScript Node.js, Python Execution time under 1 ms up to 5 or 30 seconds Events viewer request/response all four, including origin Access to network and request body no yes Price very low higher Simple header rewriting, redirects, and cache key normalization — CloudFront Functions; calling an external API, A/B logic, working with the body — Lambda@Edge. Global Accelerator Two static anycast IP addresses; traffic enters the nearest point of presence and then travels over the AWS backbone to the nearest healthy endpoint (ALB, NLB, EC2, Elastic IP). Works with TCP and UDP, so it fits cases where CloudFront can't help: gaming, VoIP, IoT, custom protocols. Properties it's chosen for: static IPs for allowlisting, fast cross Region failover based on health checks (seconds), weighted traffic distribution across Regions, client affinity. CloudFront versus Global Accelerator Requirement Answer Caching HTTP content CloudFront Static IP addresses Global Accelerator UDP or arbitrary TCP Global Accelerator Serving video and static assets CloudFront Instant failover between Regions Global Accelerator Reducing load on the origin CloudFront Dynamic API with high latency due to distance either works: CloudFront terminates TLS closer to the user and keeps a connection to the origin open, GA shortens the path over the backbone Cost CloudFront saves on outbound traffic: serving from cache is cheaper than serving from S3 or EC2, plus there's no origin data transfer charge on a cache hit. Price Class limits the set of edge locations in use and lowers cost when the audience is local. Global Accelerator is billed hourly plus a premium for data transferred. Common patterns Static content in S3 with OAC + dynamic content on an ALB as a second origin, routed by path pattern. API Gateway behind CloudFront with GET caching and WAF on the distribution. Video: S3 as the origin, signed cookies for subscribers, MediaConvert for transcoding. Multi Region fault tolerance: Route 53 failover or Global Accelerator in front of regional load balancers. What the exam asks 1. \"Users worldwide complain about slow image loading\" — CloudFront. 2. \"The bucket must stay private, but content is still served\" — OAC. 3. \"Need fixed IPs for a partner firewall\" — Global Accelerator or NLB. 4. \"A UDP game server with players in three Regions\" — Global Accelerator. 5. \"Paid content is available only to subscribers for 24 hours\" — signed URL or cookies. 6. \"Block access from specific countries\" — geo restriction (or a WAF rule). 7. \"The cache serves an old version of the site after a deploy\" — invalidation or file name versioning. 8. \"The certificate doesn't show up in the CloudFront list\" — it wasn't issued in us east 1. Numbers to remember hundreds of points of presence, cache by Region plus regional edge caches; TTL is set by the cache policy and origin headers, 24 hours by default; invalidation: the first 1000 paths per month are free, then it's billed; CloudFront Functions — under 1 ms and viewer events only, Lambda@Edge — up to 5 seconds on viewer events and up to 30 seconds on origin events; a certificate for a custom domain must come from ACM in us east 1 only; Global Accelerator provides 2 static anycast addresses and switches traffic within seconds. How to read the wording Wording in the question Answer \"global users, static and media content\" CloudFront \"keep the S3 bucket private\" OAC \"static IP addresses for allowlisting\" Global Accelerator or NLB \"UDP or non HTTP protocol\" Global Accelerator \"restrict content to paying subscribers\" signed URL or signed cookies \"block specific countries\" geo restriction or WAF \"fast failover between Regions\" Global Accelerator or Route 53 health checks \"reduce load and egress cost from origin\" CloudFront Checking the cache when walking through a question The cache works well when the cache key is stable: extra headers, cookies, and query parameters in the key fragment the cache and drop the hit rate to zero. Cues in the answer choices: \"forward all headers and cookies\" is almost always a regression, \"cache policy with only required query strings\" is an improvement. Dynamic content benefits from CloudFront too: TLS is terminated near the user, and a persistent connection over the AWS backbone carries traffic to the origin, cutting down session establishment time. Mini practicum 1. A site on an ALB in eu central 1; half the users, in Asia, complain about slow image loading. → CloudFront in front of the ALB and S3: the cache sits closer to the user, and TLS is terminated at the edge. 2. Files from a private bucket need to be served only through the CDN. → Origin Access Control plus a bucket policy that allows access only from the distribution. 3. A partner requires a list of static IP addresses for allowlisting; the application runs over TCP. → Global Accelerator: two static anycast addresses and failover driven by health checks. 4. After a deploy, users see the old version of the JavaScript. → Version the file names or invalidate the paths; also clean up the cache policy while you're at it. Bank audit {{q:647}} — VoIP runs over UDP, and CloudFront only serves HTTP/HTTPS; the correct answer is Global Accelerator, the dump key was corrected. {{q:754}} — disputed: static IP addresses are provided by Global Accelerator, not CloudFront. {{q:971}} — disputed: repeatedly serving the same files to servers across different Regions is CloudFront caching, not Transfer Acceleration. {{q:974}} — the CloudFront certificate always comes from us east 1; the key was corrected to AD. {{q:447}} — CloudFront has no health checks of its own: active active failover between Regions is built on Route 53. "},{"id":"route53","title":"Route 53 — DNS and traffic routing","order":17,"svc":["Route 53"],"questions":[12,29,35,38,56,69,83,120,132,141,142,145,158,166,193,217,224,226,264,272,294,323,367,378,408,434,447,464,466,527,530,532,544,545,582,625,627,642,647,692,700,712,741,744,754,813,815,836,874,876,905,915,927,933,935,955,976,984,1008],"html":"<h1>Route 53</h1>\n<p>A managed DNS service integrated with AWS services, with health checks and seven routing policies. On the exam it's used as a tool for fault tolerance and global traffic distribution, not just as zone hosting.</p>\n<h2>Zones and records</h2>\n<ul><li>a public hosted zone serves names on the internet, a private hosted zone serves them inside specified VPCs;</li><li>record types: A, AAAA, CNAME, MX, TXT, NS, SRV, CAA, PTR;</li><li>an alias record is an AWS extension: it points to an ALB, NLB, CloudFront, an S3 website, API Gateway, Global Accelerator, or another record in the same zone. It's free and works at the domain root (zone apex), where CNAME is not allowed — this is a distinction tested in almost every set;</li><li>TTL determines how quickly clients see a change: TTL is lowered ahead of a migration.</li></ul>\n<h2>Routing policies</h2>\n<div class='table-wrap'><table><thead><tr><th>Policy</th><th>What it does</th><th>Typical requirement</th></tr></thead><tbody><tr><td>Simple</td><td>one record, no health check</td><td>the basic case</td></tr><tr><td>Weighted</td><td>distribution by weight</td><td>canary release, a 10/90 migration</td></tr><tr><td>Latency-based</td><td>the region with the lowest network latency</td><td>\"best performance for global users\"</td></tr><tr><td>Failover</td><td>active-passive based on a health check</td><td>a DR setup with a standby site</td></tr><tr><td>Geolocation</td><td>by the user's country or continent</td><td>content localization, legal restrictions</td></tr><tr><td>Geoproximity</td><td>by distance, with an optional bias</td><td>gradually shifting traffic between regions</td></tr><tr><td>Multivalue answer</td><td>up to 8 healthy records in the response</td><td>simple load balancing without a load balancer</td></tr></tbody></table></div>\n<p>The distinction between latency and geolocation is a recurring trap: \"lowest latency\" means latency-based routing; \"users from Germany must see the German site\" means geolocation.</p>\n<h2>Health checks</h2>\n<p>Three kinds: checking an endpoint over HTTP/HTTPS/TCP, checking the status of other checks (calculated), and checking a CloudWatch alarm. They're used in failover, multivalue, and alongside DNS records for any policy. A health check tied to a CloudWatch alarm is a way to monitor private resources that can't be polled from the internet.</p>\n<p>An active-active setup across regions: a latency policy with a health check in each region; active-passive: a failover policy with a primary that has a health check and a secondary. In this setup, CloudFront does not perform origin health checks across regions — Route 53 does.</p>\n<h2>Domain registration and DNSSEC</h2>\n<p>Route 53 can be both the domain registrar and the zone host at the same time, but these are independent functions: a domain registered with a third-party registrar can happily be served by a zone in Route 53 by changing the NS records. DNSSEC signs the zone and protects against response spoofing; it's enabled on a public hosted zone using a key in KMS.</p>\n<h2>Resolver and hybrid DNS</h2>\n<p>Route 53 Resolver handles name resolution inside a VPC. An inbound endpoint accepts requests from an on-premises network, and an outbound endpoint with rules forwards requests to corporate DNS. Rules are shared across accounts via RAM. Resolver DNS Firewall blocks lookups of malicious domains and forms part of a defense against exfiltration.</p>\n<h2>Fault-tolerance patterns</h2>\n<div class='table-wrap'><table><thead><tr><th>Task</th><th>Construction</th></tr></thead><tbody><tr><td>A standby static site during an outage</td><td>failover to an S3 website or CloudFront</td></tr><tr><td>Load balancing across regions by performance</td><td>latency + health checks</td></tr><tr><td>Gradual migration to a new platform</td><td>weighted, with the weight changing over time</td></tr><tr><td>Data-localization requirements</td><td>geolocation</td></tr><tr><td>Simple load balancing without an ELB</td><td>multivalue answer</td></tr></tbody></table></div>\n<h2>What the exam asks</h2>\n<ol><li>\"The domain root must point to an ALB\" — an alias record, since CNAME on the apex is impossible.</li><li>\"Users must land in the region with the lowest latency\" — latency-based routing.</li><li>\"Fail over to a standby region if the primary fails\" — failover with a health check.</li><li>\"Show different content per country\" — geolocation.</li><li>\"Roll out a new version to 5 percent of traffic\" — weighted.</li><li>\"On-premises servers must resolve a private hosted zone\" — a Resolver inbound endpoint.</li><li>\"The domain is registered with a third-party registrar\" — keep the registrar, switch the NS records to Route 53.</li><li>\"Need to switch traffic fast, but clients cache DNS for a long time\" — lower the TTL in advance or use Global Accelerator with static IPs.</li></ol>\n<h2>Numbers to remember</h2>\n<ul><li>a health check polls the endpoint from multiple points around the world, at a 30- or 10-second interval, and fails over after three consecutive failures;</li><li>multivalue answer returns up to 8 healthy records;</li><li>alias is free; regular queries are billed per million;</li><li>a private hosted zone can be associated with multiple VPCs, including across accounts;</li><li>default TTL is 300 seconds for new records; an alias inherits the TTL of its target;</li><li>Resolver endpoints are billed hourly per IP address.</li></ul>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>In the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"apex domain must point to a load balancer\"</td><td>an alias record</td></tr><tr><td>\"lowest latency for global users\"</td><td>latency-based routing</td></tr><tr><td>\"active-passive disaster recovery\"</td><td>failover routing with a health check</td></tr><tr><td>\"serve different content per country\"</td><td>geolocation</td></tr><tr><td>\"shift 10 percent of traffic to the new stack\"</td><td>weighted routing</td></tr><tr><td>\"on-premises servers must resolve AWS private names\"</td><td>a Resolver inbound endpoint</td></tr><tr><td>\"block malicious domain lookups\"</td><td>Route 53 Resolver DNS Firewall</td></tr><tr><td>\"need static IPs and instant failover\"</td><td>Global Accelerator, not DNS</td></tr></tbody></table></div>\n<h2>Full multi-region patterns</h2>\n<p>Active-passive: the primary region runs the application, the standby is in pilot light or warm standby, and Route 53 failover switches based on the health check. RTO is determined by how long it takes to scale up the standby plus the record's TTL.</p>\n<p>Active-active: both regions serve traffic; a latency policy with health checks distributes users, and data is synchronized via Aurora Global Database, DynamoDB global tables, or S3 replication. This setup costs more, but RTO is measured in minutes or less.</p>\n<p>A separate technique: a maintenance page on S3 as the secondary in a failover record — a cheap way to show users a proper message instead of an error.</p>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> The domain example.com must point to an ALB; CNAME at the root doesn't work. → An alias A record to the load balancer: free and allowed at the zone apex.</p>\n<p><strong>2.</strong> An application is deployed across four regions, and users must land in the fastest one. → Latency-based routing with a health check in each region.</p>\n<p><strong>3.</strong> The primary region goes down; traffic must switch to the standby automatically. → Failover routing: a primary with a health check, and a secondary — either a standby stack or a maintenance page on S3.</p>\n<p><strong>4.</strong> On-premises servers can't resolve RDS names from a private hosted zone. → A Route 53 Resolver inbound endpoint, forwarding requests from the corporate DNS.</p>\n<h2>Related chapters</h2>\n<ul><li>Caching and edge content delivery — the CloudFront chapter.</li><li>Health checks and cross-region failover in DR setups — the recovery chapter.</li><li>Private zones and Resolver in hybrid networks — the hybrid connectivity chapter.</li><li>Load balancing within a region — the load balancers and Auto Scaling chapter.</li><li>Certificates for your own domains — the encryption chapter.</li></ul>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"447\" href=\"#\">questions 447</a> — a multi-region API on API Gateway and Lambda: active-active failover is built on Route 53 health checks; dump key corrected.</li><li><a class=\"qref\" data-q=\"692\" href=\"#\">questions 692</a> — disputed: \"MOST high-performing\" means latency routing; geolocation routes by geography, not by actual latency.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>8</td><td>14%</td><td>1</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>25</td><td>42%</td><td>4</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>20</td><td>34%</td><td>13</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>6</td><td>10%</td><td>1</td></tr></tbody></table></div><p>Total questions in this chapter: <b>59</b>. With a written walkthrough: <b>59</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#83</b> — PERF: speeding up site load times for Europe</li><li><b>#120</b> — PERF: routing to instances across two regions</li><li><b>#166</b> — PERF: serving static content to millions of users</li><li><b>#226</b> — PERF: ingesting data from millions of devices</li><li><b>#264</b> — RES: excluding unhealthy nodes from DNS</li><li><b>#294</b> — SEC: S3 access without internet egress</li><li><b>#434</b> — RES: failover to another region with minimal downtime</li><li><b>#464</b> — RES: eliminating a single point of failure for the database</li></ul>","text":" Route 53 A managed DNS service integrated with AWS services, with health checks and seven routing policies. On the exam it's used as a tool for fault tolerance and global traffic distribution, not just as zone hosting. Zones and records a public hosted zone serves names on the internet, a private hosted zone serves them inside specified VPCs; record types: A, AAAA, CNAME, MX, TXT, NS, SRV, CAA, PTR; an alias record is an AWS extension: it points to an ALB, NLB, CloudFront, an S3 website, API Gateway, Global Accelerator, or another record in the same zone. It's free and works at the domain root (zone apex), where CNAME is not allowed — this is a distinction tested in almost every set; TTL determines how quickly clients see a change: TTL is lowered ahead of a migration. Routing policies Policy What it does Typical requirement Simple one record, no health check the basic case Weighted distribution by weight canary release, a 10/90 migration Latency based the region with the lowest network latency \"best performance for global users\" Failover active passive based on a health check a DR setup with a standby site Geolocation by the user's country or continent content localization, legal restrictions Geoproximity by distance, with an optional bias gradually shifting traffic between regions Multivalue answer up to 8 healthy records in the response simple load balancing without a load balancer The distinction between latency and geolocation is a recurring trap: \"lowest latency\" means latency based routing; \"users from Germany must see the German site\" means geolocation. Health checks Three kinds: checking an endpoint over HTTP/HTTPS/TCP, checking the status of other checks (calculated), and checking a CloudWatch alarm. They're used in failover, multivalue, and alongside DNS records for any policy. A health check tied to a CloudWatch alarm is a way to monitor private resources that can't be polled from the internet. An active active setup across regions: a latency policy with a health check in each region; active passive: a failover policy with a primary that has a health check and a secondary. In this setup, CloudFront does not perform origin health checks across regions — Route 53 does. Domain registration and DNSSEC Route 53 can be both the domain registrar and the zone host at the same time, but these are independent functions: a domain registered with a third party registrar can happily be served by a zone in Route 53 by changing the NS records. DNSSEC signs the zone and protects against response spoofing; it's enabled on a public hosted zone using a key in KMS. Resolver and hybrid DNS Route 53 Resolver handles name resolution inside a VPC. An inbound endpoint accepts requests from an on premises network, and an outbound endpoint with rules forwards requests to corporate DNS. Rules are shared across accounts via RAM. Resolver DNS Firewall blocks lookups of malicious domains and forms part of a defense against exfiltration. Fault tolerance patterns Task Construction A standby static site during an outage failover to an S3 website or CloudFront Load balancing across regions by performance latency + health checks Gradual migration to a new platform weighted, with the weight changing over time Data localization requirements geolocation Simple load balancing without an ELB multivalue answer What the exam asks 1. \"The domain root must point to an ALB\" — an alias record, since CNAME on the apex is impossible. 2. \"Users must land in the region with the lowest latency\" — latency based routing. 3. \"Fail over to a standby region if the primary fails\" — failover with a health check. 4. \"Show different content per country\" — geolocation. 5. \"Roll out a new version to 5 percent of traffic\" — weighted. 6. \"On premises servers must resolve a private hosted zone\" — a Resolver inbound endpoint. 7. \"The domain is registered with a third party registrar\" — keep the registrar, switch the NS records to Route 53. 8. \"Need to switch traffic fast, but clients cache DNS for a long time\" — lower the TTL in advance or use Global Accelerator with static IPs. Numbers to remember a health check polls the endpoint from multiple points around the world, at a 30 or 10 second interval, and fails over after three consecutive failures; multivalue answer returns up to 8 healthy records; alias is free; regular queries are billed per million; a private hosted zone can be associated with multiple VPCs, including across accounts; default TTL is 300 seconds for new records; an alias inherits the TTL of its target; Resolver endpoints are billed hourly per IP address. How to read the wording In the question Answer \"apex domain must point to a load balancer\" an alias record \"lowest latency for global users\" latency based routing \"active passive disaster recovery\" failover routing with a health check \"serve different content per country\" geolocation \"shift 10 percent of traffic to the new stack\" weighted routing \"on premises servers must resolve AWS private names\" a Resolver inbound endpoint \"block malicious domain lookups\" Route 53 Resolver DNS Firewall \"need static IPs and instant failover\" Global Accelerator, not DNS Full multi region patterns Active passive: the primary region runs the application, the standby is in pilot light or warm standby, and Route 53 failover switches based on the health check. RTO is determined by how long it takes to scale up the standby plus the record's TTL. Active active: both regions serve traffic; a latency policy with health checks distributes users, and data is synchronized via Aurora Global Database, DynamoDB global tables, or S3 replication. This setup costs more, but RTO is measured in minutes or less. A separate technique: a maintenance page on S3 as the secondary in a failover record — a cheap way to show users a proper message instead of an error. Mini practicum 1. The domain example.com must point to an ALB; CNAME at the root doesn't work. → An alias A record to the load balancer: free and allowed at the zone apex. 2. An application is deployed across four regions, and users must land in the fastest one. → Latency based routing with a health check in each region. 3. The primary region goes down; traffic must switch to the standby automatically. → Failover routing: a primary with a health check, and a secondary — either a standby stack or a maintenance page on S3. 4. On premises servers can't resolve RDS names from a private hosted zone. → A Route 53 Resolver inbound endpoint, forwarding requests from the corporate DNS. Related chapters Caching and edge content delivery — the CloudFront chapter. Health checks and cross region failover in DR setups — the recovery chapter. Private zones and Resolver in hybrid networks — the hybrid connectivity chapter. Load balancing within a region — the load balancers and Auto Scaling chapter. Certificates for your own domains — the encryption chapter. Bank audit {{q:447}} — a multi region API on API Gateway and Lambda: active active failover is built on Route 53 health checks; dump key corrected. {{q:692}} — disputed: \"MOST high performing\" means latency routing; geolocation routes by geography, not by actual latency. "},{"id":"containers","title":"Containers — ECS, EKS, Fargate","order":18,"svc":["ECS","EKS","Fargate","Elastic Beanstalk"],"questions":[32,41,52,57,58,63,112,128,129,140,143,161,163,181,185,187,190,197,198,220,223,236,263,283,285,303,345,355,371,397,408,422,423,424,427,451,452,457,458,468,475,479,483,486,513,514,516,522,535,541,563,574,575,587,591,599,602,611,613,615,630,636,637,654,663,664,677,689,695,698,699,715,720,721,724,752,758,760,772,775,794,795,802,805,808,814,820,865,871,877,885,892,897,900,904,926,932,941,948,954,968,969,996,1017],"html":"<h1>Containers</h1>\n<p>A container is a way to package an application with its dependencies. In AWS, the question comes down to two axes: which orchestrator (ECS or EKS) and what runs the tasks (EC2 or Fargate).</p>\n<h2>ECS</h2>\n<p>AWS's own orchestrator: a cluster, a task definition (image, CPU, memory, ports, environment variables, roles), a service (desired task count, ELB integration, autoscaling).</p>\n<ul><li>task role — permissions for the application itself; task execution role — permissions for the agent to pull the image from ECR and write logs; the two get confused, and that shows up in questions;</li><li>networking modes: awsvpc gives a task its own ENI and security group (the standard), bridge and host run on EC2;</li><li>capacity providers spread tasks across EC2, Fargate, and Fargate Spot;</li><li>service auto scaling scales on CPU, memory, requests per ALB target, or an arbitrary CloudWatch metric;</li><li>ECS Anywhere runs tasks on your own hardware.</li></ul>\n<h2>EKS</h2>\n<p>Managed Kubernetes: AWS runs the control plane, you run the worker nodes (managed node groups, self-managed, or Fargate). Chosen when you need the Kubernetes ecosystem: existing manifests and Helm charts, portability across clouds, operators, specific controllers.</p>\n<ul><li>IRSA (IAM Roles for Service Accounts) grants permissions to pods via OIDC — the right answer to \"give a pod access to S3 without keys\";</li><li>AWS Load Balancer Controller provisions an ALB for Ingress and an NLB for Service;</li><li>Cluster Autoscaler or Karpenter add nodes, HPA scales pods;</li><li>nodes can run on Spot for batch workloads.</li></ul>\n<h2>Fargate</h2>\n<p>Serverless task execution: you describe CPU and memory, AWS runs the container with no instance management. No host access, no daemonset-style agents on the node, no GPU (use EC2 for GPU), no instance store, but it does offer ephemeral storage and EFS mounting.</p>\n<p>Chosen for wording like \"no servers to manage,\" \"minimize operational overhead,\" \"variable workload.\" The EC2 mode is chosen when you need GPUs, specific instance types, maximum density, or savings on steady long-running load via RI and Savings Plans.</p>\n<h2>Image registry</h2>\n<p>ECR stores images, scans them for vulnerabilities (basic and enhanced scanning via Inspector), supports lifecycle policies to clean up old tags, replication across regions and accounts, and private access via an interface endpoint.</p>\n<h2>Storage and configuration</h2>\n<ul><li>EFS mounts into ECS tasks and EKS pods — shared file access between containers across AZs;</li><li>FSx for Lustre attaches to EKS for heavy compute workloads;</li><li>secrets are injected from Secrets Manager and Parameter Store directly into the task definition;</li><li>logs go to CloudWatch Logs via awslogs, or to other destinations via FireLens.</li></ul>\n<h2>Elastic Beanstalk</h2>\n<p>A platform for deploying an application on standard stacks: it creates EC2, ASG, and ELB for you, and sets up monitoring and deployment. The answer to \"stand up a web application with minimal operational overhead, without rewriting it or learning containers.\" Supports rolling, immutable, and blue/green deployments.</p>\n<h2>What to choose</h2>\n<div class='table-wrap'><table><thead><tr><th>Requirement</th><th>Answer</th></tr></thead><tbody><tr><td>Already have Kubernetes manifests</td><td>EKS</td></tr><tr><td>Just run containers on AWS</td><td>ECS</td></tr><tr><td>Don't want to manage nodes</td><td>Fargate</td></tr><tr><td>Need GPUs or special instance types</td><td>ECS/EKS on EC2</td></tr><tr><td>Scheduled batch jobs and queues</td><td>AWS Batch or ECS Scheduled Tasks</td></tr><tr><td>Java/Python application without containers</td><td>Elastic Beanstalk</td></tr><tr><td>Event once a second, 200 ms of work</td><td>Lambda, no containers needed</td></tr></tbody></table></div>\n<h2>What the exam asks</h2>\n<ol><li>\"Microservices, minimal administration, no Kubernetes experience\" — ECS on Fargate.</li><li>\"A pod needs access to S3 without keys\" — IRSA.</li><li>\"Tasks need a shared volume across AZs\" — EFS.</li><li>\"Reduce the cost of processing a queue with containers\" — Fargate Spot or EC2 Spot in a capacity provider.</li><li>\"Route HTTP traffic to services in EKS\" — ALB via the AWS Load Balancer Controller.</li><li>\"Images must be checked for vulnerabilities\" — ECR scanning with Inspector.</li><li>\"Migrate a docker-compose application to the cloud quickly\" — ECS on Fargate.</li><li>\"Need full control over the nodes and kernel\" — EC2 mode, not Fargate.</li></ol>\n<h2>Numbers to remember</h2>\n<ul><li>Fargate: 0.25 to 16 vCPU, memory from 0.5 GB to 120 GB, ephemeral storage up to 200 GB;</li><li>a task definition stores versions; a service points to a specific revision;</li><li>ECS awsvpc gives a task its own ENI, which consumes the instance's ENI limit;</li><li>EKS: AWS runs the control plane, billed hourly per cluster plus the nodes;</li><li>ECR: images up to 10 GB per layer within limits, lifecycle policies clean up tags;</li><li>Fargate Spot is roughly 70 percent cheaper, and a task can be interrupted.</li></ul>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>Wording in the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"containers without managing servers\"</td><td>Fargate</td></tr><tr><td>\"existing Kubernetes manifests and Helm\"</td><td>EKS</td></tr><tr><td>\"pods need AWS permissions without keys\"</td><td>IRSA</td></tr><tr><td>\"shared storage between tasks across AZs\"</td><td>EFS</td></tr><tr><td>\"GPU workloads in containers\"</td><td>ECS or EKS on EC2</td></tr><tr><td>\"cheap batch container jobs\"</td><td>Fargate Spot or EC2 Spot</td></tr><tr><td>\"deploy a Java app, no container experience\"</td><td>Elastic Beanstalk</td></tr><tr><td>\"route HTTP to services by path\"</td><td>ALB with a target group per service</td></tr></tbody></table></div>\n<h2>Deployments and reliability</h2>\n<p>ECS supports rolling updates via the minimumHealthyPercent and maximumPercent parameters, as well as blue/green deployments through CodeDeploy with target group switching and automatic rollback on alarms. In EKS, the same needs are met by Kubernetes Deployment strategies and progressive-delivery controllers. For fault tolerance, tasks are spread across multiple AZs (ECS's spread placement strategy, Kubernetes' topology spread constraints), and the ALB health check tracks application readiness, not just container startup.</p>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> A team with no Kubernetes experience is migrating a monolith off docker-compose and doesn't want to manage servers. → ECS on Fargate with an ALB in front of the service.</p>\n<p><strong>2.</strong> A pod in EKS needs access to a bucket; keys in the manifest aren't allowed. → IRSA: an IAM role bound to a service account via the cluster's OIDC provider.</p>\n<p><strong>3.</strong> Batch processing with containers runs for hours and can be interrupted. → Fargate Spot or EC2 Spot in a capacity provider, jobs pulled from an SQS queue.</p>\n<p><strong>4.</strong> Images need to be scanned for vulnerabilities on every push. → ECR with enhanced scanning powered by Inspector, findings surfaced in Security Hub.</p>\n<h2>Related chapters</h2>\n<ul><li>Choosing between containers and functions — the Lambda chapter.</li><li>Balancing HTTP traffic and health checks — the load balancers chapter.</li><li>Shared file storage for tasks — the EFS and FSx chapter.</li><li>Permissions for pods and tasks without keys — the IAM chapter.</li><li>Image scanning and perimeter protection — the security chapter.</li></ul>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"591\" href=\"#\">questions 591</a> — disputed: for routing to microservices in EKS, the standard solution is an ALB; API Gateway is usually more expensive.</li><li><a class=\"qref\" data-q=\"422\" href=\"#\">questions 422</a> — low confidence: SQS with ECS Fargate vs. SQS with Lambda, decided by model size and processing time.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>18</td><td>17%</td><td>4</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>21</td><td>20%</td><td>5</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>40</td><td>38%</td><td>34</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>25</td><td>24%</td><td>4</td></tr></tbody></table></div><p>Total questions in this chapter: <b>104</b>. With a written walkthrough: <b>104</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#57</b> — PERF: choosing a managed ML moderation service</li><li><b>#58</b> — PERF: containers without infrastructure management</li><li><b>#112</b> — PERF: elastic platform for growing request volume</li><li><b>#128</b> — COST: interruptible containers without data loss</li><li><b>#129</b> — PERF: removing the scaling ceiling and capacity planning</li><li><b>#143</b> — PERF: breaking a monolith into scalable components</li><li><b>#181</b> — RES: messaging between microservices without ordering</li><li><b>#185</b> — SEC: container permissions for S3 calls</li></ul>","text":" Containers A container is a way to package an application with its dependencies. In AWS, the question comes down to two axes: which orchestrator (ECS or EKS) and what runs the tasks (EC2 or Fargate). ECS AWS's own orchestrator: a cluster, a task definition (image, CPU, memory, ports, environment variables, roles), a service (desired task count, ELB integration, autoscaling). task role — permissions for the application itself; task execution role — permissions for the agent to pull the image from ECR and write logs; the two get confused, and that shows up in questions; networking modes: awsvpc gives a task its own ENI and security group (the standard), bridge and host run on EC2; capacity providers spread tasks across EC2, Fargate, and Fargate Spot; service auto scaling scales on CPU, memory, requests per ALB target, or an arbitrary CloudWatch metric; ECS Anywhere runs tasks on your own hardware. EKS Managed Kubernetes: AWS runs the control plane, you run the worker nodes (managed node groups, self managed, or Fargate). Chosen when you need the Kubernetes ecosystem: existing manifests and Helm charts, portability across clouds, operators, specific controllers. IRSA (IAM Roles for Service Accounts) grants permissions to pods via OIDC — the right answer to \"give a pod access to S3 without keys\"; AWS Load Balancer Controller provisions an ALB for Ingress and an NLB for Service; Cluster Autoscaler or Karpenter add nodes, HPA scales pods; nodes can run on Spot for batch workloads. Fargate Serverless task execution: you describe CPU and memory, AWS runs the container with no instance management. No host access, no daemonset style agents on the node, no GPU (use EC2 for GPU), no instance store, but it does offer ephemeral storage and EFS mounting. Chosen for wording like \"no servers to manage,\" \"minimize operational overhead,\" \"variable workload.\" The EC2 mode is chosen when you need GPUs, specific instance types, maximum density, or savings on steady long running load via RI and Savings Plans. Image registry ECR stores images, scans them for vulnerabilities (basic and enhanced scanning via Inspector), supports lifecycle policies to clean up old tags, replication across regions and accounts, and private access via an interface endpoint. Storage and configuration EFS mounts into ECS tasks and EKS pods — shared file access between containers across AZs; FSx for Lustre attaches to EKS for heavy compute workloads; secrets are injected from Secrets Manager and Parameter Store directly into the task definition; logs go to CloudWatch Logs via awslogs, or to other destinations via FireLens. Elastic Beanstalk A platform for deploying an application on standard stacks: it creates EC2, ASG, and ELB for you, and sets up monitoring and deployment. The answer to \"stand up a web application with minimal operational overhead, without rewriting it or learning containers.\" Supports rolling, immutable, and blue/green deployments. What to choose Requirement Answer Already have Kubernetes manifests EKS Just run containers on AWS ECS Don't want to manage nodes Fargate Need GPUs or special instance types ECS/EKS on EC2 Scheduled batch jobs and queues AWS Batch or ECS Scheduled Tasks Java/Python application without containers Elastic Beanstalk Event once a second, 200 ms of work Lambda, no containers needed What the exam asks 1. \"Microservices, minimal administration, no Kubernetes experience\" — ECS on Fargate. 2. \"A pod needs access to S3 without keys\" — IRSA. 3. \"Tasks need a shared volume across AZs\" — EFS. 4. \"Reduce the cost of processing a queue with containers\" — Fargate Spot or EC2 Spot in a capacity provider. 5. \"Route HTTP traffic to services in EKS\" — ALB via the AWS Load Balancer Controller. 6. \"Images must be checked for vulnerabilities\" — ECR scanning with Inspector. 7. \"Migrate a docker compose application to the cloud quickly\" — ECS on Fargate. 8. \"Need full control over the nodes and kernel\" — EC2 mode, not Fargate. Numbers to remember Fargate: 0.25 to 16 vCPU, memory from 0.5 GB to 120 GB, ephemeral storage up to 200 GB; a task definition stores versions; a service points to a specific revision; ECS awsvpc gives a task its own ENI, which consumes the instance's ENI limit; EKS: AWS runs the control plane, billed hourly per cluster plus the nodes; ECR: images up to 10 GB per layer within limits, lifecycle policies clean up tags; Fargate Spot is roughly 70 percent cheaper, and a task can be interrupted. How to read the wording Wording in the question Answer \"containers without managing servers\" Fargate \"existing Kubernetes manifests and Helm\" EKS \"pods need AWS permissions without keys\" IRSA \"shared storage between tasks across AZs\" EFS \"GPU workloads in containers\" ECS or EKS on EC2 \"cheap batch container jobs\" Fargate Spot or EC2 Spot \"deploy a Java app, no container experience\" Elastic Beanstalk \"route HTTP to services by path\" ALB with a target group per service Deployments and reliability ECS supports rolling updates via the minimumHealthyPercent and maximumPercent parameters, as well as blue/green deployments through CodeDeploy with target group switching and automatic rollback on alarms. In EKS, the same needs are met by Kubernetes Deployment strategies and progressive delivery controllers. For fault tolerance, tasks are spread across multiple AZs (ECS's spread placement strategy, Kubernetes' topology spread constraints), and the ALB health check tracks application readiness, not just container startup. Mini practicum 1. A team with no Kubernetes experience is migrating a monolith off docker compose and doesn't want to manage servers. → ECS on Fargate with an ALB in front of the service. 2. A pod in EKS needs access to a bucket; keys in the manifest aren't allowed. → IRSA: an IAM role bound to a service account via the cluster's OIDC provider. 3. Batch processing with containers runs for hours and can be interrupted. → Fargate Spot or EC2 Spot in a capacity provider, jobs pulled from an SQS queue. 4. Images need to be scanned for vulnerabilities on every push. → ECR with enhanced scanning powered by Inspector, findings surfaced in Security Hub. Related chapters Choosing between containers and functions — the Lambda chapter. Balancing HTTP traffic and health checks — the load balancers chapter. Shared file storage for tasks — the EFS and FSx chapter. Permissions for pods and tasks without keys — the IAM chapter. Image scanning and perimeter protection — the security chapter. Bank audit {{q:591}} — disputed: for routing to microservices in EKS, the standard solution is an ALB; API Gateway is usually more expensive. {{q:422}} — low confidence: SQS with ECS Fargate vs. SQS with Lambda, decided by model size and processing time. "},{"id":"analytics","title":"Analytics and data flows","order":19,"svc":["Kinesis","Glue","Athena","Redshift","EMR","OpenSearch","QuickSight","Lake Formation","ElastiCache"],"questions":[2,7,14,16,24,31,33,40,48,49,51,59,77,90,94,103,107,113,114,117,118,129,134,139,144,152,156,160,164,173,177,192,193,199,201,204,206,209,214,220,225,226,238,250,255,257,258,262,267,268,269,278,280,284,286,292,308,309,311,314,317,320,328,333,337,341,351,357,361,362,373,375,376,386,390,393,402,416,427,431,432,442,443,463,472,473,481,489,490,495,501,515,517,523,524,526,537,547,557,561,565,568,578,587,589,590,593,596,598,603,609,611,615,620,631,633,641,643,652,672,674,676,694,695,703,706,707,711,713,718,726,730,735,755,769,773,776,783,784,788,798,804,812,817,818,821,824,830,850,861,864,865,879,880,888,894,898,910,911,912,915,917,920,931,952,962,966,968,975,990,999,1007,1012],"html":"<h1>Analytics</h1>\n<p>A block with a lot of similar services. It's easiest to work through by following the chain: how data gets into the cloud (ingest) → where it lives (storage) → what processes it (process) → what displays it (visualize).</p>\n<h2>Kinesis</h2>\n<div class='table-wrap'><table><thead><tr><th>Service</th><th>Purpose</th><th>Notable traits</th></tr></thead><tbody><tr><td>Data Streams</td><td>ingest and retention of a stream</td><td>shards or on-demand, retention up to 365 days, multiple consumers, ordering within a shard</td></tr><tr><td>Data Firehose</td><td>delivers a stream to a destination</td><td>no administration, buffering, Lambda transformation, S3/Redshift/OpenSearch/Splunk destinations</td></tr><tr><td>Managed Service for Apache Flink</td><td>stream analytics</td><td>SQL and Flink applications on top of a stream</td></tr><tr><td>Video Streams</td><td>video streams</td><td>ingest from cameras, ML integration</td></tr></tbody></table></div>\n<p>The distinction that gets tested: Data Streams — when you need arbitrary access to the data, several independent consumers, replay, and minimal latency; Firehose — when the task is \"just drop the stream into S3/Redshift/OpenSearch with no code and no servers.\" Firehose buffers (typically 60 seconds or more), so \"near real-time delivery\" fits it, while \"process within milliseconds\" does not.</p>\n<p>SQS solves a different problem in this lineup: a work queue, not a stream for analytics.</p>\n<h2>Storage and catalog</h2>\n<p>S3 is the actual data layer for analytics (a data lake). Format matters: columnar Parquet and ORC cut down the amount of data scanned, which lowers Athena cost and query time; partitioning by date and key cuts it down even further.</p>\n<p>Glue Data Catalog holds table metadata for Athena, EMR, and Redshift Spectrum; Glue Crawler infers the schema automatically; Glue ETL (Spark) and Glue Studio run transformations without servers. Lake Formation adds centralized permissions on tables, columns, and rows on top of the catalog.</p>\n<h2>Processing and queries</h2>\n<div class='table-wrap'><table><thead><tr><th>Service</th><th>Model</th><th>When it's chosen</th></tr></thead><tbody><tr><td>Athena</td><td>SQL over data in S3, pay per byte scanned</td><td>one-off and irregular queries, logs, minimal administration</td></tr><tr><td>Redshift</td><td>MPP warehouse with loaded data</td><td>ongoing heavy analytics, complex joins, BI over terabytes</td></tr><tr><td>Redshift Spectrum</td><td>Redshift queries against data in S3</td><td>hot data in the cluster, cold data in the lake</td></tr><tr><td>EMR</td><td>Hadoop, Spark, Hive, Presto on a cluster</td><td>existing Spark/Hadoop jobs, fine-tuning, ML pipelines</td></tr><tr><td>OpenSearch</td><td>search and log analysis</td><td>full-text search, log dashboards, near real-time</td></tr><tr><td>QuickSight</td><td>BI dashboards</td><td>business-facing visualization, SPICE cache, ML insights</td></tr></tbody></table></div>\n<p>The rule for choosing between Athena and Redshift: if the question says \"ad-hoc,\" \"occasionally,\" \"no infrastructure to manage,\" \"data already in S3\" — Athena; if it says \"complex joins,\" \"consistent high query volume,\" \"data warehouse,\" \"BI team runs reports all day\" — Redshift.</p>\n<h2>Redshift in more detail</h2>\n<p>RA3 nodes separate compute from storage (Managed Storage); Serverless removes the cluster entirely. Features that show up in questions: Concurrency Scaling adds capacity during read spikes, materialized views speed up repeated queries, data sharing gives access to another cluster's data without copying it, cross-Region snapshots are part of a DR plan, and Zero-ETL from Aurora removes the need for a pipeline.</p>\n<h2>ElastiCache in analytics and beyond</h2>\n<p>Redis and Memcached, fully managed. Redis: replication, Multi-AZ with automatic failover, persistence, data structures, pub/sub, sorted sets for leaderboards, clustering. Memcached: a simple cache, multithreading, horizontal scaling, no replication and no persistence.</p>\n<p>Typical answers: caching hot queries in front of RDS, session storage, leaderboards, rate limiting. If you need durability and failover — Redis; if you need the simplest cache with per-node scaling — Memcached.</p>\n<h2>Typical pipelines</h2>\n<ul><li>Application logs → Firehose → S3 → Athena/QuickSight (cheap, near real-time).</li><li>Clicks → Data Streams → Flink → DynamoDB/OpenSearch (real-time analytics).</li><li>Partner files → S3 → Glue Crawler → Glue ETL → Redshift → QuickSight.</li><li>IoT data → IoT Core → Firehose → S3 → Athena.</li><li>Log search and monitoring → OpenSearch with dashboards.</li></ul>\n<h2>What the exam asks</h2>\n<ol><li>\"Query logs in S3 with SQL, no infrastructure\" — Athena.</li><li>\"Ingest a stream and drop it into S3 with no code\" — Firehose.</li><li>\"Several applications reading one stream independently\" — Kinesis Data Streams.</li><li>\"Warehouse for BI under constant load\" — Redshift.</li><li>\"Migrate existing Spark jobs to the cloud\" — EMR.</li><li>\"Full-text search and dashboards over logs\" — OpenSearch.</li><li>\"Reduce Athena query cost\" — Parquet, partitioning, compression.</li><li>\"Grant centralized permissions on lake tables\" — Lake Formation.</li></ol>\n<h2>Numbers to remember</h2>\n<ul><li>Kinesis Data Streams: a shard gives 1 MB/s or 1,000 records/s on write and 2 MB/s on read, records up to 1 MB, retention from 24 hours to 365 days;</li><li>Firehose: buffers by size (1–128 MB) or time (60–900 seconds), so delivery is near real-time, not instant;</li><li>Athena is billed per byte scanned; Parquet and partitioning cut that by a large factor;</li><li>Redshift RA3 separates compute from storage, Concurrency Scaling adds capacity during spikes;</li><li>OpenSearch stores hot, warm (UltraWarm), and cold data at different price points;</li><li>QuickSight SPICE is an in-memory cache for fast dashboards.</li></ul>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>Wording in the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"ad-hoc SQL over data already in S3\"</td><td>Athena</td></tr><tr><td>\"data warehouse, complex joins, BI all day\"</td><td>Redshift</td></tr><tr><td>\"stream to S3 with no code\"</td><td>Kinesis Data Firehose</td></tr><tr><td>\"multiple consumers, replay the stream\"</td><td>Kinesis Data Streams</td></tr><tr><td>\"existing Spark and Hadoop jobs\"</td><td>EMR</td></tr><tr><td>\"full-text search and log dashboards\"</td><td>OpenSearch</td></tr><tr><td>\"reduce Athena cost\"</td><td>Parquet, partitioning, compression</td></tr><tr><td>\"fine-grained permissions on lake tables\"</td><td>Lake Formation</td></tr></tbody></table></div>\n<h2>Telling similar pairs apart</h2>\n<p>Firehose vs. Data Streams: if the data just needs to land in storage with no in-flight processing — Firehose; if several applications read one stream and control over the read position matters — Data Streams.</p>\n<p>Athena vs. Redshift Spectrum: Athena is self-contained and needs no cluster; Spectrum makes sense where a Redshift cluster already exists and you need to reach cold data in S3 in the same query as hot data.</p>\n<p>OpenSearch vs. Athena: OpenSearch keeps an index and answers search queries in milliseconds; Athena scans files and answers in seconds to minutes. Logs for interactive investigation — OpenSearch; infrequent reports — Athena.</p>\n<p>ElastiCache vs. DAX: DAX understands the DynamoDB API and needs no code changes; ElastiCache is general-purpose, but you have to populate and invalidate the cache yourself.</p>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> ALB logs have piled up in S3, and an analyst wants a few queries a week. → Athena over the data in S3, ideally with date partitioning.</p>\n<p><strong>2.</strong> A clickstream needs to land in S3 and simultaneously feed real-time aggregates. → Kinesis Data Streams: one consumer is Firehose to S3, the other is Flink for aggregates.</p>\n<p><strong>3.</strong> Finance builds reports over terabytes of data all workday long. → Redshift (RA3) with materialized views and Concurrency Scaling.</p>\n<p><strong>4.</strong> Engineers need to search application logs with dashboards and alerts. → OpenSearch, with logs delivered via Firehose and UltraWarm for older indices.</p>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"687\" href=\"#\">questions 687</a> — demand forecasting with no ML experience: train a predictor in Amazon Forecast and invoke it from Lambda; the dump key conflated training in SageMaker with the Forecast predictor.</li><li><a class=\"qref\" data-q=\"113\" href=\"#\">questions 113</a> — low confidence: Snowball Edge with later Glue processing vs. compute on the device.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>22</td><td>13%</td><td>9</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>40</td><td>23%</td><td>13</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>73</td><td>42%</td><td>46</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>38</td><td>22%</td><td>4</td></tr></tbody></table></div><p>Total questions in this chapter: <b>173</b>. With a written walkthrough: <b>173</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#2</b> — PERF: choosing an analytics service for data in S3</li><li><b>#16</b> — PERF: choosing a BI platform for a data mart</li><li><b>#31</b> — SEC: enforcing resource tag compliance</li><li><b>#51</b> — RES: event-driven report generation and distribution</li><li><b>#59</b> — PERF: ingesting and processing clickstream data</li><li><b>#90</b> — PERF: script interferes with developers' database usage</li><li><b>#94</b> — PERF: elastic processing under variable load</li><li><b>#103</b> — PERF: preventing duplicate ETL data processing</li></ul>","text":" Analytics A block with a lot of similar services. It's easiest to work through by following the chain: how data gets into the cloud (ingest) → where it lives (storage) → what processes it (process) → what displays it (visualize). Kinesis Service Purpose Notable traits Data Streams ingest and retention of a stream shards or on demand, retention up to 365 days, multiple consumers, ordering within a shard Data Firehose delivers a stream to a destination no administration, buffering, Lambda transformation, S3/Redshift/OpenSearch/Splunk destinations Managed Service for Apache Flink stream analytics SQL and Flink applications on top of a stream Video Streams video streams ingest from cameras, ML integration The distinction that gets tested: Data Streams — when you need arbitrary access to the data, several independent consumers, replay, and minimal latency; Firehose — when the task is \"just drop the stream into S3/Redshift/OpenSearch with no code and no servers.\" Firehose buffers (typically 60 seconds or more), so \"near real time delivery\" fits it, while \"process within milliseconds\" does not. SQS solves a different problem in this lineup: a work queue, not a stream for analytics. Storage and catalog S3 is the actual data layer for analytics (a data lake). Format matters: columnar Parquet and ORC cut down the amount of data scanned, which lowers Athena cost and query time; partitioning by date and key cuts it down even further. Glue Data Catalog holds table metadata for Athena, EMR, and Redshift Spectrum; Glue Crawler infers the schema automatically; Glue ETL (Spark) and Glue Studio run transformations without servers. Lake Formation adds centralized permissions on tables, columns, and rows on top of the catalog. Processing and queries Service Model When it's chosen Athena SQL over data in S3, pay per byte scanned one off and irregular queries, logs, minimal administration Redshift MPP warehouse with loaded data ongoing heavy analytics, complex joins, BI over terabytes Redshift Spectrum Redshift queries against data in S3 hot data in the cluster, cold data in the lake EMR Hadoop, Spark, Hive, Presto on a cluster existing Spark/Hadoop jobs, fine tuning, ML pipelines OpenSearch search and log analysis full text search, log dashboards, near real time QuickSight BI dashboards business facing visualization, SPICE cache, ML insights The rule for choosing between Athena and Redshift: if the question says \"ad hoc,\" \"occasionally,\" \"no infrastructure to manage,\" \"data already in S3\" — Athena; if it says \"complex joins,\" \"consistent high query volume,\" \"data warehouse,\" \"BI team runs reports all day\" — Redshift. Redshift in more detail RA3 nodes separate compute from storage (Managed Storage); Serverless removes the cluster entirely. Features that show up in questions: Concurrency Scaling adds capacity during read spikes, materialized views speed up repeated queries, data sharing gives access to another cluster's data without copying it, cross Region snapshots are part of a DR plan, and Zero ETL from Aurora removes the need for a pipeline. ElastiCache in analytics and beyond Redis and Memcached, fully managed. Redis: replication, Multi AZ with automatic failover, persistence, data structures, pub/sub, sorted sets for leaderboards, clustering. Memcached: a simple cache, multithreading, horizontal scaling, no replication and no persistence. Typical answers: caching hot queries in front of RDS, session storage, leaderboards, rate limiting. If you need durability and failover — Redis; if you need the simplest cache with per node scaling — Memcached. Typical pipelines Application logs → Firehose → S3 → Athena/QuickSight (cheap, near real time). Clicks → Data Streams → Flink → DynamoDB/OpenSearch (real time analytics). Partner files → S3 → Glue Crawler → Glue ETL → Redshift → QuickSight. IoT data → IoT Core → Firehose → S3 → Athena. Log search and monitoring → OpenSearch with dashboards. What the exam asks 1. \"Query logs in S3 with SQL, no infrastructure\" — Athena. 2. \"Ingest a stream and drop it into S3 with no code\" — Firehose. 3. \"Several applications reading one stream independently\" — Kinesis Data Streams. 4. \"Warehouse for BI under constant load\" — Redshift. 5. \"Migrate existing Spark jobs to the cloud\" — EMR. 6. \"Full text search and dashboards over logs\" — OpenSearch. 7. \"Reduce Athena query cost\" — Parquet, partitioning, compression. 8. \"Grant centralized permissions on lake tables\" — Lake Formation. Numbers to remember Kinesis Data Streams: a shard gives 1 MB/s or 1,000 records/s on write and 2 MB/s on read, records up to 1 MB, retention from 24 hours to 365 days; Firehose: buffers by size (1–128 MB) or time (60–900 seconds), so delivery is near real time, not instant; Athena is billed per byte scanned; Parquet and partitioning cut that by a large factor; Redshift RA3 separates compute from storage, Concurrency Scaling adds capacity during spikes; OpenSearch stores hot, warm (UltraWarm), and cold data at different price points; QuickSight SPICE is an in memory cache for fast dashboards. How to read the wording Wording in the question Answer \"ad hoc SQL over data already in S3\" Athena \"data warehouse, complex joins, BI all day\" Redshift \"stream to S3 with no code\" Kinesis Data Firehose \"multiple consumers, replay the stream\" Kinesis Data Streams \"existing Spark and Hadoop jobs\" EMR \"full text search and log dashboards\" OpenSearch \"reduce Athena cost\" Parquet, partitioning, compression \"fine grained permissions on lake tables\" Lake Formation Telling similar pairs apart Firehose vs. Data Streams: if the data just needs to land in storage with no in flight processing — Firehose; if several applications read one stream and control over the read position matters — Data Streams. Athena vs. Redshift Spectrum: Athena is self contained and needs no cluster; Spectrum makes sense where a Redshift cluster already exists and you need to reach cold data in S3 in the same query as hot data. OpenSearch vs. Athena: OpenSearch keeps an index and answers search queries in milliseconds; Athena scans files and answers in seconds to minutes. Logs for interactive investigation — OpenSearch; infrequent reports — Athena. ElastiCache vs. DAX: DAX understands the DynamoDB API and needs no code changes; ElastiCache is general purpose, but you have to populate and invalidate the cache yourself. Mini practicum 1. ALB logs have piled up in S3, and an analyst wants a few queries a week. → Athena over the data in S3, ideally with date partitioning. 2. A clickstream needs to land in S3 and simultaneously feed real time aggregates. → Kinesis Data Streams: one consumer is Firehose to S3, the other is Flink for aggregates. 3. Finance builds reports over terabytes of data all workday long. → Redshift (RA3) with materialized views and Concurrency Scaling. 4. Engineers need to search application logs with dashboards and alerts. → OpenSearch, with logs delivered via Firehose and UltraWarm for older indices. Bank audit {{q:687}} — demand forecasting with no ML experience: train a predictor in Amazon Forecast and invoke it from Lambda; the dump key conflated training in SageMaker with the Forecast predictor. {{q:113}} — low confidence: Snowball Edge with later Glue processing vs. compute on the device. "},{"id":"perimeter-security","title":"Perimeter security and threat detection","order":20,"svc":["WAF","Shield","GuardDuty","Macie","Inspector","Cognito"],"questions":[15,26,35,46,104,116,119,151,159,165,169,170,180,200,213,278,315,319,325,327,329,340,345,359,366,382,390,395,396,399,412,437,457,473,484,495,503,512,529,533,553,608,616,623,624,625,655,682,701,732,733,749,754,787,792,803,808,828,853,867,871,873,901,902,922,924,927,928,961,1011,1018],"html":"<h1>Perimeter security</h1>\n<p>A layer of services that catch attacks, find vulnerabilities, and detect suspicious activity. In questions, they're distinguished by what they protect: traffic, the account, data, instances, and images.</p>\n<h2>WAF</h2>\n<p>Filters HTTP requests at layer 7. It attaches to CloudFront, ALB, API Gateway, AppSync, a Cognito user pool, and App Runner — but not to an NLB. A Web ACL is made up of rules:</p>\n<ul><li>managed rule groups from AWS and partners (OWASP Top 10, bots, known bad addresses);</li><li>custom rules on IP, country, headers, body, length, regular expressions;</li><li>rate-based rules cap the rate from a single address — the answer to \"too many requests from one client\" and to layer-7 floods;</li><li>Bot Control and Fraud Control (protection against credential stuffing and fraudulent sign-ups);</li><li>logging to Firehose, CloudWatch Logs, or S3.</li></ul>\n<p>A requirement to \"block SQL injection and XSS\" points to WAF, not a security group and not a NACL.</p>\n<h2>Shield</h2>\n<p>Shield Standard is included for everyone at no cost and protects against common volumetric attacks at the network and transport layers. Shield Advanced (paid) adds enhanced protection, a 24/7 response team (SRT), cost protection against a traffic-spike bill, detailed visibility, and integration with WAF on managed rules. Phrases like \"dedicated DDoS response team\", \"cost protection\", \"advanced DDoS\" point to Advanced.</p>\n<h2>GuardDuty</h2>\n<p>Continuous threat detection over CloudTrail, VPC Flow Logs, DNS logs, plus EKS logs, S3 events, RDS login logs, EBS snapshots (malware protection), and Lambda. It turns on in minutes, with nothing to deploy on instances. It finds: cryptocurrency mining, calls to C2 servers, anomalous API calls, credential compromise, port scanning.</p>\n<p>Phrases like \"detect unusual activity\", \"compromised credentials\", \"no agents to install\" point to GuardDuty. Automating a response uses EventBridge and Lambda, while Security Hub gives a centralized view of findings.</p>\n<h2>Inspector</h2>\n<p>Scans for vulnerabilities: EC2 (via the SSM agent), images in ECR, and Lambda functions. It runs continuously, matches packages against the CVE database, and scores risk factoring in network reachability. The answer to \"automatically check images and instances for known vulnerabilities\".</p>\n<h2>Macie</h2>\n<p>Looks for sensitive data in S3: personal data, card numbers, access keys, tokens. It builds a bucket inventory with risk scoring (public access, encryption, external access). The phrase \"discover PII in S3\" always points to Macie.</p>\n<h2>Security Hub, Detective, Config</h2>\n<ul><li>Security Hub aggregates findings from GuardDuty, Inspector, Macie, Config, and partner solutions, and checks compliance against standards (CIS, PCI DSS, AWS FSBP);</li><li>Detective builds a graph of related events for incident investigation;</li><li>Config records configurations and compliance rules, and can auto-remediate via SSM;</li><li>CloudTrail records API calls and serves as a source of evidence.</li></ul>\n<h2>Network Firewall and DNS Firewall</h2>\n<p>AWS Network Firewall is a managed stateful firewall at the VPC level, with Suricata rules, domain filtering, and IPS. It's deployed in an inspection VPC together with Transit Gateway or a GWLB. Route 53 Resolver DNS Firewall blocks resolution of malicious domains and hinders data exfiltration over DNS.</p>\n<p>Compared with security groups and NACLs: those work at the ENI and subnet level and don't understand domains, signatures, or content; Network Firewall does.</p>\n<h2>Cognito</h2>\n<p>User pools are an application's user directory: sign-up, sign-in, MFA, password recovery, federation with Google, Apple, Facebook, SAML, and OIDC; they issue JWTs that API Gateway or an ALB validates. Identity pools exchange a token for temporary IAM credentials for direct access to AWS services. The typical answer to \"need to authenticate mobile app users and give them access to S3 on their behalf\".</p>\n<h2>What the exam asks</h2>\n<ol><li>\"A web app is under SQL injection attacks\" — WAF on an ALB or CloudFront.</li><li>\"Need a response team and protection from a DDoS bill spike\" — Shield Advanced.</li><li>\"Find PII in buckets\" — Macie.</li><li>\"Detect credential compromise with no agents\" — GuardDuty.</li><li>\"Check container images for CVEs\" — Inspector with ECR.</li><li>\"A centralized view of compliance against standards\" — Security Hub.</li><li>\"Limit the number of requests from one IP\" — a rate-based rule in WAF.</li><li>\"Filter outbound traffic by domain\" — Network Firewall or DNS Firewall.</li></ol>\n<h2>Numbers to remember</h2>\n<ul><li>WAF attaches to CloudFront, ALB, API Gateway, AppSync, Cognito, App Runner, but not to an NLB;</li><li>a rate-based rule counts requests from one IP over a 5-minute window;</li><li>GuardDuty turns on in minutes and needs no agents; findings go to EventBridge;</li><li>Inspector scans EC2 via the SSM agent, images in ECR, and Lambda functions;</li><li>Macie looks for sensitive data only in S3;</li><li>Shield Standard is free; Advanced has a fixed monthly cost and provides the SRT and cost protection;</li><li>Security Hub aggregates findings, Detective builds an investigation graph.</li></ul>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>In the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"SQL injection, XSS, OWASP\"</td><td>WAF with managed rules</td></tr><tr><td>\"dedicated DDoS response team, cost protection\"</td><td>Shield Advanced</td></tr><tr><td>\"detect compromised credentials, no agents\"</td><td>GuardDuty</td></tr><tr><td>\"find PII stored in S3\"</td><td>Macie</td></tr><tr><td>\"scan container images for CVEs\"</td><td>Inspector</td></tr><tr><td>\"single view of compliance across accounts\"</td><td>Security Hub</td></tr><tr><td>\"limit requests per IP\"</td><td>a rate-based rule</td></tr><tr><td>\"inspect and filter egress traffic by domain\"</td><td>Network Firewall or DNS Firewall</td></tr></tbody></table></div>\n<h2>Incident response</h2>\n<p>A typical automated loop: GuardDuty finds a threat → an event goes to EventBridge → Lambda or SSM Automation isolates the instance (swaps the security group, takes a snapshot, revokes the role's sessions) → a notification goes to SNS → the finding lands in Security Hub. Investigation draws on Detective, CloudTrail, and VPC Flow Logs; recovery draws on snapshots and AWS Backup. In questions, look for the option where the response is automated and doesn't require an on-call engineer to log into the console manually.</p>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> A public API is being brute-forced from a single address range. → WAF with a rate-based rule and an IP rule on CloudFront or an ALB.</p>\n<p><strong>2.</strong> The security team needs to find personal data leaks in buckets. → Macie with recurring discovery jobs and findings surfaced in Security Hub.</p>\n<p><strong>3.</strong> Need to detect mining and calls to C2 servers with no agents installed. → GuardDuty across all accounts in the organization with a delegated administrator.</p>\n<p><strong>4.</strong> Need to block outbound calls to domains outside an allow list. → AWS Network Firewall with domain filtering or Route 53 Resolver DNS Firewall.</p>\n<h2>Related chapters</h2>\n<ul><li>Permissions, roles, and permission boundaries — the IAM chapter.</li><li>Data encryption and key management — the KMS chapter.</li><li>Network boundaries, security groups, and NACLs — the VPC chapter.</li><li>Logging and compliance — the governance chapter.</li><li>Protecting content delivery and WAF on a CDN — the CloudFront chapter.</li></ul>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"754\" href=\"#\">questions 754</a> — disputed: WAF on an ALB works correctly, but the static IP requirement is met by Global Accelerator.</li><li><a class=\"qref\" data-q=\"878\" href=\"#\">questions 878</a> — disputed: sharing a common mailbox with account owners conflicts with a maximum-security requirement.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>62</td><td>87%</td><td>19</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>2</td><td>3%</td><td>0</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>3</td><td>4%</td><td>3</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>4</td><td>6%</td><td>2</td></tr></tbody></table></div><p>Total questions in this chapter: <b>71</b>. With a written walkthrough: <b>71</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#15</b> — SEC: VPC traffic inspection and filtering</li><li><b>#116</b> — PERF: static site instead of a CMS requiring patching</li><li><b>#119</b> — SEC: protecting an API from SQLi and XSS</li><li><b>#170</b> — SEC: access restricted to a single country</li><li><b>#200</b> — SEC: controlling access to a REST API</li><li><b>#213</b> — SEC: filtering application-layer attacks</li><li><b>#315</b> — SEC: active vulnerability scanning of instances</li><li><b>#329</b> — SEC: regular scanning and patching with reporting</li></ul>","text":" Perimeter security A layer of services that catch attacks, find vulnerabilities, and detect suspicious activity. In questions, they're distinguished by what they protect: traffic, the account, data, instances, and images. WAF Filters HTTP requests at layer 7. It attaches to CloudFront, ALB, API Gateway, AppSync, a Cognito user pool, and App Runner — but not to an NLB. A Web ACL is made up of rules: managed rule groups from AWS and partners (OWASP Top 10, bots, known bad addresses); custom rules on IP, country, headers, body, length, regular expressions; rate based rules cap the rate from a single address — the answer to \"too many requests from one client\" and to layer 7 floods; Bot Control and Fraud Control (protection against credential stuffing and fraudulent sign ups); logging to Firehose, CloudWatch Logs, or S3. A requirement to \"block SQL injection and XSS\" points to WAF, not a security group and not a NACL. Shield Shield Standard is included for everyone at no cost and protects against common volumetric attacks at the network and transport layers. Shield Advanced (paid) adds enhanced protection, a 24/7 response team (SRT), cost protection against a traffic spike bill, detailed visibility, and integration with WAF on managed rules. Phrases like \"dedicated DDoS response team\", \"cost protection\", \"advanced DDoS\" point to Advanced. GuardDuty Continuous threat detection over CloudTrail, VPC Flow Logs, DNS logs, plus EKS logs, S3 events, RDS login logs, EBS snapshots (malware protection), and Lambda. It turns on in minutes, with nothing to deploy on instances. It finds: cryptocurrency mining, calls to C2 servers, anomalous API calls, credential compromise, port scanning. Phrases like \"detect unusual activity\", \"compromised credentials\", \"no agents to install\" point to GuardDuty. Automating a response uses EventBridge and Lambda, while Security Hub gives a centralized view of findings. Inspector Scans for vulnerabilities: EC2 (via the SSM agent), images in ECR, and Lambda functions. It runs continuously, matches packages against the CVE database, and scores risk factoring in network reachability. The answer to \"automatically check images and instances for known vulnerabilities\". Macie Looks for sensitive data in S3: personal data, card numbers, access keys, tokens. It builds a bucket inventory with risk scoring (public access, encryption, external access). The phrase \"discover PII in S3\" always points to Macie. Security Hub, Detective, Config Security Hub aggregates findings from GuardDuty, Inspector, Macie, Config, and partner solutions, and checks compliance against standards (CIS, PCI DSS, AWS FSBP); Detective builds a graph of related events for incident investigation; Config records configurations and compliance rules, and can auto remediate via SSM; CloudTrail records API calls and serves as a source of evidence. Network Firewall and DNS Firewall AWS Network Firewall is a managed stateful firewall at the VPC level, with Suricata rules, domain filtering, and IPS. It's deployed in an inspection VPC together with Transit Gateway or a GWLB. Route 53 Resolver DNS Firewall blocks resolution of malicious domains and hinders data exfiltration over DNS. Compared with security groups and NACLs: those work at the ENI and subnet level and don't understand domains, signatures, or content; Network Firewall does. Cognito User pools are an application's user directory: sign up, sign in, MFA, password recovery, federation with Google, Apple, Facebook, SAML, and OIDC; they issue JWTs that API Gateway or an ALB validates. Identity pools exchange a token for temporary IAM credentials for direct access to AWS services. The typical answer to \"need to authenticate mobile app users and give them access to S3 on their behalf\". What the exam asks 1. \"A web app is under SQL injection attacks\" — WAF on an ALB or CloudFront. 2. \"Need a response team and protection from a DDoS bill spike\" — Shield Advanced. 3. \"Find PII in buckets\" — Macie. 4. \"Detect credential compromise with no agents\" — GuardDuty. 5. \"Check container images for CVEs\" — Inspector with ECR. 6. \"A centralized view of compliance against standards\" — Security Hub. 7. \"Limit the number of requests from one IP\" — a rate based rule in WAF. 8. \"Filter outbound traffic by domain\" — Network Firewall or DNS Firewall. Numbers to remember WAF attaches to CloudFront, ALB, API Gateway, AppSync, Cognito, App Runner, but not to an NLB; a rate based rule counts requests from one IP over a 5 minute window; GuardDuty turns on in minutes and needs no agents; findings go to EventBridge; Inspector scans EC2 via the SSM agent, images in ECR, and Lambda functions; Macie looks for sensitive data only in S3; Shield Standard is free; Advanced has a fixed monthly cost and provides the SRT and cost protection; Security Hub aggregates findings, Detective builds an investigation graph. How to read the wording In the question Answer \"SQL injection, XSS, OWASP\" WAF with managed rules \"dedicated DDoS response team, cost protection\" Shield Advanced \"detect compromised credentials, no agents\" GuardDuty \"find PII stored in S3\" Macie \"scan container images for CVEs\" Inspector \"single view of compliance across accounts\" Security Hub \"limit requests per IP\" a rate based rule \"inspect and filter egress traffic by domain\" Network Firewall or DNS Firewall Incident response A typical automated loop: GuardDuty finds a threat → an event goes to EventBridge → Lambda or SSM Automation isolates the instance (swaps the security group, takes a snapshot, revokes the role's sessions) → a notification goes to SNS → the finding lands in Security Hub. Investigation draws on Detective, CloudTrail, and VPC Flow Logs; recovery draws on snapshots and AWS Backup. In questions, look for the option where the response is automated and doesn't require an on call engineer to log into the console manually. Mini practicum 1. A public API is being brute forced from a single address range. → WAF with a rate based rule and an IP rule on CloudFront or an ALB. 2. The security team needs to find personal data leaks in buckets. → Macie with recurring discovery jobs and findings surfaced in Security Hub. 3. Need to detect mining and calls to C2 servers with no agents installed. → GuardDuty across all accounts in the organization with a delegated administrator. 4. Need to block outbound calls to domains outside an allow list. → AWS Network Firewall with domain filtering or Route 53 Resolver DNS Firewall. Related chapters Permissions, roles, and permission boundaries — the IAM chapter. Data encryption and key management — the KMS chapter. Network boundaries, security groups, and NACLs — the VPC chapter. Logging and compliance — the governance chapter. Protecting content delivery and WAF on a CDN — the CloudFront chapter. Bank audit {{q:754}} — disputed: WAF on an ALB works correctly, but the static IP requirement is met by Global Accelerator. {{q:878}} — disputed: sharing a common mailbox with account owners conflicts with a maximum security requirement. "},{"id":"governance","title":"Governance: Organizations, Config, CloudTrail, SSM, Backup","order":21,"svc":["Organizations","Control Tower","Config","CloudTrail","CloudWatch","Systems Manager","Backup","CloudFormation"],"questions":[2,3,4,8,11,13,18,26,27,28,31,34,37,41,50,51,61,62,75,78,82,86,105,109,110,117,136,137,139,144,145,147,150,151,152,157,163,168,178,179,194,196,206,209,210,211,217,219,227,232,248,250,252,253,255,257,259,274,279,297,303,309,312,315,316,318,319,329,335,336,339,368,369,387,390,394,395,399,412,419,426,428,433,434,444,453,454,455,456,459,467,475,479,484,488,492,503,508,512,517,519,521,524,529,534,543,548,553,556,559,560,563,569,586,597,602,613,615,616,618,619,627,628,635,641,653,657,662,665,668,669,671,676,682,688,689,695,707,709,714,723,727,733,742,748,750,757,766,768,769,774,777,786,787,789,793,826,828,842,846,847,849,854,859,861,863,867,869,872,873,874,876,878,880,883,887,889,892,893,901,907,908,910,923,924,926,927,936,937,942,945,947,951,955,959,968,969,970,978,981,988,989,994,1001,1006,1008,1018],"html":"<h1>Governance and observability</h1>\n<p>This block covers how to keep the landscape under control: account structure, logging, compliance, operational automation, and backup.</p>\n<h2>Organizations and Control Tower</h2>\n<p>Organizations groups accounts into an OU tree with a consolidated bill, SCPs, and shared services (a CloudTrail organization trail, a Config aggregator, RAM). Consolidated billing gives volume discounts and shared Reserved Instances and Savings Plans across the whole organization — the standard answer to \"lower the combined cost across several accounts.\"</p>\n<p>Control Tower deploys a landing zone: OUs by purpose, log archive and audit accounts, guardrails (preventive ones built on SCPs, detective ones built on Config), and Account Factory for issuing new accounts to a standard. The phrase \"new accounts must automatically comply\" points to Control Tower, not manual policies.</p>\n<h2>CloudTrail</h2>\n<p>A log of API calls: who, when, from where, with what parameters. Management events are written to Event history for free and kept for 90 days; for longer retention you create a trail into S3 — ideally an organization trail, in a separate account, with file integrity validation and KMS encryption turned on. Data events (operations on S3 objects, Lambda invocations) aren't logged by default and have to be enabled separately — a detail that frequently shows up as \"we can't see who deleted the object.\"</p>\n<p>CloudTrail Lake stores events and lets you query them with a SQL-like language; integration with Athena solves the same problem on top of S3.</p>\n<h2>Config</h2>\n<p>Records resource configuration over time and evaluates it against rules: \"volumes must be encrypted,\" \"buckets must not be public,\" \"instances must have an Owner tag.\" It can auto-remediate through SSM Automation. Conformance packs bundle rule sets for standards, and an aggregator rolls up the picture across all accounts and Regions.</p>\n<p>The difference from CloudTrail: CloudTrail answers \"who did this,\" Config answers \"what state is the resource in now, and what was it before.\" The difference from preventive control: Config detects a violation after the fact, while SCPs and secure defaults stop it from happening at all.</p>\n<h2>CloudWatch</h2>\n<ul><li>metrics (standard and custom), alarms, composite alarms;</li><li>Logs: log groups, streams, metric filters on text patterns, Logs Insights for queries, subscriptions for streaming delivery to Firehose or Lambda;</li><li>the CloudWatch agent collects memory, disk, and log data from EC2 and on-premises servers;</li><li>dashboards, Container Insights, Lambda Insights, Application Signals;</li><li>EventBridge (formerly CloudWatch Events) reacts to events and schedules.</li></ul>\n<p>An alarm with an action — an SNS notification, Auto Scaling, instance recovery, a Lambda invocation through EventBridge — is the standard answer to \"react automatically.\"</p>\n<h2>Systems Manager</h2>\n<p>A toolset for operations: Session Manager (access without SSH or a bastion), Patch Manager (maintenance windows and patch reports), Run Command, State Manager, Automation (runbooks), Inventory, Parameter Store, Fleet Manager. It works on both EC2 and on-premises servers through an agent.</p>\n<p>Three answers that come up constantly: secure access to instances — Session Manager; centralized OS updates — Patch Manager; configuration storage — Parameter Store.</p>\n<h2>AWS Backup</h2>\n<p>Centralized backup policies for EBS, EC2, RDS, Aurora, DynamoDB, EFS, FSx, Storage Gateway, S3, and other services. Plans define the schedule, retention period, transition to cold storage, and copying to another Region and account. Vault Lock makes copies immutable (WORM) — the answer to a regulatory requirement that \"backups cannot be deleted before their retention period.\" Backup Audit Manager checks that every resource is covered by a policy.</p>\n<h2>Infrastructure as code</h2>\n<p>CloudFormation describes resources in a template and supports change sets, drift detection, nested stacks, and StackSets for deploying across many accounts and Regions. CDK generates templates from code in a familiar language. Service Catalog publishes approved products that teams can deploy without permissions on the underlying services — the answer to \"let teams create only approved configurations.\"</p>\n<h2>Tags and cost tracking</h2>\n<p>Tags are the foundation for cost allocation, ABAC access, automation, and search. Tag Editor and Tag Policies in Organizations keep tagging consistent; activated cost allocation tags show up in Cost Explorer reports and the Cost and Usage Report.</p>\n<h2>What the exam asks</h2>\n<ol><li>\"Who deleted the object in the bucket\" — CloudTrail data events for S3.</li><li>\"Make sure all volumes are encrypted, and fix violations\" — Config with remediation.</li><li>\"New accounts must be created to a standard\" — Control Tower Account Factory.</li><li>\"Patch hundreds of instances on a schedule\" — Patch Manager.</li><li>\"Unified backups across services, retained for 7 years\" — AWS Backup with Vault Lock.</li><li>\"Deploy the same infrastructure across 20 accounts\" — CloudFormation StackSets.</li><li>\"Application logs need to be analyzed and alerted on by pattern\" — CloudWatch Logs with a metric filter and an alarm.</li><li>\"Block Regions outside the EU across all accounts\" — SCP.</li></ol>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>Wording in the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"who deleted the object\"</td><td>CloudTrail data events</td></tr><tr><td>\"resource must always be compliant, fix automatically\"</td><td>Config with remediation</td></tr><tr><td>\"prevent the action entirely\"</td><td>SCP or a secure default, not Config</td></tr><tr><td>\"new accounts follow the standard\"</td><td>Control Tower Account Factory</td></tr><tr><td>\"patch hundreds of instances\"</td><td>Patch Manager</td></tr><tr><td>\"backups immutable for seven years\"</td><td>AWS Backup with Vault Lock</td></tr><tr><td>\"same stack in 20 accounts and Regions\"</td><td>CloudFormation StackSets</td></tr><tr><td>\"teams deploy only approved configurations\"</td><td>Service Catalog</td></tr></tbody></table></div>\n<h2>Centralized logging</h2>\n<p>The standard pattern: an organization CloudTrail trail writes to a bucket in the security account, with validation and Object Lock turned on; VPC Flow Logs and application logs are collected into CloudWatch Logs with a subscription to Firehose that delivers into the same bucket; only the security team has access to the bucket, and application accounts can't delete objects. This separation is the typical correct answer in questions about protecting evidence from a rogue administrator.</p>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> You need to prove to a regulator who accessed bucket objects and when. → CloudTrail data events for S3, written to a separate bucket in the security account with integrity validation.</p>\n<p><strong>2.</strong> Public buckets keep appearing across the organization. → Preventively: Block Public Access at the account level plus SCPs; detectively: a Config rule with automatic remediation.</p>\n<p><strong>3.</strong> You need to roll out the same set of roles and logging into 25 accounts. → CloudFormation StackSets through Organizations.</p>\n<p><strong>4.</strong> Backups must be retained for 7 years and must not be deletable even by an administrator. → AWS Backup with Vault Lock in compliance mode, plus a copy in another Region.</p>\n<h2>Related chapters</h2>\n<ul><li>Organization-level restrictions and SCPs — the IAM chapter.</li><li>Threat detection and finding aggregation — the perimeter security chapter.</li><li>Backups for specific services — the EBS, RDS, and file storage chapters.</li><li>Metrics and alarms for Auto Scaling — the load balancers chapter.</li><li>Cost reports and allocation tags — the cost chapter.</li></ul>\n<h2>Numbers to remember</h2>\n<ul><li>an SCP doesn't apply to the organization's management account, and up to 5 policies can be attached to a single node in the hierarchy;</li><li>a CloudTrail event typically shows up in the log within 15 minutes; the console's Event history keeps 90 days, anything beyond that requires writing to S3;</li><li>data events (access to S3 objects, function invocations) are off by default and billed separately;</li><li>basic EC2 monitoring publishes metrics every 5 minutes, detailed monitoring every minute;</li><li>CloudWatch metrics are retained for 15 months, with progressively coarser resolution over time;</li><li>CloudWatch Logs log groups have no default retention limit — it must be set explicitly;</li><li>AWS Backup Vault Lock in compliance mode becomes irreversible after a three-day maturation period; in governance mode it can be lifted by a user with special permission;</li><li>Config keeps a history of resource configurations and lets you define compliance rules, including ones with automatic remediation.</li></ul>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"893\" href=\"#\">questions 893</a> — automatic guardrails on account creation come from Control Tower; the dump key naming Organizations was corrected.</li><li><a class=\"qref\" data-q=\"887\" href=\"#\">questions 887</a> — Config detects but doesn't prevent: blocking unencrypted volumes is done with an account setting.</li><li><a class=\"qref\" data-q=\"308\" href=\"#\">questions 308</a> — low confidence: Trusted Advisor recommendations for RDS Reserved Instances also lower cost.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>97</td><td>47%</td><td>29</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>44</td><td>21%</td><td>16</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>24</td><td>12%</td><td>15</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>42</td><td>20%</td><td>9</td></tr></tbody></table></div><p>Total questions in this chapter: <b>207</b>. With a written walkthrough: <b>207</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#2</b> — PERF: choosing an analytics service for data in S3</li><li><b>#3</b> — SEC: restricting bucket access to the organization</li><li><b>#28</b> — SEC: single sign-on and federation with AD</li><li><b>#31</b> — SEC: enforcing resource tag compliance</li><li><b>#50</b> — SEC: urgent vulnerability patching</li><li><b>#51</b> — RES: event-driven report generation and distribution</li><li><b>#75</b> — RES: transactions lost under tier overload</li><li><b>#109</b> — SEC: preventing object modification and controlling deletion</li></ul>","text":" Governance and observability This block covers how to keep the landscape under control: account structure, logging, compliance, operational automation, and backup. Organizations and Control Tower Organizations groups accounts into an OU tree with a consolidated bill, SCPs, and shared services (a CloudTrail organization trail, a Config aggregator, RAM). Consolidated billing gives volume discounts and shared Reserved Instances and Savings Plans across the whole organization — the standard answer to \"lower the combined cost across several accounts.\" Control Tower deploys a landing zone: OUs by purpose, log archive and audit accounts, guardrails (preventive ones built on SCPs, detective ones built on Config), and Account Factory for issuing new accounts to a standard. The phrase \"new accounts must automatically comply\" points to Control Tower, not manual policies. CloudTrail A log of API calls: who, when, from where, with what parameters. Management events are written to Event history for free and kept for 90 days; for longer retention you create a trail into S3 — ideally an organization trail, in a separate account, with file integrity validation and KMS encryption turned on. Data events (operations on S3 objects, Lambda invocations) aren't logged by default and have to be enabled separately — a detail that frequently shows up as \"we can't see who deleted the object.\" CloudTrail Lake stores events and lets you query them with a SQL like language; integration with Athena solves the same problem on top of S3. Config Records resource configuration over time and evaluates it against rules: \"volumes must be encrypted,\" \"buckets must not be public,\" \"instances must have an Owner tag.\" It can auto remediate through SSM Automation. Conformance packs bundle rule sets for standards, and an aggregator rolls up the picture across all accounts and Regions. The difference from CloudTrail: CloudTrail answers \"who did this,\" Config answers \"what state is the resource in now, and what was it before.\" The difference from preventive control: Config detects a violation after the fact, while SCPs and secure defaults stop it from happening at all. CloudWatch metrics (standard and custom), alarms, composite alarms; Logs: log groups, streams, metric filters on text patterns, Logs Insights for queries, subscriptions for streaming delivery to Firehose or Lambda; the CloudWatch agent collects memory, disk, and log data from EC2 and on premises servers; dashboards, Container Insights, Lambda Insights, Application Signals; EventBridge (formerly CloudWatch Events) reacts to events and schedules. An alarm with an action — an SNS notification, Auto Scaling, instance recovery, a Lambda invocation through EventBridge — is the standard answer to \"react automatically.\" Systems Manager A toolset for operations: Session Manager (access without SSH or a bastion), Patch Manager (maintenance windows and patch reports), Run Command, State Manager, Automation (runbooks), Inventory, Parameter Store, Fleet Manager. It works on both EC2 and on premises servers through an agent. Three answers that come up constantly: secure access to instances — Session Manager; centralized OS updates — Patch Manager; configuration storage — Parameter Store. AWS Backup Centralized backup policies for EBS, EC2, RDS, Aurora, DynamoDB, EFS, FSx, Storage Gateway, S3, and other services. Plans define the schedule, retention period, transition to cold storage, and copying to another Region and account. Vault Lock makes copies immutable (WORM) — the answer to a regulatory requirement that \"backups cannot be deleted before their retention period.\" Backup Audit Manager checks that every resource is covered by a policy. Infrastructure as code CloudFormation describes resources in a template and supports change sets, drift detection, nested stacks, and StackSets for deploying across many accounts and Regions. CDK generates templates from code in a familiar language. Service Catalog publishes approved products that teams can deploy without permissions on the underlying services — the answer to \"let teams create only approved configurations.\" Tags and cost tracking Tags are the foundation for cost allocation, ABAC access, automation, and search. Tag Editor and Tag Policies in Organizations keep tagging consistent; activated cost allocation tags show up in Cost Explorer reports and the Cost and Usage Report. What the exam asks 1. \"Who deleted the object in the bucket\" — CloudTrail data events for S3. 2. \"Make sure all volumes are encrypted, and fix violations\" — Config with remediation. 3. \"New accounts must be created to a standard\" — Control Tower Account Factory. 4. \"Patch hundreds of instances on a schedule\" — Patch Manager. 5. \"Unified backups across services, retained for 7 years\" — AWS Backup with Vault Lock. 6. \"Deploy the same infrastructure across 20 accounts\" — CloudFormation StackSets. 7. \"Application logs need to be analyzed and alerted on by pattern\" — CloudWatch Logs with a metric filter and an alarm. 8. \"Block Regions outside the EU across all accounts\" — SCP. How to read the wording Wording in the question Answer \"who deleted the object\" CloudTrail data events \"resource must always be compliant, fix automatically\" Config with remediation \"prevent the action entirely\" SCP or a secure default, not Config \"new accounts follow the standard\" Control Tower Account Factory \"patch hundreds of instances\" Patch Manager \"backups immutable for seven years\" AWS Backup with Vault Lock \"same stack in 20 accounts and Regions\" CloudFormation StackSets \"teams deploy only approved configurations\" Service Catalog Centralized logging The standard pattern: an organization CloudTrail trail writes to a bucket in the security account, with validation and Object Lock turned on; VPC Flow Logs and application logs are collected into CloudWatch Logs with a subscription to Firehose that delivers into the same bucket; only the security team has access to the bucket, and application accounts can't delete objects. This separation is the typical correct answer in questions about protecting evidence from a rogue administrator. Mini practicum 1. You need to prove to a regulator who accessed bucket objects and when. → CloudTrail data events for S3, written to a separate bucket in the security account with integrity validation. 2. Public buckets keep appearing across the organization. → Preventively: Block Public Access at the account level plus SCPs; detectively: a Config rule with automatic remediation. 3. You need to roll out the same set of roles and logging into 25 accounts. → CloudFormation StackSets through Organizations. 4. Backups must be retained for 7 years and must not be deletable even by an administrator. → AWS Backup with Vault Lock in compliance mode, plus a copy in another Region. Related chapters Organization level restrictions and SCPs — the IAM chapter. Threat detection and finding aggregation — the perimeter security chapter. Backups for specific services — the EBS, RDS, and file storage chapters. Metrics and alarms for Auto Scaling — the load balancers chapter. Cost reports and allocation tags — the cost chapter. Numbers to remember an SCP doesn't apply to the organization's management account, and up to 5 policies can be attached to a single node in the hierarchy; a CloudTrail event typically shows up in the log within 15 minutes; the console's Event history keeps 90 days, anything beyond that requires writing to S3; data events (access to S3 objects, function invocations) are off by default and billed separately; basic EC2 monitoring publishes metrics every 5 minutes, detailed monitoring every minute; CloudWatch metrics are retained for 15 months, with progressively coarser resolution over time; CloudWatch Logs log groups have no default retention limit — it must be set explicitly; AWS Backup Vault Lock in compliance mode becomes irreversible after a three day maturation period; in governance mode it can be lifted by a user with special permission; Config keeps a history of resource configurations and lets you define compliance rules, including ones with automatic remediation. Bank audit {{q:893}} — automatic guardrails on account creation come from Control Tower; the dump key naming Organizations was corrected. {{q:887}} — Config detects but doesn't prevent: blocking unencrypted volumes is done with an account setting. {{q:308}} — low confidence: Trusted Advisor recommendations for RDS Reserved Instances also lower cost. "},{"id":"cost","title":"Cost and optimization","order":22,"svc":["Cost Explorer","Budgets","Organizations"],"questions":[3,24,28,31,137,151,168,227,238,284,412,419,433,455,459,467,484,488,492,512,521,525,543,548,559,560,586,619,628,641,653,657,662,665,668,671,688,689,709,715,733,748,750,777,781,787,789,826,828,859,872,878,880,883,893,908,924,943,945,981,988,1018],"html":"<h1>Cost</h1>\n<p>The \"Design Cost-Optimized Architectures\" domain accounts for a fifth of the exam, but cost considerations show up in nearly every question: phrasing like \"MOST cost-effective,\" \"minimize cost,\" \"as low as possible\" decides between technically equivalent options.</p>\n<h2>What makes up the bill</h2>\n<div class='table-wrap'><table><thead><tr><th>Category</th><th>What's billed</th><th>How it's reduced</th></tr></thead><tbody><tr><td>Compute</td><td>instance hours or seconds, Lambda GB-seconds, Aurora ACUs</td><td>Savings Plans, RI, Spot, right sizing, shutting down idle resources</td></tr><tr><td>Storage</td><td>GB per month by class, snapshots, backups</td><td>storage classes, lifecycle, deleting old snapshots</td></tr><tr><td>Traffic</td><td>outbound to the internet, between AZs, between regions, through NAT</td><td>CloudFront, endpoints, traffic locality</td></tr><tr><td>Requests and operations</td><td>API calls, RCU/WCU, load balancer LCUs, KMS requests</td><td>batching, caching, Bucket Keys</td></tr><tr><td>Managed services</td><td>hourly rates for NAT, TGW attachments, interface endpoints</td><td>consolidation, dropping unnecessary components</td></tr></tbody></table></div>\n<p>Inbound traffic into AWS is free; outbound traffic costs money. Cross-AZ traffic is billed in both directions; traffic within a single AZ over private addresses is free. That explains a good half of the \"cheap\" answers: removing cross-zone hops, serving content through CloudFront, replacing NAT with a gateway endpoint.</p>\n<h2>Compute pricing models</h2>\n<p>Quick guide to the choice:</p>\n<ul><li>steady 24/7 workload → Savings Plans (Compute for flexibility, EC2 Instance for the maximum discount) or Reserved Instances;</li><li>interruptible batch jobs → Spot, up to 90 percent cheaper;</li><li>infrequent short-lived tasks → Lambda or Fargate, you pay only for execution;</li><li>development environments → a start/stop schedule (Instance Scheduler), Aurora Serverless v2, stopping RDS;</li><li>spikes on top of a baseline → On-Demand or Spot layered on top of a reserved base.</li></ul>\n<h2>Tools</h2>\n<ul><li>Cost Explorer — analyzes and forecasts spend, breaks it down by service, tag, and account, and recommends RI and Savings Plans;</li><li>AWS Budgets — budgets by cost, usage, or RI coverage, with notifications and automated actions (for example, applying a restrictive policy);</li><li>Cost and Usage Report — line-item detail in S3 for analysis in Athena and QuickSight;</li><li>Compute Optimizer — recommendations on instance types, EBS volumes, Lambda functions, and ASGs based on actual metrics;</li><li>Trusted Advisor — checks across five categories, including idle resources and unused RIs (the full set of checks requires Business or Enterprise support);</li><li>S3 Storage Lens and Storage Class Analysis — what to move to colder storage classes;</li><li>Billing Conductor and cost allocation tags — splitting spend across teams.</li></ul>\n<p>Division of labor: Budgets warns and acts on a threshold, Cost Explorer explains the past and forecasts the future, Compute Optimizer says what to downsize, Trusted Advisor finds the obvious waste.</p>\n<h2>Common architecture findings</h2>\n<div class='table-wrap'><table><thead><tr><th>Symptom</th><th>Cheap fix</th></tr></thead><tbody><tr><td>Traffic from private subnets to S3 goes through NAT</td><td>gateway endpoint</td></tr><tr><td>One NAT Gateway serves three AZs</td><td>a NAT per AZ (less cross-AZ traffic)</td></tr><tr><td>Serving static content from EC2</td><td>S3 plus CloudFront</td></tr><tr><td>Logs and archives have sat in S3 Standard for years</td><td>lifecycle into Glacier or Deep Archive</td></tr><tr><td>Snapshots pile up with no policy</td><td>Data Lifecycle Manager, AWS Backup</td></tr><tr><td>Dev instances run overnight and on weekends</td><td>a stop schedule</td></tr><tr><td>Oversized instances</td><td>Compute Optimizer, move to gp3, T-family</td></tr><tr><td>Lots of small objects in Standard-IA</td><td>move back to Standard or aggregate them</td></tr><tr><td>Separate accounts buying RIs independently</td><td>consolidated billing in Organizations</td></tr><tr><td>A database cluster sits idle at night</td><td>Aurora Serverless v2 or stopping it</td></tr></tbody></table></div>\n<h2>The cost of reliability</h2>\n<p>The exam often pits cost against fault tolerance. Rules of thumb: One Zone classes and a single AZ are cheaper but don't survive a zone outage; pilot light is cheaper than warm standby but recovers slower; replication to another region always adds both storage and traffic cost. If a question contains both \"cost-effective\" and \"highly available,\" pick the cheapest option among those that actually meet the availability requirement — not the cheapest option overall.</p>\n<h2>What the exam asks</h2>\n<ol><li>\"Reduce the cost of a steady round-the-clock workload\" — Savings Plans or RI.</li><li>\"Processing can be interrupted\" — Spot.</li><li>\"Data is read once a year, retrieval within hours is acceptable\" — Glacier Deep Archive.</li><li>\"The NAT bill is too high\" — endpoints for S3 and DynamoDB.</li><li>\"Need a warning when the budget is exceeded\" — AWS Budgets with SNS.</li><li>\"Understand which instances are oversized\" — Compute Optimizer.</li><li>\"Split spend across teams\" — cost allocation tags and Cost Explorer.</li><li>\"Many accounts, want shared discounts\" — Organizations with consolidated billing.</li></ol>\n<h2>How to read the wording</h2>\n<div class='table-wrap'><table><thead><tr><th>Wording in the question</th><th>Answer</th></tr></thead><tbody><tr><td>\"steady 24/7 workload, lowest cost\"</td><td>Savings Plans or RI</td></tr><tr><td>\"fault-tolerant batch jobs\"</td><td>Spot</td></tr><tr><td>\"accessed once a year, retrieval in hours is fine\"</td><td>Glacier Deep Archive</td></tr><tr><td>\"NAT gateway data processing charges\"</td><td>gateway endpoints for S3 and DynamoDB</td></tr><tr><td>\"alert when spending exceeds a threshold\"</td><td>AWS Budgets</td></tr><tr><td>\"which instances are oversized\"</td><td>Compute Optimizer</td></tr><tr><td>\"allocate costs to teams\"</td><td>cost allocation tags and Cost Explorer</td></tr><tr><td>\"multiple accounts, volume discounts\"</td><td>Organizations with consolidated billing</td></tr></tbody></table></div>\n<h2>Order of operations for optimizing an architecture</h2>\n<ol><li>Remove idle resources: unattached Elastic IPs, old snapshots, unused volumes, test environments running overnight.</li><li>Right-size: Compute Optimizer, gp3 instead of gp2, current instance generations.</li><li>Cover the baseline load with commitments: Savings Plans, RI, leaving spikes to On-Demand and Spot.</li><li>Move data to the right storage class and turn on lifecycle policies.</li><li>Cut unnecessary traffic: CloudFront for delivery, endpoints instead of NAT, AZ locality.</li><li>Replace self-managed components with managed ones wherever that's cheaper on a total cost of ownership basis (RDS instead of a database on EC2, Fargate instead of a node cluster).</li><li>Put controls in place: budgets, tags, regular review with Cost Explorer and Trusted Advisor.</li></ol>\n<h2>Mini practicum</h2>\n<p><strong>1.</strong> The NAT Gateway bill exceeds the cost of the instances themselves; most of the traffic goes to S3. → A gateway endpoint for S3: traffic stops going through NAT and isn't billed.</p>\n<p><strong>2.</strong> Test environments run around the clock but are only used from 9 to 6 on weekdays. → A start/stop schedule (Instance Scheduler or EventBridge with SSM Automation).</p>\n<p><strong>3.</strong> Five accounts buy reservations separately and don't fully utilize them. → An organization with consolidated billing: discounts and RI coverage are shared across accounts.</p>\n<p><strong>4.</strong> Five years of log archives sit in S3 Standard, read once a year for an audit request. → Lifecycle into Glacier Deep Archive, if retrieval within hours is acceptable.</p>\n<h2>Related chapters</h2>\n<ul><li>Compute purchasing models — the EC2 chapter.</li><li>Storage classes and lifecycle — the S3 chapter.</li><li>Traffic, NAT, and endpoints — the VPC chapter.</li><li>Caching and reducing outbound traffic — the CloudFront chapter.</li><li>Serverless database options — the Aurora and DynamoDB chapters.</li></ul>\n<h2>Wording traps</h2>\n<p>The word \"cost-effective\" in a question doesn't mean \"the cheapest option on the list\": it means \"the cheapest option among those that meet all the other requirements.\" If \"highly available\" or \"no data loss\" appears alongside it, single-AZ options with no replication are eliminated, even if they're cheaper.</p>\n<p>The reverse trap is \"operational overhead.\" A managed service is often more expensive on the bill, but wins when the requirement is phrased around effort, not money. Read carefully what's actually supposed to be minimized: cost, recovery time, or the amount of administration.</p>\n<h2>Numbers to remember</h2>\n<ul><li>commitments run for 1 or 3 years: Compute Savings Plans up to 66% savings, EC2 Instance Savings Plans and standard Reserved Instances up to 72%, convertible ones around 54%, Spot Instances up to 90%;</li><li>all upfront is cheaper than partial upfront, partial upfront is cheaper than no upfront;</li><li>cost data refreshes no more than once a day, so no tool gives instant spend visibility;</li><li>Cost Explorer shows 12 months of history and forecasts 12 months ahead; hourly granularity and resource-level data are opt-in add-ons that cost extra;</li><li>the first two budgets in AWS Budgets are free; each additional budget is billed per day;</li><li>cost allocation tags must be activated manually and don't work retroactively: they only appear in reports from the moment of activation;</li><li>cross-AZ traffic within a region is billed; traffic within a single AZ over private addresses is free;</li><li>a NAT gateway is billed both hourly and per gigabyte, which is why cost-saving questions often replace it with a gateway endpoint.</li></ul>\n<h2>Bank audit</h2>\n<ul><li><a class=\"qref\" data-q=\"245\" href=\"#\">questions 245</a> — disputed: cutting cost on a dev environment; changing the target group doesn't reduce the ASG's instance count.</li><li><a class=\"qref\" data-q=\"308\" href=\"#\">questions 308</a> — low confidence: Trusted Advisor recommendations on RDS RIs.</li><li><a class=\"qref\" data-q=\"574\" href=\"#\">questions 574</a> — disputed: two hours of use per week; Serverless v2 vs. stopping a provisioned cluster.</li><li><a class=\"qref\" data-q=\"851\" href=\"#\">questions 851</a> — disputed: Aurora Serverless scales down when idle.</li><li><a class=\"qref\" data-q=\"835\" href=\"#\">questions 835</a> — disputed: public VIF vs. an interface endpoint for access to S3.</li></ul><h2>Domain breakdown for this chapter's questions</h2><p>This block is generated automatically when the handbook is built, from the classified bank. The “manual” column shows how many of the chapter's questions got their domain not from the <code>classify.py</code> rules but from a by-hand reading of the wording.</p><div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Questions</th><th>Share of chapter</th><th>Manual</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>35</td><td>56%</td><td>15</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>1</td><td>2%</td><td>0</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>1</td><td>2%</td><td>1</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>25</td><td>40%</td><td>4</td></tr></tbody></table></div><p>Total questions in this chapter: <b>62</b>. With a written walkthrough: <b>62</b>.</p><p>Examples of manual domain calls for this chapter's questions:</p><ul><li><b>#3</b> — SEC: restricting bucket access to the organization</li><li><b>#28</b> — SEC: single sign-on and federation with AD</li><li><b>#31</b> — SEC: enforcing resource tag compliance</li><li><b>#168</b> — SEC: restricting services across all accounts</li><li><b>#412</b> — SEC: blocking public access across all buckets</li><li><b>#433</b> — SEC: blocking tag changes via SCP</li><li><b>#525</b> — COST: programmatic access to costs and forecasts</li><li><b>#548</b> — SEC: restricting the set of services per account</li></ul>","text":" Cost The \"Design Cost Optimized Architectures\" domain accounts for a fifth of the exam, but cost considerations show up in nearly every question: phrasing like \"MOST cost effective,\" \"minimize cost,\" \"as low as possible\" decides between technically equivalent options. What makes up the bill Category What's billed How it's reduced Compute instance hours or seconds, Lambda GB seconds, Aurora ACUs Savings Plans, RI, Spot, right sizing, shutting down idle resources Storage GB per month by class, snapshots, backups storage classes, lifecycle, deleting old snapshots Traffic outbound to the internet, between AZs, between regions, through NAT CloudFront, endpoints, traffic locality Requests and operations API calls, RCU/WCU, load balancer LCUs, KMS requests batching, caching, Bucket Keys Managed services hourly rates for NAT, TGW attachments, interface endpoints consolidation, dropping unnecessary components Inbound traffic into AWS is free; outbound traffic costs money. Cross AZ traffic is billed in both directions; traffic within a single AZ over private addresses is free. That explains a good half of the \"cheap\" answers: removing cross zone hops, serving content through CloudFront, replacing NAT with a gateway endpoint. Compute pricing models Quick guide to the choice: steady 24/7 workload → Savings Plans (Compute for flexibility, EC2 Instance for the maximum discount) or Reserved Instances; interruptible batch jobs → Spot, up to 90 percent cheaper; infrequent short lived tasks → Lambda or Fargate, you pay only for execution; development environments → a start/stop schedule (Instance Scheduler), Aurora Serverless v2, stopping RDS; spikes on top of a baseline → On Demand or Spot layered on top of a reserved base. Tools Cost Explorer — analyzes and forecasts spend, breaks it down by service, tag, and account, and recommends RI and Savings Plans; AWS Budgets — budgets by cost, usage, or RI coverage, with notifications and automated actions (for example, applying a restrictive policy); Cost and Usage Report — line item detail in S3 for analysis in Athena and QuickSight; Compute Optimizer — recommendations on instance types, EBS volumes, Lambda functions, and ASGs based on actual metrics; Trusted Advisor — checks across five categories, including idle resources and unused RIs (the full set of checks requires Business or Enterprise support); S3 Storage Lens and Storage Class Analysis — what to move to colder storage classes; Billing Conductor and cost allocation tags — splitting spend across teams. Division of labor: Budgets warns and acts on a threshold, Cost Explorer explains the past and forecasts the future, Compute Optimizer says what to downsize, Trusted Advisor finds the obvious waste. Common architecture findings Symptom Cheap fix Traffic from private subnets to S3 goes through NAT gateway endpoint One NAT Gateway serves three AZs a NAT per AZ (less cross AZ traffic) Serving static content from EC2 S3 plus CloudFront Logs and archives have sat in S3 Standard for years lifecycle into Glacier or Deep Archive Snapshots pile up with no policy Data Lifecycle Manager, AWS Backup Dev instances run overnight and on weekends a stop schedule Oversized instances Compute Optimizer, move to gp3, T family Lots of small objects in Standard IA move back to Standard or aggregate them Separate accounts buying RIs independently consolidated billing in Organizations A database cluster sits idle at night Aurora Serverless v2 or stopping it The cost of reliability The exam often pits cost against fault tolerance. Rules of thumb: One Zone classes and a single AZ are cheaper but don't survive a zone outage; pilot light is cheaper than warm standby but recovers slower; replication to another region always adds both storage and traffic cost. If a question contains both \"cost effective\" and \"highly available,\" pick the cheapest option among those that actually meet the availability requirement — not the cheapest option overall. What the exam asks 1. \"Reduce the cost of a steady round the clock workload\" — Savings Plans or RI. 2. \"Processing can be interrupted\" — Spot. 3. \"Data is read once a year, retrieval within hours is acceptable\" — Glacier Deep Archive. 4. \"The NAT bill is too high\" — endpoints for S3 and DynamoDB. 5. \"Need a warning when the budget is exceeded\" — AWS Budgets with SNS. 6. \"Understand which instances are oversized\" — Compute Optimizer. 7. \"Split spend across teams\" — cost allocation tags and Cost Explorer. 8. \"Many accounts, want shared discounts\" — Organizations with consolidated billing. How to read the wording Wording in the question Answer \"steady 24/7 workload, lowest cost\" Savings Plans or RI \"fault tolerant batch jobs\" Spot \"accessed once a year, retrieval in hours is fine\" Glacier Deep Archive \"NAT gateway data processing charges\" gateway endpoints for S3 and DynamoDB \"alert when spending exceeds a threshold\" AWS Budgets \"which instances are oversized\" Compute Optimizer \"allocate costs to teams\" cost allocation tags and Cost Explorer \"multiple accounts, volume discounts\" Organizations with consolidated billing Order of operations for optimizing an architecture 1. Remove idle resources: unattached Elastic IPs, old snapshots, unused volumes, test environments running overnight. 2. Right size: Compute Optimizer, gp3 instead of gp2, current instance generations. 3. Cover the baseline load with commitments: Savings Plans, RI, leaving spikes to On Demand and Spot. 4. Move data to the right storage class and turn on lifecycle policies. 5. Cut unnecessary traffic: CloudFront for delivery, endpoints instead of NAT, AZ locality. 6. Replace self managed components with managed ones wherever that's cheaper on a total cost of ownership basis (RDS instead of a database on EC2, Fargate instead of a node cluster). 7. Put controls in place: budgets, tags, regular review with Cost Explorer and Trusted Advisor. Mini practicum 1. The NAT Gateway bill exceeds the cost of the instances themselves; most of the traffic goes to S3. → A gateway endpoint for S3: traffic stops going through NAT and isn't billed. 2. Test environments run around the clock but are only used from 9 to 6 on weekdays. → A start/stop schedule (Instance Scheduler or EventBridge with SSM Automation). 3. Five accounts buy reservations separately and don't fully utilize them. → An organization with consolidated billing: discounts and RI coverage are shared across accounts. 4. Five years of log archives sit in S3 Standard, read once a year for an audit request. → Lifecycle into Glacier Deep Archive, if retrieval within hours is acceptable. Related chapters Compute purchasing models — the EC2 chapter. Storage classes and lifecycle — the S3 chapter. Traffic, NAT, and endpoints — the VPC chapter. Caching and reducing outbound traffic — the CloudFront chapter. Serverless database options — the Aurora and DynamoDB chapters. Wording traps The word \"cost effective\" in a question doesn't mean \"the cheapest option on the list\": it means \"the cheapest option among those that meet all the other requirements.\" If \"highly available\" or \"no data loss\" appears alongside it, single AZ options with no replication are eliminated, even if they're cheaper. The reverse trap is \"operational overhead.\" A managed service is often more expensive on the bill, but wins when the requirement is phrased around effort, not money. Read carefully what's actually supposed to be minimized: cost, recovery time, or the amount of administration. Numbers to remember commitments run for 1 or 3 years: Compute Savings Plans up to 66% savings, EC2 Instance Savings Plans and standard Reserved Instances up to 72%, convertible ones around 54%, Spot Instances up to 90%; all upfront is cheaper than partial upfront, partial upfront is cheaper than no upfront; cost data refreshes no more than once a day, so no tool gives instant spend visibility; Cost Explorer shows 12 months of history and forecasts 12 months ahead; hourly granularity and resource level data are opt in add ons that cost extra; the first two budgets in AWS Budgets are free; each additional budget is billed per day; cost allocation tags must be activated manually and don't work retroactively: they only appear in reports from the moment of activation; cross AZ traffic within a region is billed; traffic within a single AZ over private addresses is free; a NAT gateway is billed both hourly and per gigabyte, which is why cost saving questions often replace it with a gateway endpoint. Bank audit {{q:245}} — disputed: cutting cost on a dev environment; changing the target group doesn't reduce the ASG's instance count. {{q:308}} — low confidence: Trusted Advisor recommendations on RDS RIs. {{q:574}} — disputed: two hours of use per week; Serverless v2 vs. stopping a provisioned cluster. {{q:851}} — disputed: Aurora Serverless scales down when idle. {{q:835}} — disputed: public VIF vs. an interface endpoint for access to S3. "},{"id":"appendix-domains","title":"Appendix — how a question's domain is determined","order":23,"svc":[],"questions":[],"html":"<h1>How a question's domain is determined</h1>\n<p>The SAA-C03 exam is split into four domains with fixed weights. Knowing which domain a question belongs to is useful for more than statistics: the domain tells you which requirement in the text is the deciding one, and therefore which answer option is correct. The same service can be the right answer in different questions for entirely different reasons.</p>\n<div class='table-wrap'><table><thead><tr><th>Code</th><th>Domain</th><th>Exam weight</th><th>Questions in the bank</th></tr></thead><tbody><tr><td>SEC</td><td>Design Secure Architectures</td><td>30%</td><td>324</td></tr><tr><td>RES</td><td>Design Resilient Architectures</td><td>26%</td><td>209</td></tr><tr><td>PERF</td><td>Design High-Performing Architectures</td><td>24%</td><td>259</td></tr><tr><td>COST</td><td>Design Cost-Optimized Architectures</td><td>20%</td><td>227</td></tr></tbody></table></div>\n<p>The bank's shares don't match the exam weights, and that's expected: the dump wasn't assembled against the blueprint. \"Exam\" mode in the trainer draws questions using the weights in the table above, not proportionally to the bank.</p>\n<h2>Where the bank's labeling comes from</h2>\n<p>Domain labeling isn't part of the source dump — it was added on top of the bank in two passes.</p>\n<p><strong>First pass, rule-based.</strong> The script <code>scripts/classify.py</code> looks for requirement markers in the question text. This labeled 672 questions — the ones where the wording leaves no real choice.</p>\n<div class='table-wrap'><table><thead><tr><th>Domain</th><th>Markers the rule fires on</th></tr></thead><tbody><tr><td>COST</td><td><code>cost-effective</code>, <code>minimize cost</code>, <code>lowest cost</code>, <code>budget</code>, <code>Savings Plans</code>, <code>Reserved Instance</code>, <code>Spot</code>, <code>data transfer charges</code></td></tr><tr><td>SEC</td><td><code>encrypt</code>, <code>KMS</code>, <code>IAM</code>, <code>least privilege</code>, <code>unauthorized</code>, <code>public access</code>, <code>WAF</code>, <code>compliance</code>, <code>PII</code>, <code>MFA</code>, <code>must not traverse the internet</code>, <code>cross-account</code></td></tr><tr><td>RES</td><td><code>highly available</code>, <code>fault tolerant</code>, <code>disaster recovery</code>, <code>RPO</code>, <code>RTO</code>, <code>failover</code>, <code>durable</code>, <code>Multi-AZ</code>, <code>backup</code>, <code>must not lose data</code>, <code>decouple</code></td></tr><tr><td>PERF</td><td><code>latency</code>, <code>throughput</code>, <code>caching</code>, <code>IOPS</code>, <code>concurrency</code>, <code>real-time</code>, <code>bottleneck</code>, <code>response time</code></td></tr></tbody></table></div>\n<p><strong>Second pass, manual.</strong> The remaining 347 questions had no markers: they either use neutral phrasing like \"which solution will meet these requirements,\" or carry several requirements at once. Each such question was read individually, its domain assigned by hand, with a short justification recorded alongside — visible in the trainer's walkthrough under the answer. No question in the bank is left with a domain assigned \"by default.\"</p>\n<h2>The core principle: discriminator, not topic</h2>\n<p>The domain is determined by <strong>the requirement that rules out the wrong options</strong>, not by the question's topic or the motive stated in the first paragraph. This is the most common mistake when labeling on your own, and at the same time the most useful skill on the exam itself: finding the phrase in the text that the question was actually written around.</p>\n<p>Three examples from the bank where the topic and the discriminator diverge:</p>\n<ul><li>a question about S3 and a VPC endpoint. If the text says \"traffic must not go out to the internet\" — that's SEC, because isolation is what's being decided. If it says \"the NAT gateway bill is too high\" — that's COST, because traffic cost is what's being decided. The service in the answer is the same either way;</li><li>a question about blocking large instance launches across all accounts. The motive in the preamble is financial (idle instances), but the options differ by mechanism, and the correct one is an SCP in Organizations. This is SEC: an organizational guardrail;</li><li>a question about choosing a Storage Gateway type. The company \"doesn't want to buy a new array,\" but the options differ by gateway type, and the type is determined by the clients' protocol and which data needs to stay local. This is PERF, not COST.</li></ul>\n<h2>Tie-breaking rules</h2>\n<p>When a requirement honestly pulls toward two domains, these rules apply. They were derived while re-checking disputed calls and are recorded here to keep labeling consistent.</p>\n<div class='table-wrap'><table><thead><tr><th>Situation</th><th>Domain</th><th>Why</th></tr></thead><tbody><tr><td>Choosing a storage type: EFS vs. EBS, FSx type, Storage Gateway type</td><td>PERF</td><td>Decided by protocol and access pattern</td></tr><tr><td>Choosing a storage class and lifecycle: IA, Glacier, Deep Archive</td><td>COST</td><td>Decided by storage price and acceptable retrieval time</td></tr><tr><td>Organizational guardrail: SCP, Control Tower, Config, inventory</td><td>SEC</td><td>Decided by governance and permission restriction</td></tr><tr><td>Tagging for cost allocation</td><td>COST</td><td>Decided by cost accounting</td></tr><tr><td>Moving state out of instances: sessions, shared documents</td><td>RES</td><td>Decided by surviving instance replacement</td></tr><tr><td>Shared storage for speed, growth, or consistency</td><td>PERF</td><td>Decided by access performance</td></tr><tr><td>Choosing compute for a task's duration and profile</td><td>PERF</td><td>Decided by matching the resource to the task</td></tr><tr><td>Guaranteeing capacity, surviving a zone or region outage</td><td>RES</td><td>Decided by availability</td></tr><tr><td>A queue or bus for \"don't lose it\" and \"decouple\"</td><td>RES</td><td>Decided by exchange resilience</td></tr><tr><td>Choosing an analytics service for the data and queries</td><td>PERF</td><td>Decided by the service's fit for the workload</td></tr></tbody></table></div>\n<h2>Wording clues</h2>\n<p>The exam almost always highlights the key requirement in all caps or a superlative. Read the last paragraph of the question first.</p>\n<div class='table-wrap'><table><thead><tr><th>Wording</th><th>What it means</th></tr></thead><tbody><tr><td>MOST cost-effectively</td><td>among the options that meet all requirements, pick the cheap one — not the cheapest overall</td></tr><tr><td>LEAST operational overhead</td><td>less administration: a managed service over a self-managed one</td></tr><tr><td>MOST securely</td><td>the option with the fewest permissions granted and no public access</td></tr><tr><td>Highly available, fault tolerant</td><td>at least two Availability Zones, automatic replacement of a failed component</td></tr><tr><td>MOST performant, lowest latency</td><td>closer to the user, caching, the right resource class</td></tr><tr><td>Without changing application code</td><td>look for a drop-in replacement transparent to the application: DAX, RDS Proxy, Babelfish, alias</td></tr><tr><td>No servers to manage</td><td>Lambda, Fargate, Aurora Serverless, managed services</td></tr></tbody></table></div>\n<h2>How to use this while studying</h2>\n<ol><li>Practice by domain: the \"Domain\" filter in training limits the question pool to one domain, and weak spots show up faster than with random ordering.</li><li>Look at the justification in the walkthrough. If you assigned a question to a different domain but still picked the right answer — that's not a mistake, just a different framing of the same thing. If the domains diverged and the answer is wrong — you were probably reading the wrong requirement.</li><li>Keep the weights in mind: on the real exam SEC accounts for almost a third of the questions, so IAM, KMS, VPC isolation, and access policies pay off the most.</li><li>Every chapter of the handbook has an automatic \"Domains for this chapter's questions\" block showing which domain the chapter's topic most often turns out to be.</li></ol>\n<h2>What to do if you disagree with a label</h2>\n<p>Labeling lives in <code>data/dom-overrides.json</code>, in the format <code>{\"id\": {\"dom\": \"SEC\", \"why\": \"short justification\"}}</code>, and is applied at build time. Changing one line and rebuilding changes the question's domain, which filters it falls into, and whether it appears in the exam-mode draw. Build checks require every manual label to carry a justification, and no question in the bank to be left without a rationale for its wording.</p>","text":" How a question's domain is determined The SAA C03 exam is split into four domains with fixed weights. Knowing which domain a question belongs to is useful for more than statistics: the domain tells you which requirement in the text is the deciding one, and therefore which answer option is correct. The same service can be the right answer in different questions for entirely different reasons. Code Domain Exam weight Questions in the bank SEC Design Secure Architectures 30% 324 RES Design Resilient Architectures 26% 209 PERF Design High Performing Architectures 24% 259 COST Design Cost Optimized Architectures 20% 227 The bank's shares don't match the exam weights, and that's expected: the dump wasn't assembled against the blueprint. \"Exam\" mode in the trainer draws questions using the weights in the table above, not proportionally to the bank. Where the bank's labeling comes from Domain labeling isn't part of the source dump — it was added on top of the bank in two passes. First pass, rule based. The script scripts/classify.py looks for requirement markers in the question text. This labeled 672 questions — the ones where the wording leaves no real choice. Domain Markers the rule fires on COST cost effective , minimize cost , lowest cost , budget , Savings Plans , Reserved Instance , Spot , data transfer charges SEC encrypt , KMS , IAM , least privilege , unauthorized , public access , WAF , compliance , PII , MFA , must not traverse the internet , cross account RES highly available , fault tolerant , disaster recovery , RPO , RTO , failover , durable , Multi AZ , backup , must not lose data , decouple PERF latency , throughput , caching , IOPS , concurrency , real time , bottleneck , response time Second pass, manual. The remaining 347 questions had no markers: they either use neutral phrasing like \"which solution will meet these requirements,\" or carry several requirements at once. Each such question was read individually, its domain assigned by hand, with a short justification recorded alongside — visible in the trainer's walkthrough under the answer. No question in the bank is left with a domain assigned \"by default.\" The core principle: discriminator, not topic The domain is determined by the requirement that rules out the wrong options , not by the question's topic or the motive stated in the first paragraph. This is the most common mistake when labeling on your own, and at the same time the most useful skill on the exam itself: finding the phrase in the text that the question was actually written around. Three examples from the bank where the topic and the discriminator diverge: a question about S3 and a VPC endpoint. If the text says \"traffic must not go out to the internet\" — that's SEC, because isolation is what's being decided. If it says \"the NAT gateway bill is too high\" — that's COST, because traffic cost is what's being decided. The service in the answer is the same either way; a question about blocking large instance launches across all accounts. The motive in the preamble is financial (idle instances), but the options differ by mechanism, and the correct one is an SCP in Organizations. This is SEC: an organizational guardrail; a question about choosing a Storage Gateway type. The company \"doesn't want to buy a new array,\" but the options differ by gateway type, and the type is determined by the clients' protocol and which data needs to stay local. This is PERF, not COST. Tie breaking rules When a requirement honestly pulls toward two domains, these rules apply. They were derived while re checking disputed calls and are recorded here to keep labeling consistent. Situation Domain Why Choosing a storage type: EFS vs. EBS, FSx type, Storage Gateway type PERF Decided by protocol and access pattern Choosing a storage class and lifecycle: IA, Glacier, Deep Archive COST Decided by storage price and acceptable retrieval time Organizational guardrail: SCP, Control Tower, Config, inventory SEC Decided by governance and permission restriction Tagging for cost allocation COST Decided by cost accounting Moving state out of instances: sessions, shared documents RES Decided by surviving instance replacement Shared storage for speed, growth, or consistency PERF Decided by access performance Choosing compute for a task's duration and profile PERF Decided by matching the resource to the task Guaranteeing capacity, surviving a zone or region outage RES Decided by availability A queue or bus for \"don't lose it\" and \"decouple\" RES Decided by exchange resilience Choosing an analytics service for the data and queries PERF Decided by the service's fit for the workload Wording clues The exam almost always highlights the key requirement in all caps or a superlative. Read the last paragraph of the question first. Wording What it means MOST cost effectively among the options that meet all requirements, pick the cheap one — not the cheapest overall LEAST operational overhead less administration: a managed service over a self managed one MOST securely the option with the fewest permissions granted and no public access Highly available, fault tolerant at least two Availability Zones, automatic replacement of a failed component MOST performant, lowest latency closer to the user, caching, the right resource class Without changing application code look for a drop in replacement transparent to the application: DAX, RDS Proxy, Babelfish, alias No servers to manage Lambda, Fargate, Aurora Serverless, managed services How to use this while studying 1. Practice by domain: the \"Domain\" filter in training limits the question pool to one domain, and weak spots show up faster than with random ordering. 2. Look at the justification in the walkthrough. If you assigned a question to a different domain but still picked the right answer — that's not a mistake, just a different framing of the same thing. If the domains diverged and the answer is wrong — you were probably reading the wrong requirement. 3. Keep the weights in mind: on the real exam SEC accounts for almost a third of the questions, so IAM, KMS, VPC isolation, and access policies pay off the most. 4. Every chapter of the handbook has an automatic \"Domains for this chapter's questions\" block showing which domain the chapter's topic most often turns out to be. What to do if you disagree with a label Labeling lives in data/dom overrides.json , in the format {\"id\": {\"dom\": \"SEC\", \"why\": \"short justification\"}} , and is applied at build time. Changing one line and rebuilding changes the question's domain, which filters it falls into, and whether it appears in the exam mode draw. Build checks require every manual label to carry a justification, and no question in the bank to be left without a rationale for its wording. "}]};
